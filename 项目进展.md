# 项目进展

## 当前状态

第一版定位为 `SP 广告健康监控与决策辅助系统`。

系统不让 AI 自动执行广告动作，只做：

- 异常健康监控
- 规则判定
- AI 解释与证据整理
- 人工采纳 / 修改后采纳 / 拒绝 / 加入观察
- 处理记录与复盘

## 已确认决策

- 第一版只做 `SP 商品广告`
- 默认判断周期为 `近 30 天`
- 直接接积加开放平台 API
- 第一版数据库使用 `SQLite`
- 产品目标由人工设置
- 产品目标分类采用：
  - 测试词款
  - 放量款
  - 利润款
  - 排名承接款
  - 清库存款
  - 止损款
- 建议颗粒度：
  - 产品层承载经营目标
  - 关键词 / 搜索词层承载具体异常和建议
- 第一版页面采用 5 页：
  - 广告健康驾驶舱
  - 产品目标与规则设置
  - 异常与 AI 建议队列
  - 建议详情与溯源
  - 处理记录与复盘

## 2026-06-09

### TASK-071 已完成：产品中心广告覆盖状态分层

- 已确认问题：销售表现接口同步的是销售产品池，不等于 SP 广告投放产品池。
- 已新增产品中心“产品广告覆盖状态”汇总，区分有广告覆盖、有 SP 数据待归因、未投放 / 无 SP 数据。
- 当前真实页面显示：有广告覆盖 1 个、有 SP 数据待归因 0 个、未投放 / 无 SP 数据 217 个。
- 未投放 / 无 SP 数据产品只作为销售产品档案展示，不进入广告调优待办，不再展示“管理归因”和“设置目标 / 规则”主操作。
- 已通过 `python scripts\smoke_frontend_product_ad_coverage_status.py`、`python scripts\smoke_frontend_product_goal_rule_guidance.py`、`python scripts\smoke_frontend_product_center_attribution_status.py`、`python scripts\smoke_real_e2e.py` 和 `frontend` 目录 `npm run build`。
- Product Design 复核结论：分层解决了销售产品池和广告投放产品池混淆的问题；后续建议增加广告覆盖状态筛选，并把“未投放 / 无 SP 数据”的解释进一步收敛为“本系统暂无 SP 覆盖证据”。

### TASK-072 已完成：产品中心广告覆盖筛选与术语精修

- 已启动 `TASK-072`。
- 本任务只做产品中心前端筛选和术语精修，不改后端、不写真实库、不改变广告归因规则。
- 目标是把“未投放 / 无 SP 数据”的主文案收敛为“本系统暂无 SP 覆盖证据”，避免把系统证据不足误写成业务已确认未投放。
- 已新增“广告覆盖状态”筛选控件，默认“全部覆盖状态”，可筛选“有广告覆盖”“有 SP 数据待归因”“本系统暂无 SP 覆盖证据”。
- 已通过 `python scripts\smoke_frontend_product_ad_coverage_filter.py`、`python scripts\smoke_frontend_product_ad_coverage_status.py`、`python scripts\smoke_frontend_product_goal_rule_guidance.py`、`python scripts\smoke_real_e2e.py` 和 `frontend` 目录 `npm run build`。
- 浏览器 DOM 复核确认产品中心可见筛选控件，首屏销售产品行显示“本系统暂无 SP 覆盖证据”和“只作为销售产品档案，不进入广告调优待办”。
- Product Design 复核结论：筛选位置合适，术语更准确；新发现的问题是“本系统暂无 SP 覆盖证据”的产品行仍可看到产品目标和规则字段输入框，后续应单独收敛为只读展示。

### TASK-073 已完成：无 SP 覆盖产品目标字段只读收敛

- 已启动 `TASK-073`。
- 本任务只处理产品中心行内目标 / 规则字段，不改后端、不写真实库、不改变产品归因规则。
- 目标是让“本系统暂无 SP 覆盖证据”的销售档案产品不再显示广告目标 / 规则输入框。
- 已新增无 SP 覆盖产品只读渲染逻辑：产品目标、目标 ACOS、目标 CVR、最小点击、最小花费、最小订单、最大 CPC、库存阈值、备注这些广告目标 / 规则列改为显示“只读销售档案”和“不配置广告目标 / 规则”。
- 有广告覆盖或有 SP 数据待归因产品仍保留人工目标 / 规则编辑能力。
- 浏览器 DOM 复核确认真实产品中心首屏无 SP 覆盖产品显示“只读销售档案”和“本系统暂无 SP 覆盖证据，不配置广告目标 / 规则”。
- 已通过 `python scripts\smoke_frontend_product_no_sp_readonly_rules.py`、`python scripts\smoke_frontend_product_ad_coverage_filter.py`、`python scripts\smoke_frontend_product_goal_rule_guidance.py`、`python scripts\smoke_real_e2e.py` 和 `frontend` 目录 `npm run build`。
- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- Product Design 复核结论：无 SP 覆盖产品现在被表达为只读销售档案，能避免运营把销售表现产品误当成广告调优对象。
- Product Design 复核发现的新问题：同一句只读说明在多列重复出现会增加横向表格噪音，后续应把产品中心收敛为“广告调优对象 / 销售档案”两个更轻视图。

### TASK-074 已完成：产品中心广告调优对象与销售档案视图收敛

- 已新增“产品中心视图”，默认展示“广告调优对象”。
- “广告调优对象”仅展示有广告覆盖或有 SP 数据待归因的产品；当前真实页面默认表格为 1 条 RBK004 广告对象。
- “销售档案”仅展示“本系统暂无 SP 覆盖证据”的产品，并隐藏广告目标 / 规则相关列。
- 浏览器复核确认“销售档案”首屏显示 CHG001 等销售表现产品，表头不再包含产品目标、目标 ACOS、最小点击、备注、操作等广告规则列。
- 切换产品中心视图时会重置广告覆盖状态筛选为“全部覆盖状态”。
- 已通过 `python scripts\smoke_frontend_product_center_view_scope.py`、`python scripts\smoke_frontend_product_no_sp_readonly_rules.py`、`python scripts\smoke_frontend_product_ad_coverage_filter.py`、`python scripts\smoke_real_e2e.py` 和 `frontend` 目录 `npm run build`。
- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- Product Design 复核结论：默认进入广告调优对象视图能把运营注意力聚焦到真正需要广告判断的产品，销售档案视图隐藏广告规则列后不再用重复只读说明制造噪音。
- Product Design 复核发现的新问题：“产品中心视图”和“广告覆盖状态”两个筛选仍有一定认知重叠，后续可继续合并或弱化覆盖状态筛选。

### TASK-075 已完成：产品中心筛选层级继续减负

- 已让“广告覆盖状态”筛选只在“全部产品”视图展示。
- 默认“广告调优对象”视图不再展示广告覆盖状态筛选，避免和视图范围冲突。
- “销售档案”视图不再展示广告覆盖状态筛选，因为该视图已经等价于“本系统暂无 SP 覆盖证据”范围。
- 产品广告覆盖状态汇总卡继续保留，用来说明销售表现产品不等于广告投放产品。
- 浏览器复核确认默认“广告调优对象”视图 `coverageControlCount=0`，切到“全部产品”后 `coverageControlCount=1` 且显示“全部覆盖状态”，再切回后控件消失。
- 已通过 `python scripts\smoke_frontend_product_center_filter_reduction.py`、`python scripts\smoke_frontend_product_center_view_scope.py`、`python scripts\smoke_frontend_product_ad_coverage_filter.py`、`python scripts\smoke_real_e2e.py` 和 `frontend` 目录 `npm run build`。
- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- Product Design 复核结论：筛选层级更轻，默认视图只剩“产品中心视图”这个主决策，不再让运营在广告调优对象里看到互相冲突的覆盖状态筛选。
- Product Design 复核发现：产品中心筛选已基本收敛，下一步应回到真实广告对象目标 / 规则设置后的异常生成和 AI 建议主链路验收。

### TASK-076 已完成：产品中心广告调优对象 API 状态固化

- 已确认问题：销售表现接口同步的是销售产品池，不等于 SP 广告投放产品池；只靠前端临时判断容易让后续页面误用产品列表。
- 新增 [smoke_product_ad_object_scope_api.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_product_ad_object_scope_api.py)，先红灯确认产品 API 缺少 `ad_coverage_status`，再实现转绿。
- 产品 API 新增 `ad_coverage_status` 和 `is_ad_tuning_eligible` 两个派生字段。
- 状态口径：有活跃人工归因规则为 `attributed`；无归因但有当前周期 SP 指标为 `sp_unattributed`；无归因且无 SP 指标为 `not_advertised`。
- 前端 `Product` 类型已补字段，产品中心优先使用 API 字段，保留本地计算兜底。
- 真实本地 API `/api/products?market_id=1` 已确认销售表现产品返回 `ad_coverage_status=not_advertised`、`is_ad_tuning_eligible=false`。
- 已重启本地开发服务：后端 `http://127.0.0.1:8001`，前端 `http://127.0.0.1:5173`。
- 已通过 `python scripts\smoke_product_ad_object_scope_api.py`、`python scripts\smoke_frontend_product_center_view_scope.py`、`python scripts\smoke_frontend_product_ad_coverage_status.py`、`python scripts\smoke_real_e2e.py` 和 `frontend` 目录 `npm run build`。
- 重启后已再次通过 `python scripts\smoke_real_e2e.py`。
- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、真实 `data/app.db`。
- 未新增任何广告执行能力。
- Product Design 复核结论：产品中心对象边界已从“前端猜测”提升为“API 明确返回”，后续搜索词分析、异常队列和复盘页面可以复用同一口径；销售表现产品保留为档案，但默认不会进入广告调优。
- 下一步建议：`TASK-077：真实广告调优对象目标规则后的异常生成验收`。

### TASK-077 已完成：真实广告调优对象目标规则后的异常建议链路验收

- 新增 [smoke_real_ad_object_anomaly_chain.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_ad_object_anomaly_chain.py)，先红灯确认脚本不存在，再实现转绿。
- 脚本复制真实 `data/app.db` 到 `%TEMP%\amazon_ad_analysis_task077\app_task077.db`，所有目标 / 规则写入和异常建议生成都只发生在临时库。
- 首次使用项目 `tmp/` 目录时出现 SQLite `disk I/O error`；已确认同目录最小 SQLite 写入也失败，改用 `%TEMP%` 后通过。
- 真实库当前有活跃归因广告对象：`RBK004 kids sunglasses / MSKU RBK004 / product_id=2`。
- 真实库当前缺少该产品的人工目标和规则：`real_had_goal=false`、`real_had_rule=false`。
- 临时库模拟人工目标 / 规则：`goal_type=profit`、`target_acos=0.1378`。
- 临时库基于真实 SP 指标生成 `acos_worse` 异常：点击 138、花费 150.47、订单 52、销售额 545.79、ACOS 27.57%。
- 临时库为该异常生成 AI 建议，包含 `source_trace_json`、`evidence_summary`、`recommended_manual_decision=adopt`。
- 脚本确认真实 `data/app.db` 修改时间未变化。
- 已通过 `python scripts\smoke_real_ad_object_anomaly_chain.py`、`python scripts\smoke_real_no_demo_anomaly_objects.py`、`python scripts\smoke_real_e2e.py`。
- `python scripts\smoke_real_no_demo_anomaly_objects.py` 仍返回真实主流程异常数和建议数为 0，说明临时链路验收没有污染真实队列。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、前端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- Product Design 复核结论：真实队列为空的原因现在被清楚定位为“运营尚未人工设置目标 / 规则”，而不是数据链路或 AI 建议链路不可用。
- 下一步建议：`TASK-078：真实目标 / 规则保存后的异常建议入队验收`。

### TASK-078 已完成：真实目标规则保存后的异常建议入队门禁

- 新增 [smoke_real_goal_rule_queue_gate.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_goal_rule_queue_gate.py)，先红灯确认脚本不存在，再实现转绿。
- 脚本使用 SQLite 只读 URI 打开真实 `data/app.db`，并通过修改时间确认未写真实库。
- 真实库当前找到 1 个广告调优对象：`RBK004 kids sunglasses / MSKU RBK004 / product_id=2`，活跃归因规则数为 1。
- 当前真实广告指标：点击 138、花费 150.47、订单 52、销售额 545.79、ACOS 27.57%。
- 当前门禁输出 `workflow_status=needs_manual_goal_rule`、`queue_ready=false`。
- 当前缺口为 `product_goal` 和 `target_acos`，需要运营在前端产品中心人工保存产品目标和目标 ACOS 等规则门槛。
- 已通过 `python scripts\smoke_real_goal_rule_queue_gate.py`、`python scripts\smoke_real_no_demo_anomaly_objects.py`、`python scripts\smoke_real_e2e.py`。
- `python scripts\smoke_real_no_demo_anomaly_objects.py` 仍返回真实异常数和建议数为 0，符合“缺人工目标 / 规则，不应入队”的当前状态。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、前端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- Product Design 复核结论：门禁把“当前队列为空”翻译成可执行前置任务，即为 RBK004 广告调优对象补产品目标和目标 ACOS，而不是让运营猜系统哪里坏了。
- 下一步建议：`TASK-079：前端人工目标规则设置引导收敛`。

### TASK-079 已完成：前端人工目标规则设置引导收敛

- 新增 [smoke_frontend_real_goal_rule_gate_guidance.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_real_goal_rule_gate_guidance.py)，先红灯确认脚本不存在，再实现转绿。
- [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 新增 `getProductGoalRuleMissingItems`，明确展示 `缺少产品目标` 和 `缺少目标 ACOS`。
- `needsProductGoalRuleSetup` 已收敛为：必须具备广告调优资格、存在 SP 指标、且缺少人工目标或目标 ACOS。
- `productsNeedingGoalRuleSetup` 只在“广告调优对象”视图中从当前 `productCenterProducts` 表格范围筛选，不再从全量销售表现产品中提示。
- 引导卡片展示“需要人工设置产品目标 / 规则”、当前 SP 指标、缺失项标签和“设置目标 / 规则”入口。
- 文案明确只引导运营人工设置，不会自动保存目标 / 规则，也不会自动修改广告。
- 已通过 `python scripts\smoke_frontend_real_goal_rule_gate_guidance.py`、`python scripts\smoke_frontend_product_goal_rule_drawer.py`、`python scripts\smoke_real_goal_rule_queue_gate.py`、`python scripts\smoke_real_e2e.py` 和 `frontend` 目录 `npm run build`。
- `python scripts\smoke_real_goal_rule_queue_gate.py` 当前仍返回 `workflow_status=needs_manual_goal_rule`、`queue_ready=false`，缺口为 `product_goal` 和 `target_acos`。
- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- Product Design 复核结论：目标 / 规则缺口现在只暴露给广告调优对象，销售表现同步来的普通产品继续作为销售档案，不会被催促设置广告规则。
- Product Design 复核发现：下一步应由运营为 `RBK004 kids sunglasses / MSKU RBK004` 人工保存目标和目标 ACOS 后，再验收真实异常与 AI 建议是否入队。
- 下一步建议：`TASK-080：真实目标规则保存后异常建议入队验收`。

### 已完成

- 创建开发文档：[开发文档.md](D:/DataProject/coedx_project/Amazon广告分析/开发文档.md)
- 创建后端最小骨架：
  - FastAPI 入口
  - SQLite 初始化
  - `.env` 配置读取
  - 积加开放平台客户端
  - SP 关键词报表同步服务
  - smoke 同步脚本
- 核对积加官方 API 文档并修正认证方式：
  - 使用 `AppID` + `AppKey` 获取 `accessToken`
  - `获取accessToken` 实际地址为 `/api/open/api_token`
  - 业务接口实际地址为 `GERPGO_BASE_URL + /open + apiUrl`
  - 业务接口请求头使用 `accessToken`
- 跑通真实 SP 关键词报表同步

### 验证结果

- `marketId`: 1
- 同步周期：`2026-05-27` 至 `2026-06-09`
- 同步接口：`/operation/ads/spKeywordsPage/query`
- 同步结果：成功
- 写入 `sp_keyword_metrics`: 30 条
- 原始响应文件：[sp_keywords_1_2026-05-27_2026-06-09_20260609211054.json](D:/DataProject/coedx_project/Amazon广告分析/data/raw/sp_keywords_1_2026-05-27_2026-06-09_20260609211054.json)
- Python 语法检查：`ast ok 15`

### 问题与处理

- 积加接口连续分页过快会返回 `code=90008`，提示接口调用次数超过限制。
- 已在分页请求之间加入约 `1.2s` 间隔。
- smoke 同步默认限制为 `count=10`、`max_pages=3`，先验证连通和落库，不做全量同步。
- 当前目录不是 Git 仓库，暂未做提交记录。

### 下一步

- 基于真实 SP 关键词数据实现规则引擎。
- 第一批异常类型建议先做：
  - ACOS 异常
  - 点击多无订单
  - CVR 下滑
  - 花费异常
  - 曝光异常
- 规则引擎输出后，再接 AI 解释与建议详情页。

## 2026-06-10

### 已完成

- 建立目标模式开发控制层：
  - [AGENTS.md](D:/DataProject/coedx_project/Amazon广告分析/AGENTS.md)
  - [目标模式运行手册.md](D:/DataProject/coedx_project/Amazon广告分析/目标模式运行手册.md)
  - [开发任务清单.md](D:/DataProject/coedx_project/Amazon广告分析/开发任务清单.md)
  - [当前任务.md](D:/DataProject/coedx_project/Amazon广告分析/当前任务.md)
  - [project_state.json](D:/DataProject/coedx_project/Amazon广告分析/project_state.json)
  - [review_checklist.md](D:/DataProject/coedx_project/Amazon广告分析/review_checklist.md)
- 当前任务已锁定为 `TASK-002：审查并收敛 SP 异常规则引擎`。
- 完成 `TASK-002：审查并收敛 SP 异常规则引擎`：
  - 新增 [smoke_rule_engine.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_rule_engine.py)
  - 使用 SQLite 内存库写入最小 SP fixture，不污染真实业务库
  - 验证 7 条异常事件生成和必填证据字段
  - 确认规则输出没有自动执行广告动作字段
- 当前任务已推进为 `TASK-003：异常事件 API 收敛`。
- 完成 `TASK-003：异常事件 API 收敛`：
  - 新增 [smoke_anomaly_api.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_anomaly_api.py)
  - 使用 FastAPI `TestClient` 验证异常列表和异常详情
  - 验证 `market_id`、`anomaly_type`、`status` 三类核心筛选
  - 额外验证 `impressions_drop` 到 `impression_low` 的异常类型别名
  - 确认 API 返回内容没有自动执行广告动作字段
- 当前任务已推进为 `TASK-004：AI 建议结构化输出`。
- 完成 `TASK-004：AI 建议结构化输出`：
  - 新增 [smoke_ai_suggestion.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_ai_suggestion.py)
  - 基于异常事件生成 2 条结构化 AI 建议
  - 校验建议等级、标题、摘要、推理、风险说明、证据摘要、推荐人工处理选项和溯源 JSON
  - 确认建议输出没有自动执行广告动作字段
- 当前任务已推进为 `TASK-005：人工确认与动作记录`。
- 完成 `TASK-005：人工确认与动作记录`：
  - 新增 [smoke_manual_decision.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_manual_decision.py)
  - 验证 `adopt`、`adopt_with_changes`、`reject`、`observe` 四种人工处理
  - 验证 `observe` 将异常状态更新为 `observing`
  - 验证其他处理将异常状态更新为 `handled`
  - 确认人工处理流程没有自动执行广告动作字段
- 当前任务已推进为 `TASK-006：前端异常队列`。
- 完成 `TASK-006：前端异常队列`：
  - 新增 [smoke_frontend_anomaly_queue.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_anomaly_queue.py)
  - 验证前端已有异常队列、筛选、表格、详情入口和详情抽屉
  - 验证 API 封装支持异常类型、状态、建议等级、产品目标筛选
  - 验证页面没有自动执行广告动作按钮
  - `npm run build` 通过
- 当前任务已推进为 `TASK-007：建议详情与溯源页`。
- 完成 `TASK-007：建议详情与溯源页`：
  - 新增 [smoke_frontend_suggestion_detail.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_suggestion_detail.py)
  - 验证异常详情入口、详情抽屉、AI 建议、溯源 JSON、规则证据和人工处理入口
  - 验证页面没有自动执行广告动作按钮
  - `npm run build` 通过
- 当前任务已推进为 `TASK-008：处理记录与复盘页`。
- 完成 `TASK-008：处理记录与复盘页`：
  - 新增 [smoke_frontend_decision_review.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_decision_review.py)
  - 验证处理记录与复盘 Tab、处理记录表格、筛选、复盘入口、7 天 / 14 天复盘周期
  - 验证页面没有自动执行广告动作按钮
  - `npm run build` 通过
- `TASK-001` 至 `TASK-008` 已全部完成，当前阶段收口。

### 下一步

- 如继续开发，需要先新增下一阶段任务卡，再按目标模式执行。

### TASK-009 启动

- 已新增 `TASK-009：真实端到端验收`。
- 本任务只做本地真实联调验收，不调用积加 API，不生成广告执行动作。
- 验收重点：
  - 后端 `/health`
  - 异常、建议、处理记录、驾驶舱健康 API
  - 前端入口
  - API 返回字段不包含自动执行广告动作字段

### TASK-009 完成

- 新增 [smoke_real_e2e.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_e2e.py)
- 本项目后端启动在 `http://127.0.0.1:8001`
- 前端启动在 `http://127.0.0.1:5173`
- 验收命令已通过：

```powershell
python scripts/smoke_real_e2e.py
```

- 验收内容：
  - `/health`
  - `/api/anomalies`
  - `/api/suggestions`
  - `/api/decisions`
  - `/api/dashboard/health`
  - 前端 React root 入口
  - API 返回字段未发现自动执行广告动作字段

### TASK-010 启动

- 已新增 `TASK-010：产品目标与规则设置闭环验收`。
- 本任务只验证产品目标、规则门槛、广告活动绑定的本地闭环。
- 不调用积加 API，不新增任何广告执行能力。

### TASK-010 完成

- 新增 [smoke_product_settings.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_product_settings.py)
- 使用临时 SQLite 文件库验证产品设置 API，不污染真实业务库。
- 验收命令已通过：

```powershell
python scripts/smoke_product_settings.py
python scripts/smoke_real_e2e.py
```

- 验收内容：
  - 产品创建
  - 产品目标设置
  - 规则门槛设置
  - 广告活动绑定到本地指标归属
  - 产品详情和产品列表读取
  - 前端产品目标与规则设置页面入口
  - API 返回字段未发现自动执行广告动作字段

### TASK-011 启动

- 已新增 `TASK-011：UI 方案对齐与核心页面重构`。
- 本任务只做核心页面视觉层级和运营工作台风格对齐。
- 不新增业务功能，不改积加同步逻辑，不增加任何广告自动执行能力。

### TASK-011 完成

- 新增 [smoke_ui_alignment.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_ui_alignment.py)
- 重构 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 顶部运营定位和状态标签：
  - `SP 广告健康监控 / 人工决策辅助`
  - `只读建议`
  - `近 14 天`
  - `人工确认后记录`
- 重构 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css) 的核心视觉层级：
  - 页面背景和顶部工具条
  - Tabs 工作台容器
  - 指标卡强调色
  - 表格表头和 hover 状态
  - 建议详情抽屉
  - AI 建议证据区域
- 验收命令已通过：

```powershell
python scripts/smoke_ui_alignment.py
npm run build
python scripts/smoke_real_e2e.py
```

- `npm run build` 在普通沙箱下因 esbuild 子进程 `spawn EPERM` 失败，提权后构建通过；这是沙箱限制，不是代码编译错误。

### TASK-012 启动

- 已新增 `TASK-012：前端 API 指向修正与截图验收`。
- 当前截图发现驾驶舱显示 `暂无驾驶舱数据`。
- 已确认后端 `http://127.0.0.1:8001/api/dashboard/health` 有真实数据。
- 已确认 `frontend/.env.local` 当前指向 `http://127.0.0.1:8010`，与真实后端端口不一致。
- 本任务不修改 `frontend/.env.local`，只用临时环境变量启动前端做截图验收。

### TASK-012 完成

- 已停止原 5173 前端进程。
- 已用临时环境变量启动前端：

```powershell
VITE_API_BASE_URL=http://127.0.0.1:8001
```

- 已确认 Vite 运行时模块实际使用 `http://127.0.0.1:8001`。
- 截图 [dashboard-fixed-wait.png](D:/DataProject/coedx_project/Amazon广告分析/tmp/task012/dashboard-fixed-wait.png) 已显示：
  - 同步状态
  - 指标卡
  - ACOS / CVR / 花费趋势
  - 异常类型分布
- 验收命令已通过：

```powershell
python scripts/smoke_real_e2e.py
```

- 本任务未修改 `frontend/.env.local`，未新增任何广告自动执行能力。

### TASK-013 启动

- 已新增 `TASK-013：本地启动配置收敛`。
- 本任务只补本地启动脚本和轻量验收脚本。
- 不修改 `.env`、`.env.example`、`frontend/.env.local`。
- 不新增任何广告自动执行能力。

### TASK-013 完成

- 新增 [start_local_dev.ps1](D:/DataProject/coedx_project/Amazon广告分析/scripts/start_local_dev.ps1)
- 新增 [smoke_local_dev_config.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_local_dev_config.py)
- 本地启动命令：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\start_local_dev.ps1
```

- 启动脚本固定：
  - 后端：`http://127.0.0.1:8001`
  - 前端：`http://127.0.0.1:5173`
  - 前端 API：`VITE_API_BASE_URL=http://127.0.0.1:8001`
- 验收命令已通过：

```powershell
python scripts/smoke_local_dev_config.py
python scripts/smoke_real_e2e.py
```

- 已直接执行启动脚本，确认当前服务可复用：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\start_local_dev.ps1
```

- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`，未新增任何广告自动执行能力。

### TASK-014 启动

- 已新增 `TASK-014：驾驶舱首屏视觉二次打磨`。
- 本任务只优化驾驶舱首屏视觉层级。
- 不改业务 API，不改积加同步逻辑，不新增任何广告自动执行能力。

### TASK-014 完成

- 重构 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 驾驶舱首屏结构：
  - 新增决策摘要区：`待人工确认`、`高风险异常`、`整体 ACOS`、`销售 / 订单`
  - 压缩同步状态，只保留当前同步结果、统计周期和异常队列入口
  - 保留完整经营指标、趋势图和异常类型分布
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)：
  - 新增 `dashboard-focus-grid`
  - 新增 `focus-card`
  - 新增 `compact-sync-band`
  - 收紧指标卡高度和间距
- 更新 [smoke_ui_alignment.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_ui_alignment.py)，覆盖首屏决策摘要和压缩同步区。
- 截图 [dashboard-first-screen.png](D:/DataProject/coedx_project/Amazon广告分析/tmp/task014/dashboard-first-screen.png) 已确认视觉结果。
- 验收命令已通过：

```powershell
python scripts/smoke_ui_alignment.py
npm run build
python scripts/smoke_real_e2e.py
```

- `npm run build` 普通沙箱仍因 esbuild 子进程 `spawn EPERM` 失败，提权后构建通过；这是环境限制，不是代码编译错误。
- 本任务未新增任何广告自动执行能力。

### TASK-015 启动

- 已新增 `TASK-015：引入近 30 天真实 SP 数据`。
- 本任务只调用积加只读 API，同步 SP 关键词和 SP 搜索词近 30 天数据。
- 同步成功后清理同站点、同周期内更早同步的旧指标行，避免重复统计。
- 不修改 `.env`、`.env.example`、`frontend/.env.local`，不新增任何广告自动执行能力。

### TASK-015 完成

- 新增 [sync_recent_30_days.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/sync_recent_30_days.py)
- 新增 [smoke_recent_30_days_data.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_recent_30_days_data.py)
- 已同步近 30 天真实 SP 数据：
  - 周期：`2026-05-12` 至 `2026-06-10`
  - 店铺 / 站点：`1`
  - SP 关键词：150 行
  - SP 搜索词：300 行
  - 清理同周期旧关键词行：31 行
- 原始响应：
  - `data/raw/sp_keywords_1_2026-05-12_2026-06-10_20260610224811.json`
  - `data/raw/sp_search_terms_1_2026-05-12_2026-06-10_20260610224837.json`
- 事实记录：
  - 积加接口本次返回的是周期聚合行，不是逐日行。
  - 原始响应里日期字段只有 `createDate`。
  - 当前入库后的 `data_date` 均为 `2026-05-12`，dashboard 总览可用，但趋势图不能按逐日变化解读。
- 验收命令已通过：

```powershell
python scripts/smoke_recent_30_days_data.py
python scripts/smoke_real_e2e.py
```

- `python scripts/sync_recent_30_days.py` 普通沙箱因 SQLite `disk I/O error` 失败，提权后同步成功；这是当前环境文件写入限制。
- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`，未新增任何广告自动执行能力。

### TASK-016 启动

- 已新增 `TASK-016：前端数据口径修正为近 30 天聚合数据`。
- 本任务只改前端默认口径和趋势提示。
- 不改积加同步服务，不改后端数据模型，不新增任何广告自动执行能力。

### TASK-016 完成

- 修正 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 默认日期范围：从近 14 天改为近 30 天。
- 顶部驾驶舱文案和状态标签已改为 `近 30 天`。
- 趋势区域改为 `周期表现说明`：
  - 有多日数据时继续展示趋势图。
  - 当前积加返回单点周期聚合数据时，显示“当前为周期聚合数据”，避免误读为逐日趋势。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css) 的聚合说明面板样式。
- 更新 [smoke_ui_alignment.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_ui_alignment.py)，检查近 30 天口径和聚合提示。
- 验收命令已通过：

```powershell
python scripts/smoke_ui_alignment.py
python scripts/smoke_recent_30_days_data.py
python scripts/smoke_real_e2e.py
```

- 前端构建已在 `frontend` 目录通过：

```powershell
npm run build
```

- 根目录执行 `npm run build` 会失败，因为根目录没有 `package.json`，实际前端构建入口在 `frontend/package.json`。
- 本任务未新增任何广告自动执行能力。

### TASK-017 启动

- 已新增 `TASK-017：同步店铺名称并展示数据身份`。
- 已核对积加官方文档：
  - `根据亚马逊店铺id查询店铺名称`
  - `/middle/base/marketNames/query`
  - 文档参数名为 `markerIds`
- 已用真实只读请求验证：开放网关实际可用请求为 `POST /open/middle/base/marketNames/query`，请求体 `{"markerIds":[1]}`。
- 真实返回示例：`rivbos:US`。
- 本任务不改 `.env`、不改前端本地环境文件、不新增任何广告自动执行能力。

### TASK-017 完成

- 新增 [market.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/models/market.py)，用 `market_infos` 保存 `market_id`、店铺名称和站点。
- 新增 [market_service.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/services/market_service.py)，解析积加返回的 `rivbos:US` 格式并写入 SQLite。
- 新增 [sync_market_names.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/sync_market_names.py)，已同步真实店铺：
  - `market_id`: `1`
  - 店铺名称：`rivbos`
  - 站点：`US`
- 更新 [dashboard.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/dashboard.py)，`/api/dashboard/health` 现在返回：
  - `market`
  - `data_sources`
  - `product_binding`
  - `top_campaigns`
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，驾驶舱首屏新增：
  - 当前数据身份
  - 店铺名称 / market_id / 站点
  - 数据来源 / 统计周期 / 产品绑定状态
  - 广告活动来源 Top
- 当前真实页面已确认显示 `rivbos`，截图：[dashboard_identity.png](D:/DataProject/coedx_project/Amazon广告分析/tmp/task017/dashboard_identity.png)
- 验收命令已通过：

```powershell
python scripts/smoke_dashboard_identity.py
python scripts/sync_market_names.py
python scripts/smoke_recent_30_days_data.py
python scripts/smoke_real_e2e.py
```

- 前端构建已在 `frontend` 目录通过：

```powershell
npm run build
```

- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`，未新增任何广告自动执行能力。

### TASK-018 启动

- 已新增 `TASK-018：驾驶舱首屏 UI 收敛`。
- 本任务只改驾驶舱前端布局和 UI smoke。
- 不改后端、不改积加 API、不新增任何广告自动执行能力。

### TASK-018 完成

- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，将当前数据身份从大卡片收敛为轻量身份条。
- 新增首屏“当前主任务”区域，突出待人工确认 AI 建议、异常数量和异常队列入口。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，收敛首屏卡片层级、指标明细和广告活动来源 Top。
- 更新 [smoke_ui_alignment.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_ui_alignment.py)，检查轻量身份条、主任务区、次级指标区和旧身份大卡移除。
- 当前页面截图：[dashboard_ui_compact.png](D:/DataProject/coedx_project/Amazon广告分析/tmp/task018/dashboard_ui_compact.png)
- 验收命令已通过：

```powershell
python scripts/smoke_ui_alignment.py
python scripts/smoke_dashboard_identity.py
python scripts/smoke_real_e2e.py
```

- 前端构建已在 `frontend` 目录通过：

```powershell
npm run build
```

- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`，未新增任何广告自动执行能力。

### TASK-019 启动

- 已新增 `TASK-019：当前 MVP 基线验收`。
- 本任务不新增业务功能，只验收当前真实链路。
- 不改后端、不改前端、不改 `.env`，不新增任何广告自动执行能力。

### TASK-019 完成

- 新增 [smoke_current_mvp_baseline.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_current_mvp_baseline.py)，用于验收当前 MVP 基线。
- 修正 [smoke_recent_30_days_data.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_recent_30_days_data.py) 与基线 smoke 的周期口径：使用最新成功同步周期，不受本机日期跨天影响。
- 当前真实基线：
  - 店铺：`rivbos / US / market_id=1`
  - 周期：`2026-05-12` 至 `2026-06-10`
  - 指标行数：`150`
  - ACOS：`30.39%`
  - 订单：`239`
  - 销售额：`2912.62`
  - 广告活动来源 Top：`8` 个 Campaign
  - 异常队列：`1` 条
  - AI 建议队列：`1` 条
- 当前缺口：
  - 产品绑定为 `0 / 150`，下一步应进入产品与广告归因闭环。
  - 真实库暂无人工处理记录，需要运营跑一次人工确认流程。
  - 真实库暂无复盘记录，需要在人工处理后补 7 天 / 14 天复盘。
- 验收命令已通过：

```powershell
python scripts/smoke_current_mvp_baseline.py
python scripts/smoke_recent_30_days_data.py
python scripts/smoke_dashboard_identity.py
python scripts/smoke_real_e2e.py
```

- 前端构建已在 `frontend` 目录通过：

```powershell
npm run build
```

- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`，未新增任何广告自动执行能力。

### TASK-020 启动

- 已新增 `TASK-020：产品与广告归因规则闭环`。
- 本任务只做人工确认后的本地产品归因，不做广告自动执行。
- 第一版支持 Campaign 和 Ad Group 两种归因颗粒度。
- 先写 smoke 验收脚本，再补最小后端、同步套用和前端入口。

### TASK-020 完成

- 新增 [product_attribution_service.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/services/product_attribution_service.py)，负责未归因数据池、归因规则保存、当前数据回填和后续同步套用。
- 更新 [product.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/models/product.py)，新增 `product_ad_bindings` 本地归因规则表。
- 更新 [products.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/products.py)，新增未归因数据池、归因规则列表、Campaign / Ad Group 人工确认归因接口。
- 更新 [sync_service.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/services/sync_service.py)，同步 SP 关键词和搜索词后自动套用已确认归因规则。
- 更新 [api.ts](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/api.ts)、[App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)、[styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，产品设置页新增未归因广告数据池和已确认归因规则列表。
- 浏览器复查时发现请求失败提示会触发 `message.error is not a function`，已在 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 增加安全提示包装，避免失败提示打断页面。
- 新增 [smoke_product_attribution.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_product_attribution.py)，验证 Campaign 归因、Ad Group 归因、历史回填、后续套用、dashboard 绑定统计和前端入口。
- 验收命令已通过：

```powershell
python scripts/smoke_product_attribution.py
python scripts/smoke_product_settings.py
python scripts/smoke_dashboard_identity.py
python scripts/smoke_real_e2e.py
```

- 前端构建已在 `frontend` 目录通过：

```powershell
npm run build
```

- `python scripts/smoke_product_settings.py` 在普通沙箱下因旧临时 SQLite 文件删除权限失败，提权重跑后通过；业务回归结果正常。
- 使用临时空库后端 `8011` 和临时前端 `5174` 完成浏览器复查，产品设置页可见“未归因广告数据池”和“已确认归因规则”，等待后未出现新增业务错误。
- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`，未新增任何广告自动执行能力。

### TASK-021 启动

- 已新增 `TASK-021：本地启动脚本校验后端归因接口`。
- 根因事实：普通沙箱下直接初始化 `data/app.db` 会触发 SQLite `disk I/O error`，提权后 `init_db()` 通过。
- 根因事实：8001 上的旧后端 `/health` 正常，但 `/api/products/unbound-ad-sources` 返回旧动态路由 422。
- 本任务只修本地启动校验，避免旧后端继续被误判为可用。

### TASK-021 完成

- 更新 [start_local_dev.ps1](D:/DataProject/coedx_project/Amazon广告分析/scripts/start_local_dev.ps1)，新增 `Test-BackendReady`：后端必须同时满足 `/health` 正常和 `/api/products/unbound-ad-sources` 可访问。
- 当 8001 上旧后端仅 `/health` 正常时，启动脚本会停止旧监听进程并启动新后端。
- 更新 [smoke_local_dev_config.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_local_dev_config.py)，检查启动脚本包含归因接口校验。
- 已实际运行启动脚本，新 8001 后端启动成功，`/api/products/unbound-ad-sources` 和 `/api/products/ad-bindings` 均返回 200。
- 浏览器复查当前 [http://127.0.0.1:5173/](http://127.0.0.1:5173/)：产品设置页可见“未归因广告数据池”和“已确认归因规则”，且没有新增前端错误。
- 验收命令已通过：

```powershell
python scripts/smoke_local_dev_config.py
python scripts/smoke_product_attribution.py
python scripts/smoke_real_e2e.py
```

- 前端构建已在 `frontend` 目录通过：

```powershell
npm run build
```

- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`，未新增任何广告自动执行能力。

### TASK-022 启动

- 已新增 `TASK-022：真实产品归因预览验收`。
- 本任务只读预览真实产品归因条件，不创建产品、不保存归因规则、不回填 `product_id`。
- 目标是确认真实店铺、真实周期、产品候选、未归因 Campaign / Ad Group 数据池能否支撑人工归因。

### TASK-022 完成

- 新增 [smoke_real_product_attribution_preview.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_product_attribution_preview.py)，只读调用当前本地 API，预览真实产品归因条件。
- 当前真实店铺和周期：
  - 店铺：`rivbos / US / market_id=1`
  - 周期：`2026-05-12` 至 `2026-06-10`
- 当前归因状态：
  - 产品绑定：`0 / 150`
  - 未归因 Campaign：`26`
  - 未归因 Ad Group：`26`
  - 已确认归因规则：`0`
- 关键事实：
  - 真实库当前只有 1 个产品候选，但它是 `DEMO-ACOS / DEMO-MSKU-ACOS` 演示产品。
  - 真实产品候选数为 `0`，因此 `ready_for_manual_binding=false`。
  - 现在不能直接做真实人工归因，否则会把真实广告数据绑定到演示产品。
- 验收命令已通过：

```powershell
python scripts/smoke_real_product_attribution_preview.py
python scripts/smoke_real_e2e.py
```

- 本任务未创建产品、未保存归因规则、未回填 `product_id`，未新增任何广告自动执行能力。

### TASK-023 启动

- 已新增 `TASK-023：产品归因确认闭环细化`。
- 本任务只细化人工归因前后的证据链：证据详情、产品候选、可信度说明、冲突提示和确认快照。
- 前端流程从“直接确认归因”调整为“查看证据 -> 人工确认 -> 保存证据快照”。
- 本任务不修改积加客户端、不修改真实环境配置、不写入真实 `data/app.db`，不新增任何广告自动执行能力。

### TASK-023 完成

- 新增只读归因证据接口：`/api/products/ad-attribution-evidence`。
- 归因证据包含归因对象指标、Top 关键词、Top 搜索词、产品候选、可信度分数、判断理由和已有规则冲突提示。
- 确认归因时会把确认周期、归因对象快照、候选产品、可信度、冲突、关键词 / 搜索词证据和人工确认信息写入 `evidence_json`。
- 前端产品设置页已从“直接确认归因”改为“查看证据 -> 选择产品 -> 确认归因并保存证据快照”。
- 已确认归因规则列表新增证据快照摘要。
- 验收命令已通过：

```powershell
python scripts/smoke_product_attribution_evidence.py
python scripts/smoke_product_attribution.py
python scripts/smoke_real_product_attribution_preview.py
python scripts/smoke_real_e2e.py
```

- 前端构建已在 `frontend` 目录通过：

```powershell
npm run build
```

- 浏览器截图复查未完成：本轮没有可用的 in-app Browser 控制工具，本地 Node 运行时也未安装 `playwright`。
- 真实库仍未满足人工归因条件：当前只有演示产品，真实产品候选数为 `0`，不能把真实广告数据绑定到演示产品。
- 已记录下一步建议：`TASK-024：搜索词真实字段映射修复`，因为真实 raw 搜索词字段是 `query`，当前 `sp_search_term_metrics.search_term` 全空。
- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务和真实 `data/app.db`，未新增任何广告自动执行能力。

### TASK-024 启动

- 已新增 `TASK-024：搜索词真实字段映射修复`。
- 根因事实：真实积加 raw SP 搜索词字段为 `query`，当前同步代码读取 `searchTerm`，导致 `sp_search_term_metrics.search_term` 全空。
- 本任务先修同步映射，再用已保存 raw payload 回填当前 SQLite，最后收紧近 30 天真实数据验收。
- 本任务不修改 `.env`、`.env.example`、`frontend/.env.local` 和积加客户端，不新增任何广告自动执行能力。

### TASK-024 完成

- 新增 [smoke_search_term_mapping.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_search_term_mapping.py)，先红灯确认 `query/targetingText/targetId/targetingType` 未被映射。
- 更新 [sync_service.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/services/sync_service.py)，兼容：
  - `query -> search_term`
  - `targetingText -> keyword_text`
  - `targetId -> keyword_id`
  - `targetingType -> match_type`
- 新增 [backfill_search_terms_from_raw.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/backfill_search_terms_from_raw.py)，基于最新成功同步 raw payload 回填当前 SQLite。
- 普通沙箱写入 `data/app.db` 时仍触发 SQLite `disk I/O error`，已按权限机制提权运行回填脚本并成功。
- 回填结果：
  - `raw_rows=300`
  - `db_rows=300`
  - `matched_rows=300`
  - `updated_rows=300`
  - 样例搜索词：`1 year old sunglasses`、`6-12 month boy sunhat`、`b004hvkaas`
- 更新 [smoke_recent_30_days_data.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_recent_30_days_data.py)，近 30 天真实数据验收必须确认非空用户搜索词。
- 验收命令已通过：

```powershell
python scripts/smoke_search_term_mapping.py
python scripts/backfill_search_terms_from_raw.py
python scripts/smoke_recent_30_days_data.py
python scripts/smoke_real_e2e.py
```

- 当前真实搜索词数据状态：`sp_search_term_metrics` 300 行，非空用户搜索词 300 行，去重搜索词 274 个。
- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端和前端，未新增任何广告自动执行能力。
- 下一步建议：`TASK-025：搜索词归类聚合分析接口`，先做只读后端聚合和分类，再接前端页面。

### TASK-025 启动

- 已新增 `TASK-025：搜索词归类聚合分析接口`。
- 本任务只做后端只读接口，不接前端、不写动作记录。
- 第一版搜索词分类采用可解释规则，先输出语义分类、表现分类、聚合指标和来源证据。
- 本任务不修改真实环境配置、不修改积加客户端、不写 `data/app.db`，不新增任何广告自动执行能力。

### TASK-025 完成

- 新增 [search_term_analysis_service.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/services/search_term_analysis_service.py)，按 `search_term` 聚合点击、花费、订单、销售、ACOS、CVR 和来源证据。
- 新增 [search_terms.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/search_terms.py)，提供只读接口 `/api/search-terms/analysis`。
- 更新 [main.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/main.py)，注册 `/api/search-terms` 路由。
- 第一版语义分类：
  - `age_spec`：年龄 / 人群规格词
  - `core_product`：核心产品词
  - `asin`：ASIN / 商品编号词
  - `accessory_or_unrelated`：疑似配件或无关词
  - `generic`：泛搜索词
- 第一版表现分类：
  - `high_conversion`：高转化词
  - `costly_no_order`：高花费无单词
  - `high_acos`：有单但 ACOS 高
  - `data_insufficient`：数据不足
  - `observe`：继续观察
- 新增 [smoke_search_term_analysis.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_search_term_analysis.py)，先验证接口 404 红灯，再实现转绿。
- 真实库只读检查结果：
  - 去重搜索词：`274`
  - 点击：`361`
  - 花费：`339.37`
  - 订单：`73`
  - 销售额：`964.63`
  - ACOS：`35.18%`
- live 后端已重启，`http://127.0.0.1:8001/api/search-terms/analysis` 返回 200。
- 验收命令已通过：

```powershell
python scripts/smoke_search_term_analysis.py
python scripts/smoke_recent_30_days_data.py
python scripts/smoke_real_e2e.py
```

- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务、真实 `data/app.db` 和前端，未新增任何广告自动执行能力。
- 下一步建议：`TASK-026：搜索词归类聚合分析前端页`，把该只读接口接入主工作流。

### TASK-026 启动

- 已新增 `TASK-026：搜索词归类聚合分析前端页`。
- 入口方案选择独立“搜索词分析”Tab，不挤占驾驶舱，也不依赖产品归因完成。
- 本任务只接入 TASK-025 的只读接口，展示分类、聚合和来源证据。
- 本任务不修改后端、不写 `data/app.db`、不修改真实环境配置，不新增任何广告自动执行能力。

### TASK-026 完成

- 新增 [smoke_frontend_search_terms.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_search_terms.py)，先红灯确认前端缺少搜索词分析入口，再实现转绿。
- 更新 [api.ts](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/api.ts)，新增 `SearchTermAnalysis` 类型和 `fetchSearchTermAnalysis`，只读调用 `/api/search-terms/analysis`。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增独立“搜索词分析”Tab。
- 搜索词分析页现在可展示：
  - 周期、去重搜索词、花费 / 销售、ACOS / CVR 汇总
  - 语义分类汇总
  - 表现分类汇总
  - 搜索词明细表
  - Campaign / Ad Group / 关键词来源证据
- 搜索词分析页支持店铺、周期、语义分类、表现分类、最小点击、最小花费、目标 ACOS 筛选。
- 真实接口检查结果：去重搜索词 `274`，花费 `339.37`，订单 `73`，销售额 `964.63`，Top 词 `infant sunglasses`。
- 验收命令已通过：

```powershell
python scripts/smoke_frontend_search_terms.py
python scripts/smoke_search_term_analysis.py
python scripts/smoke_real_e2e.py
npm run build
```

- 运行时前端源码已确认包含 `searchTermAnalysis`、`loadSearchTermAnalysis`、`fetchSearchTermAnalysis`。
- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`、后端和真实 `data/app.db`，未新增任何广告自动执行能力。
- 下一步建议：`TASK-027：搜索词人工处理候选池`，把高转化词、高花费无单词、高 ACOS 词转成待人工确认的观察 / 处理候选。

### TASK-027 启动

- 已新增 `TASK-027：搜索词人工处理候选池`。
- 本任务第一版做“派生候选池”，不写广告动作、不写人工处理记录。
- 候选来自搜索词聚合结果，优先覆盖高转化词、高花费无单词、高 ACOS 词。
- 前端只展示候选、证据、理由和推荐人工判断类型，不提供任何自动执行入口。

### TASK-027 完成

- 新增 [smoke_search_term_candidates.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_search_term_candidates.py)，先 404 红灯确认候选池接口不存在，再实现转绿。
- 新增 [smoke_frontend_search_term_candidates.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_search_term_candidates.py)，约束前端候选池入口、筛选、表格和禁止自动执行字段。
- 更新 [search_term_analysis_service.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/services/search_term_analysis_service.py)，新增只读候选派生：
  - `scale_opportunity`：高转化放量候选
  - `waste_risk`：高花费无单处理候选
  - `efficiency_risk`：高 ACOS 处理候选
- 更新 [search_terms.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/search_terms.py)，新增 `/api/search-terms/candidates`。
- 更新 [api.ts](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/api.ts)，新增 `SearchTermCandidate`、`SearchTermCandidates` 和 `fetchSearchTermCandidates`。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，在“搜索词分析”页新增“人工处理候选池”和候选类型筛选。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，候选池沿用当前后台视觉风格。
- 真实运行接口检查：
  - `/api/search-terms/candidates` 返回 200
  - 默认门槛下候选数：`1`
  - Top 候选：`infant sunglasses`
  - 候选类型：`efficiency_risk`
- 本地 dev 后端已重启，前端 [http://127.0.0.1:5173](http://127.0.0.1:5173) 仍指向后端 `8001`。
- 验收命令已通过：

```powershell
python scripts/smoke_search_term_candidates.py
python scripts/smoke_frontend_search_term_candidates.py
python scripts/smoke_search_term_analysis.py
python scripts/smoke_frontend_search_terms.py
python scripts/smoke_real_e2e.py
python scripts/smoke_local_dev_config.py
npm run build
```

- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务和真实 `data/app.db`，未新增任何广告自动执行能力。
- 下一步建议：`TASK-028：搜索词候选转人工记录`，让运营从候选池选择加入观察、修改后采纳或拒绝，并保存本地证据快照。

### TASK-028 启动

- 已新增 `TASK-028：搜索词候选转人工记录`。
- 第一版只做本地人工判断记录，不调用广告执行接口。
- 记录对象来自搜索词候选池，提交时后端重新计算候选并保存证据快照。
- 前端入口放在“搜索词分析”的人工处理候选池中，操作结果显示在同页候选处理记录表。

### TASK-028 完成

- 新增 [smoke_search_term_candidate_decisions.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_search_term_candidate_decisions.py)，先红灯确认缺少搜索词候选处理模型和接口，再实现转绿。
- 新增 [smoke_frontend_search_term_candidate_decisions.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_search_term_candidate_decisions.py)，约束前端 API、弹窗、记录按钮、记录列表和禁止自动执行标记。
- 更新 [decision.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/models/decision.py)，新增 `search_term_candidate_decisions` 本地记录模型。
- 更新 [search_terms.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/search_terms.py)，新增 `/api/search-terms/candidate-decisions` 的创建和查询接口。
- 提交候选处理记录时，后端会按当前筛选口径重新计算候选，并保存 `candidate_snapshot_json`，用于后续溯源。
- 更新 [api.ts](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/api.ts)，新增搜索词候选处理记录类型和读写 API。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，候选池新增“记录人工判断”按钮、提交弹窗和“候选处理记录”表。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，补充候选处理记录样式。
- 验收命令已通过：

```powershell
python scripts/smoke_search_term_candidate_decisions.py
python scripts/smoke_frontend_search_term_candidate_decisions.py
python scripts/smoke_search_term_candidates.py
python scripts/smoke_real_e2e.py
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务和真实 `data/app.db`，未新增任何广告自动执行能力。
- 下一步建议：`TASK-029：搜索词处理记录复盘入口`，查看人工记录后 7 天 / 14 天搜索词表现变化。

### TASK-029 启动

- 已新增 `TASK-029：搜索词处理记录复盘入口`。
- 第一版只做只读复盘计算，不新增复盘写库表。
- 复盘对象为搜索词候选人工处理记录，对比记录时证据快照和后续同搜索词 7 天 / 14 天聚合表现。
- 复盘结果只用于人工判断，不产生任何广告执行动作。

### TASK-029 完成

- 新增 [smoke_search_term_candidate_review.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_search_term_candidate_review.py)，先 404 红灯确认缺少复盘接口，再实现转绿。
- 新增 [smoke_frontend_search_term_candidate_review.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_search_term_candidate_review.py)，约束前端复盘 API、按钮、弹窗、周期和禁止自动执行标记。
- 更新 [search_terms.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/search_terms.py)，新增 `/api/search-terms/candidate-decisions/{decision_id}/review`。
- 复盘接口只读计算：
  - 记录时指标快照 `before_metrics`
  - 后续同搜索词 7 天 / 14 天聚合指标 `after_metrics`
  - 变化量 `delta`
  - 人工判断用结果 `result`
- 更新 [api.ts](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/api.ts)，新增 `SearchTermCandidateReview` 和 `fetchSearchTermCandidateReview`。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，在候选处理记录表新增 7 天 / 14 天复盘入口和复盘弹窗。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，新增复盘对比卡片样式。
- 验收命令已通过：

```powershell
python scripts/smoke_search_term_candidate_review.py
python scripts/smoke_frontend_search_term_candidate_review.py
python scripts/smoke_search_term_candidate_decisions.py
python scripts/smoke_real_e2e.py
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务和真实 `data/app.db`，未新增任何广告自动执行能力。
- 下一步建议：`TASK-030：真实产品基础数据接入与归因可用性复核`，解决真实产品候选缺失导致产品归因无法日常使用的问题。

### TASK-030 启动

- 已新增 `TASK-030：真实产品基础数据接入与归因可用性复核`。
- 目标是解决真实库缺少真实产品候选，导致产品归因无法日常使用的问题。
- 第一版采用 KISS 方案：先从真实未归因广告来源创建本地产品草稿，后续归因仍必须人工确认。
- 积加 `/purchase/goods/product/page` 只读探测遇到分页参数错误，继续枚举分页参数时触发接口限流，因此本任务未继续打外部接口。

### TASK-030 完成

- 新增 [smoke_product_seed_from_ad_source.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_product_seed_from_ad_source.py)，验证产品草稿创建后能进入归因候选，并且不会自动保存归因规则或回填广告指标 `product_id`。
- 新增 [smoke_frontend_product_seed_from_ad_source.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_seed_from_ad_source.py)，约束前端产品草稿入口、预填逻辑、创建后选中新产品和重新拉取归因证据。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，在未归因广告来源表和归因证据抽屉新增“从广告来源创建产品草稿”入口。
- 新增 [seed_products_from_unbound_ad_sources.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/seed_products_from_unbound_ad_sources.py)，基于真实未归因 Ad Group 名称提取产品编码并生成本地产品草稿。
- 真实库已创建 8 个非演示产品草稿：`RBK004`、`RB833`、`RB832`、`RBK037`、`RB831`、`RBK003`、`RBK0231`、`RBK068`。
- 新增 [smoke_real_product_seed_ready.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_product_seed_ready.py)，验证真实库 `real_product_count=8`、`ready_for_manual_binding=true`、`active_binding_count=0`。
- `smoke_real_product_attribution_preview.py` 已显示 `ready_for_manual_binding=true`，阻塞原因清空。
- 验收命令已通过：

```powershell
python scripts/smoke_product_seed_from_ad_source.py
python scripts/smoke_frontend_product_seed_from_ad_source.py
python scripts/seed_products_from_unbound_ad_sources.py --apply
python scripts/smoke_real_product_seed_ready.py
python scripts/smoke_real_product_attribution_preview.py
python scripts/smoke_real_e2e.py
npm run build
```

- `scripts/seed_products_from_unbound_ad_sources.py --apply` 二次运行 `created_count=0`，说明产品草稿生成幂等。
- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 本任务写入了本地真实库 `data/app.db` 的产品草稿，但未写入归因规则，`active_binding_count=0`。
- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务，未新增任何广告自动执行能力。
- 下一步建议：`TASK-031：产品归因确认与搜索词产品化聚合准备`，先人工确认核心 Ad Group / Campaign 产品归因，再做搜索词按产品维度的归类聚合。

### TASK-031 启动

- 已新增 `TASK-031：产品归因候选队列`。
- 本任务不自动保存归因规则，只生成高可信候选，运营必须先查看证据再人工确认。
- 目标是把 `TASK-030` 的真实产品草稿转化为可日常操作的产品归因工作队列。

### TASK-031 完成

- 更新 [product_attribution_service.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/services/product_attribution_service.py)，新增 `build_product_attribution_candidates`，只读生成产品归因候选。
- 更新 [products.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/products.py)，新增 `/api/products/attribution-candidates`。
- 更新 [api.ts](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/api.ts)，新增产品归因候选类型和 `fetchProductAttributionCandidates`。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，产品设置页新增“高可信归因候选”面板。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，补充归因候选面板样式。
- 新增 [smoke_product_attribution_candidates.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_product_attribution_candidates.py)，验证后端候选接口只读、不保存归因规则、不回填 `product_id`。
- 新增 [smoke_frontend_product_attribution_candidates.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_attribution_candidates.py)，约束前端候选面板、查看证据流程和禁止自动执行标记。
- 新增 [smoke_real_product_attribution_candidates.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_product_attribution_candidates.py)，验证真实库归因候选可用。
- 真实库返回 20 条高可信候选，Top 候选为 `RBK004-kids sunglasses-广泛 -> RBK004 kids sunglasses`，可信度 100。
- 真实库 `active_binding_count=0`，确认本任务没有自动写入归因规则。
- 验收命令已通过：

```powershell
python scripts/smoke_product_attribution_candidates.py
python scripts/smoke_frontend_product_attribution_candidates.py
python scripts/smoke_real_product_attribution_candidates.py
python scripts/smoke_real_product_attribution_preview.py
python scripts/smoke_real_e2e.py
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务、真实 `data/app.db`，未新增任何广告自动执行能力。
- 下一步建议：`TASK-032：搜索词按产品维度归类聚合`。前提是运营先在“高可信归因候选”中确认核心产品归因规则，否则产品维度搜索词分析没有可靠归属。

### TASK-032 启动

- 已新增 `TASK-032：产品维度搜索词分析就绪检查`。
- 目标不是直接做产品维度结论，而是先判断产品维度搜索词分析是否具备可信前提。
- 第一版只做只读就绪检查、搜索词产品筛选和未就绪提示。

### TASK-032 完成

- 更新 [search_term_analysis_service.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/services/search_term_analysis_service.py)，新增 `build_product_search_term_readiness`，只读统计已确认归因规则和已归因搜索词行数。
- 更新 [search_terms.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/search_terms.py)，新增 `/api/search-terms/product-readiness`。
- 更新 [api.ts](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/api.ts)，新增 `SearchTermProductReadiness` 和 `fetchSearchTermProductReadiness`。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，搜索词分析页新增“产品筛选”和产品维度就绪提示。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，补充产品维度就绪提示样式。
- 新增 [smoke_search_term_product_readiness.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_search_term_product_readiness.py)，验证后端未就绪 / 已就绪状态、只读计数不变和 `product_id` 筛选。
- 新增 [smoke_frontend_search_term_product_readiness.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_search_term_product_readiness.py)，约束前端产品筛选、就绪提示和禁止自动执行标记。
- 新增 [smoke_real_search_term_product_readiness.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_search_term_product_readiness.py)，验证真实库当前产品维度搜索词分析未就绪。
- 浏览器已确认搜索词分析页能看到“产品筛选”、“产品维度分析未就绪”和“请先在产品设置页人工确认产品归因规则”。
- 真实库当前结果：`active_binding_count=0`、`bound_search_term_rows=0`、`ready=false`、`products_with_search_terms=0`。
- 验收命令已通过：

```powershell
python scripts/smoke_search_term_product_readiness.py
python scripts/smoke_frontend_search_term_product_readiness.py
python scripts/smoke_real_search_term_product_readiness.py
python scripts/smoke_search_term_analysis.py
python scripts/smoke_real_e2e.py
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 本任务未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务、真实 `data/app.db`，未新增任何广告自动执行能力。
- 下一步建议：`TASK-033：人工确认核心产品归因规则`。完成后再做真正的搜索词按产品维度归类聚合。

### TASK-033 启动

- 已新增 `TASK-033：核心归因候选确认工作台`。
- 本任务不替运营确认归因，只把高可信候选按优先级和确认后影响展示出来。
- 目标是让运营先处理最值得确认的 Campaign / Ad Group，为后续搜索词按产品维度聚合准备可信归属。

### TASK-033 完成

- 更新 [product_attribution_service.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/services/product_attribution_service.py)，产品归因候选新增 `priority_rank`、`priority_label` 和 `unlock_impact`。
- 更新 [api.ts](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/api.ts)，补充产品归因候选优先级和确认后影响类型。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，产品设置页显示“建议优先确认”和“确认后影响”；默认周期错位时优先使用最近成功同步周期。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，补充优先级标签和确认后影响样式。
- 新增 [smoke_product_attribution_confirmation_workbench.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_product_attribution_confirmation_workbench.py)，验证后端候选优先级、确认后影响和只读性。
- 新增 [smoke_frontend_product_attribution_confirmation_workbench.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_attribution_confirmation_workbench.py)，约束前端工作台、最近同步周期后备和禁止自动执行标记。
- 新增 [smoke_real_product_attribution_confirmation_workbench.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_product_attribution_confirmation_workbench.py)，验证真实库工作台候选。
- 浏览器确认产品设置页已展示真实候选：Top 为 `RBK004-kids sunglasses-广泛 -> RBK004 kids sunglasses`，显示“建议优先确认 #1”、26 行搜索词、花费 150.47、销售 545.79、订单 52、ACOS 27.6%。
- 真实库 `active_binding_count=0`，本任务未自动保存归因规则或回填 `product_id`。
- 验收命令已通过：

```powershell
python scripts/smoke_product_attribution_confirmation_workbench.py
python scripts/smoke_frontend_product_attribution_confirmation_workbench.py
python scripts/smoke_real_product_attribution_confirmation_workbench.py
python scripts/smoke_real_product_attribution_candidates.py
python scripts/smoke_real_search_term_product_readiness.py
python scripts/smoke_real_e2e.py
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-034：搜索词归类聚合分析规划与目标设定`。需要先明确搜索词归类层级和聚合口径；产品维度结论仍依赖运营先人工确认归因规则。

### TASK-034 启动

- 已启动 `TASK-034：搜索词归类聚合组`。
- 头脑风暴后选择方案 A：在现有搜索词分析上补“语义分类 + 表现分类”的组合聚合组。
- 不等待产品归因完成，也不引入 AI 聚类；产品维度结论继续受人工归因规则约束。
- 同步修正一个分类问题：有订单搜索词应优先按 ACOS 判断，不应因为点击 / 花费低直接归为“数据不足”。

### TASK-034 完成

- 更新 [search_term_analysis_service.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/services/search_term_analysis_service.py)，新增 `group_summary`，按“语义分类 + 表现分类”组合聚合搜索词。
- 修正表现分类顺序：有订单搜索词优先按 ACOS 判断，不再因为点击 / 花费低直接归为数据不足。
- 更新 [api.ts](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/api.ts)，新增 `SearchTermGroupSummary`。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，搜索词分析页新增“归类聚合组”，并在默认周期错位时优先使用最近成功同步周期。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，补充归类聚合组样式。
- 新增 [smoke_search_term_group_aggregation.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_search_term_group_aggregation.py)，验证后端归类聚合、有订单低点击分类和禁止自动执行字段。
- 新增 [smoke_frontend_search_term_group_aggregation.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_search_term_group_aggregation.py)，约束前端归类组、代表搜索词和最近同步周期后备。
- 新增 [smoke_real_search_term_group_aggregation.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_search_term_group_aggregation.py)，验证真实库只读归类聚合。
- 浏览器确认真实页面显示 10 个归类聚合组和代表搜索词。
- 真实库 Top 聚合组：`age_spec:data_insufficient`，92 个词、花费 101.99、代表词包含 `baby sunglasses 0-6 months`。
- 真实库 `active_binding_count=0`，本任务未改变产品归因规则数量。
- 验收命令已通过：

```powershell
python scripts/smoke_search_term_group_aggregation.py
python scripts/smoke_frontend_search_term_group_aggregation.py
python scripts/smoke_real_search_term_group_aggregation.py
python scripts/smoke_search_term_analysis.py
python scripts/smoke_frontend_search_terms.py
python scripts/smoke_real_e2e.py
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-035：搜索词归类组下钻与人工记录`。

### TASK-035 启动

- 已启动 `TASK-035：搜索词归类组下钻与人工记录`。
- 设计收敛后选择方案 C：新增独立组级人工判断记录，避免把组判断混入单个搜索词候选记录。
- 本任务只做本地记录与筛选下钻，不执行任何广告动作。

### TASK-035 完成

- 更新 [decision.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/models/decision.py)，新增 `search_term_group_decisions` 本地组级人工判断记录表。
- 更新 [search_terms.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/search_terms.py)，新增 `POST /api/search-terms/group-decisions` 和 `GET /api/search-terms/group-decisions`。
- 提交组级人工判断时后端重新计算当前 `group_summary`，保存匹配组快照。
- 更新 [api.ts](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/api.ts)，新增组级人工记录类型和 API 客户端函数。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，归类聚合组新增“查看同组明细”和“记录组判断”；新增组级人工记录表和弹窗。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，补充组级记录区域和按钮样式。
- 新增 [smoke_search_term_group_decisions.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_search_term_group_decisions.py)，验证后端组级记录创建、快照、列表和必填校验。
- 新增 [smoke_frontend_search_term_group_decisions.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_search_term_group_decisions.py)，约束前端组下钻、组判断入口、组级记录列表和禁止自动执行标记。
- 新增 [smoke_real_search_term_group_drilldown.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_search_term_group_drilldown.py)，验证真实 API 同组下钻只读性。
- 浏览器确认搜索词分析页可见“查看同组明细”“记录组判断”“组级人工记录”，并可打开组级人工判断弹窗。
- 真实 API 下钻 Top 组 `age_spec:data_insufficient` 返回 92 行同组搜索词。
- 真实库 `active_binding_count=0`，本任务未改变产品归因规则数量。
- 验收命令已通过：

```powershell
python scripts/smoke_search_term_group_decisions.py
python scripts/smoke_frontend_search_term_group_decisions.py
python scripts/smoke_real_search_term_group_drilldown.py
python scripts/smoke_search_term_group_aggregation.py
python scripts/smoke_real_search_term_group_aggregation.py
python scripts/smoke_real_e2e.py
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务、搜索词分析服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-036：产品归因人工确认后打通产品维度搜索词组分析`。当前真实库 `active_binding_count=0`，产品维度搜索词结论仍缺少可信归属。

### TASK-036 启动

- 已启动 `TASK-036：产品归因确认后联动产品维度搜索词分析`。
- 本任务不替运营确认真实归因规则，只把“人工确认归因后查看该产品搜索词归类组”的链路打通。
- 后端归因保存和搜索词产品筛选已有接口，本任务优先不改后端，只补前端联动和临时库验收。

### TASK-036 完成

- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增 `openProductSearchTermAnalysis`，从已确认归因规则带入产品、店铺和周期并切到搜索词分析页。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，已确认归因规则列表新增“查看搜索词分析”入口和联动提示。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，补充已确认归因规则下一步按钮样式。
- 新增 [smoke_product_attribution_to_search_terms.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_product_attribution_to_search_terms.py)，用临时库验证人工确认归因后搜索词 `product_id` 回填、产品维度 ready、产品筛选 `group_summary` 非空。
- 新增 [smoke_frontend_product_attribution_to_search_terms.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_attribution_to_search_terms.py)，约束前端联动入口、筛选带入和禁止自动执行标记。
- 新增 [smoke_real_product_attribution_to_search_terms_readiness.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_product_attribution_to_search_terms_readiness.py)，真实 API 只读验证产品维度就绪状态和归因数量不变。
- 浏览器确认产品设置页可见“已确认归因规则”和“人工确认归因后，可查看该产品搜索词归类组”。
- 临时库结果：确认归因后 `active_binding_count=1`、`bound_search_term_rows=1`、产品筛选 `group_summary=1`。
- 真实库结果：`active_binding_count=0`、`bound_search_term_rows=0`、`ready=false`，本任务未写入真实归因规则。
- 验收命令已通过：

```powershell
python scripts/smoke_product_attribution_to_search_terms.py
python scripts/smoke_frontend_product_attribution_to_search_terms.py
python scripts/smoke_real_product_attribution_to_search_terms_readiness.py
python scripts/smoke_real_search_term_product_readiness.py
python scripts/smoke_real_search_term_group_drilldown.py
python scripts/smoke_real_e2e.py
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端产品接口、后端搜索词接口、同步服务、搜索词分析服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-037：真实人工归因验收引导`。链路已具备，但真实库仍 `active_binding_count=0`，需要帮助运营完成第一条真实人工归因规则并查看解锁效果。

### TASK-037 启动

- 已启动 `TASK-037：真实人工归因验收引导`。
- 本任务只在产品设置页补首条归因引导，不替运营确认真实归因规则。
- 不改后端接口、不改真实数据库、不新增任何广告执行能力。

### TASK-037 完成

- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增 `firstAttributionCandidate` 和“首条真实归因引导”。
- 引导只在 `productAdBindings.length === 0` 且存在首条候选时展示。
- 引导展示 Top 广告来源、推荐产品、可信度、可解锁搜索词行数、花费、销售、订单和 ACOS。
- 引导 CTA 为“查看证据并确认归因”，只打开现有产品归因证据抽屉，不直接保存归因规则。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，新增 `first-attribution-guide` 样式。
- 新增 [smoke_frontend_first_real_attribution_guide.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_first_real_attribution_guide.py)，约束前端引导、证据入口和禁止自动执行标记。
- 新增 [smoke_real_first_attribution_guide_readiness.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_first_attribution_guide_readiness.py)，真实 API 只读验证首条候选和归因数量不变。
- 真实库 Top 候选：`RBK004-kids sunglasses-广泛 -> RBK004 kids sunglasses`，可信度 100，可解锁 26 行搜索词。
- 真实库 `active_binding_count=0`，本任务未写入真实归因规则。
- 浏览器确认产品设置页可见“首条真实归因引导”“可解锁搜索词 26 行”“查看证据并确认归因”。
- 浏览器点击引导 CTA 后打开“产品归因证据详情”抽屉，仍显示暂无已确认归因规则。
- 验收命令已通过：

```powershell
python scripts/smoke_frontend_first_real_attribution_guide.py
python scripts/smoke_real_first_attribution_guide_readiness.py
python scripts/smoke_real_product_attribution_candidates.py
python scripts/smoke_real_product_attribution_to_search_terms_readiness.py
python scripts/smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端产品接口、后端搜索词接口、同步服务、搜索词分析服务、产品归因服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-038：产品维度搜索词归类聚合分析`。当前可以引导运营完成首条真实产品归因；归因完成后应把搜索词聚合下沉到产品维度。

### TASK-038 启动

- 已启动 `TASK-038：产品维度搜索词归类聚合分析`。
- 设计收敛后选择方案 A：复用现有搜索词分析接口和产品就绪接口，只补产品维度上下文、标题和验收。
- 当前真实库仍 `active_binding_count=0`，所以本任务用临时库验证人工归因后的产品维度聚合能力，真实库保持只读未就绪提示。

### TASK-038 完成

- 确认后端已有 `/api/search-terms/analysis?product_id=...` 和 `/api/search-terms/product-readiness`，本任务未新增后端接口。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增 `isProductSearchTermMode`、`selectedSearchTermProduct`、`selectedReadinessProduct`。
- 搜索词分析页选择产品后展示“产品维度搜索词归类聚合”上下文区。
- 产品维度上下文展示当前产品、已归因搜索词行、去重搜索词、花费、销售和 ACOS。
- 产品维度模式下归类组标题变为“产品维度归类聚合组”。
- 产品维度说明明确“基于人工确认归因后的产品筛选”。
- 未就绪时提供“前往产品设置确认归因”入口，仅切换到产品设置页，不保存归因规则。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，新增 `product-search-term-context` 样式。
- 新增 [smoke_product_dimension_search_term_groups.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_product_dimension_search_term_groups.py)，临时库验证人工确认归因后产品维度 `group_summary` 非空，组级人工记录保存 `product_id`。
- 新增 [smoke_frontend_product_dimension_search_terms.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_dimension_search_terms.py)，约束前端产品维度上下文和禁止自动执行标记。
- 临时库验证人工归因后 `active_binding_count=1`、`bound_search_term_rows=2`、`products_with_search_terms=1`。
- 临时库产品维度归类组包含 `age_spec:costly_no_order` 和 `age_spec:high_conversion`。
- 真实库只读验收：`active_binding_count=0`、`bound_search_term_rows=0`、`ready=false`，本任务未写入真实归因规则。
- 浏览器确认真实页面未选产品时显示产品维度分析未就绪和全局归类聚合组。
- 浏览器选择 `RBK004 kids sunglasses` 后显示“产品维度搜索词归类聚合”“产品维度归类聚合组”和“前往产品设置确认归因”。
- 验收命令已通过：

```powershell
python scripts/smoke_product_dimension_search_term_groups.py
python scripts/smoke_frontend_product_dimension_search_terms.py
python scripts/smoke_real_product_attribution_to_search_terms_readiness.py
python scripts/smoke_real_search_term_product_readiness.py
python scripts/smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端产品接口、后端搜索词接口、同步服务、搜索词分析服务、产品归因服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-039：真实人工归因后的产品维度复核`。运营人工确认首条真实归因后，再复核真实产品维度搜索词归类组、组级记录和后续复盘链路。

### TASK-039 启动

- 已启动 `TASK-039：真实人工归因后的产品维度复核清单`。
- 设计收敛后选择方案 C：在产品维度搜索词页新增只读复核清单，不替运营确认真实归因，不写真实库。
- 本任务不新增后端接口，复用现有产品就绪、搜索词分析和组级人工记录数据。

### TASK-039 完成

- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增 `productReviewChecklist`，只读计算产品维度复核状态。
- 搜索词分析页选择产品后展示“产品维度复核清单”。
- 复核清单包含“已确认归因规则”“已归因搜索词”“产品维度归类组”“组级人工记录”四项。
- 当前真实库未人工归因时，`RBK004 kids sunglasses` 的四项均显示待处理，并显示“待人工确认归因”。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，新增 `product-review-checklist` 样式，并在移动端断点改为单列。
- 新增 [smoke_product_dimension_review_checklist.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_product_dimension_review_checklist.py)，用内存 SQLite 验证人工确认归因和组判断后四项复核数据均可读。
- 新增 [smoke_frontend_product_dimension_review_checklist.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_dimension_review_checklist.py)，约束前端复核清单、四项检查和禁止自动执行标记。
- 真实库只读验收：`active_binding_count=0`、`bound_search_term_rows=0`、`ready=false`，本任务未写入真实归因规则。
- 浏览器确认搜索词分析页选择 `RBK004 kids sunglasses` 后可见“产品维度复核清单”和四项待处理状态。
- 验收命令已通过：

```powershell
python scripts/smoke_product_dimension_review_checklist.py
python scripts/smoke_frontend_product_dimension_review_checklist.py
python scripts/smoke_real_product_attribution_to_search_terms_readiness.py
python scripts/smoke_real_search_term_product_readiness.py
python scripts/smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端产品接口、后端搜索词接口、同步服务、搜索词分析服务、产品归因服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-040：用户搜索词归类聚合分析可用性复核`。现有聚合已经能看，但需要从运营决策效率角度验证排序、筛选、证据和人工记录是否真的有用。

### TASK-040 启动

- 已启动 `TASK-040：用户搜索词归类聚合分析可用性复核`。
- 设计收敛后选择方案 C：不重写后端归类算法，只在现有归类聚合组上新增“优先复核顺序”。
- 本任务不新增后端接口，不写真实库，不新增任何广告执行能力。

### TASK-040 完成

- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增 `groupPriorityReviewItems`，基于 `searchTermAnalysis.group_summary` 前端只读计算优先复核组。
- 搜索词分析页新增“归类组优先复核顺序”，展示优先级、理由、指标、代表搜索词。
- 优先顺序覆盖“先看高风险组”“高转化机会”“数据不足大池”三类。
- 每类至少露出一个样本，再补充高优先级组，避免真实数据里“数据不足大池”被前 4 个高转化组挤掉。
- 优先组复用 `drillDownSearchTermGroup(item.group)` 和 `openSearchTermGroupDecisionModal(item.group)`，不新增动作能力。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，新增 `search-term-priority-review` 样式，并补充移动端单列展示。
- 新增 [smoke_frontend_search_term_group_usability.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_search_term_group_usability.py)，约束前端优先复核区和禁止自动执行标记。
- 新增 [smoke_real_search_term_group_usability.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_search_term_group_usability.py)，真实 API 只读验证当前数据能计算非空优先复核组。
- 真实数据周期：`2026-05-12` 至 `2026-06-10`，`group_count=10`，`priority_count=8`。
- 真实数据 Top 优先组：`age_spec:high_acos`，即“年龄 / 人群规格词 / 有单但 ACOS 高”，花费 `35.92`，订单 `6`，词数 `4`。
- 浏览器确认“归类组优先复核顺序”可见，实际展示 5 组，并包含“先看高风险组”“高转化机会”“数据不足大池”三类理由。
- 验收命令已通过：

```powershell
python scripts/smoke_frontend_search_term_group_usability.py
python scripts/smoke_real_search_term_group_usability.py
python scripts/smoke_real_search_term_group_aggregation.py
python scripts/smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端产品接口、后端搜索词接口、同步服务、搜索词分析服务、产品归因服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-041：组级人工判断复盘入口`。归类组已经能排序、下钻和记录判断，下一步应把组级人工判断接入复盘闭环。

### TASK-041 启动

- 已启动 `TASK-041：组级人工判断复盘入口`。
- 设计收敛后选择方案 C：复用已有组级人工记录和候选复盘模式，新增组级只读复盘入口。
- 本任务不重写搜索词归类算法，不写真实库，不新增任何广告执行能力。

### TASK-041 完成

- 更新 [search_terms.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/search_terms.py)，新增 `GET /api/search-terms/group-decisions/{decision_id}/review`。
- 组级复盘根据 `group_snapshot_json` 生成判断前指标，根据后续 7 天 / 14 天同组 `group_summary` 生成判断后指标。
- 复盘返回 `before_metrics`、`after_metrics`、`delta_metrics`、`result`、`result_label`、`manual_hint`。
- 更新 [api.ts](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/api.ts)，新增 `SearchTermGroupDecisionReview` 和 `fetchSearchTermGroupDecisionReview`。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，组级人工记录表新增“查看复盘”和“14 天”入口，并新增“组级复盘”弹窗。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，新增 `search-term-group-review-panel` 样式标记。
- 新增 [smoke_search_term_group_decision_review.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_search_term_group_decision_review.py)，用内存 SQLite 验证组级复盘接口、指标变化、非法周期校验和禁止自动执行字段。
- 新增 [smoke_frontend_search_term_group_decision_review.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_search_term_group_decision_review.py)，约束前端 API、入口、弹窗、变化指标和禁止自动执行标记。
- 新增 [smoke_real_search_term_group_decision_review.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_search_term_group_decision_review.py)，真实 API 只读验收；当前真实库暂无组级人工记录，返回 `skipped_no_group_decisions`，产品归因规则数量不变。
- 浏览器确认搜索词分析页可见“组级人工记录”“查看复盘”，并显示“不代表广告已自动修改”；前端控制台无错误。
- 验收命令已通过：

```powershell
python scripts/smoke_search_term_group_decision_review.py
python scripts/smoke_frontend_search_term_group_decision_review.py
python scripts/smoke_real_search_term_group_decision_review.py
python scripts/smoke_search_term_group_decisions.py
python scripts/smoke_real_search_term_group_aggregation.py
python scripts/smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务、搜索词分析服务、产品归因服务、产品接口、决策模型、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-042：真实人工归因与组级复盘演练引导`。当前真实库仍没有产品归因规则和组级人工记录，系统能力已具备，但还需要把一次真实人工操作路径跑通。

### TASK-042 启动

- 已启动 `TASK-042：真实人工归因与组级复盘演练引导`。
- 设计收敛后选择方案 C：在产品设置页新增“真实闭环演练”任务条，复用现有入口，不新增后端接口。
- 本任务不替运营确认归因，不写真实库，不新增任何广告执行能力。

### TASK-042 完成

- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增 `realWorkflowRehearsalSteps`。
- 产品设置页新增“真实闭环演练”，包含四步：
  - 确认产品归因
  - 查看产品维度搜索词
  - 记录组级人工判断
  - 复盘组级判断
- 第一步复用现有 `openAttributionEvidence(...)`，只打开证据抽屉，不直接保存归因规则。
- 第二步复用 `openProductSearchTermAnalysis(productAdBindings[0])`，已有归因规则后可进入产品维度搜索词分析。
- 第三步和第四步只通过 `setActiveTab("search_terms")` 切到搜索词分析页，不写库。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，新增 `real-workflow-rehearsal` 样式，并补充移动端单列展示。
- 新增 [smoke_frontend_real_workflow_rehearsal.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_real_workflow_rehearsal.py)，约束前端演练入口、四步流程、人工边界和禁止自动执行标记。
- 新增 [smoke_real_workflow_rehearsal_readiness.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_workflow_rehearsal_readiness.py)，真实 API 只读检查当前闭环状态。
- 当前真实状态：
  - `workflow_status=needs_manual_attribution`
  - `active_binding_count=0`
  - `candidate_count=20`
  - `group_count=10`
  - `group_decision_count=0`
- 当前真实 Top 候选：`RBK004-kids sunglasses-广泛 -> RBK004 kids sunglasses`，可信度 `100`，可解锁搜索词 `26` 行。
- 浏览器确认产品设置页可见“真实闭环演练”四步和“查看证据并确认归因”；前端控制台无错误。
- 验收命令已通过：

```powershell
python scripts/smoke_frontend_real_workflow_rehearsal.py
python scripts/smoke_real_workflow_rehearsal_readiness.py
python scripts/smoke_frontend_first_real_attribution_guide.py
python scripts/smoke_frontend_product_dimension_review_checklist.py
python scripts/smoke_real_product_attribution_candidates.py
python scripts/smoke_real_search_term_group_aggregation.py
python scripts/smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-043：人工确认首条归因后的真实闭环验收`。需要运营在前端点击“查看证据并确认归因”并人工确认后，再验收产品维度搜索词、组级判断和复盘链路。

### TASK-043 启动

- 已启动 `TASK-043：人工确认首条归因后的真实闭环验收`。
- 本任务只验收用户已人工确认归因后的真实链路，并修正证据弹窗文案误导。
- 不替运营记录组级判断，不写真实 `data/app.db`，不新增任何广告执行能力。

### TASK-043 完成

- 新增 [smoke_frontend_attribution_evidence_clarity.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_attribution_evidence_clarity.py)，约束证据弹窗必须显示：
  - 广告对象证据快照
  - 当前将归因到的产品
  - 手工选择产品后再保存
- 新增 [smoke_real_post_attribution_workflow.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_post_attribution_workflow.py)，只读验收真实人工归因后的产品维度链路。
- 真实库当前状态：
  - `active_binding_count=1`
  - `workflow_status=needs_group_decision`
  - `bound_search_term_rows=26`
  - `product_group_count=3`
- 修复 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 中高可信归因候选表“查看证据”按钮的旧周期闭包问题。
- 浏览器确认首个候选证据弹窗使用周期 `2026-05-12 至 2026-06-10`，并显示花费 `$79.68`、销售额 `$369.06`、订单 `30`、ACOS `21.6%`。
- 验收命令已通过：

```powershell
python scripts/smoke_frontend_attribution_evidence_clarity.py
python scripts/smoke_real_post_attribution_workflow.py
python scripts/smoke_real_workflow_rehearsal_readiness.py
python scripts/smoke_real_search_term_product_readiness.py
python scripts/smoke_real_product_attribution_to_search_terms_readiness.py
python scripts/smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-044：人工记录首个组级判断后的真实复盘验收`。当前系统已进入组级人工判断阶段，需要运营在前端记录一个组级判断后再验收复盘链路。

### TASK-044 启动

- 已启动 `TASK-044：人工记录首个组级判断后的真实复盘验收`。
- 本任务只验收用户已人工保存的组级判断和复盘接口，不替运营补写判断。
- 不修改后端、前端、真实 `data/app.db`，不新增任何广告执行能力。

### TASK-044 完成

- 真实库已存在首条组级人工判断：`decision_id=1`。
- 该判断是全局组判断：`product_id=null`，不是产品级组判断。
- 组级复盘接口可读，返回 `result=data_pending`，当前后续数据不足，属于可解释状态。
- 当前全局闭环状态：`workflow_status=ready_for_review`，`group_decision_count=1`。
- 当前产品级闭环状态：产品 `RBK004 kids sunglasses` 已归因，`bound_search_term_rows=26`，但产品级 `group_decision_count=0`。
- 验收命令已通过：

```powershell
python scripts\smoke_real_search_term_group_decision_review.py
python scripts\smoke_real_workflow_rehearsal_readiness.py
python scripts\smoke_real_post_attribution_workflow.py
python scripts\smoke_real_e2e.py
```

- 复盘验收未改变产品归因规则数量。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、前端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-045：产品级组判断入口口径收敛`。当前前端容易让运营在产品维度已就绪时保存成全局组判断，需要明确全局 / 产品级口径。

### TASK-045 启动

- 已启动 `TASK-045：对象口径清晰化`。
- 本任务回应当前页面对象模糊问题：产品设置页里混杂了产品、广告来源和归因规则；搜索词组判断里混杂了全局组判断和产品级组判断。
- 设计收敛后选择最小方案：先把关键板块的“当前对象”和“记录口径”标清楚，不重做信息架构。
- 不改后端，不改真实 `data/app.db`，不新增任何广告自动执行能力。

### TASK-045 完成

- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，产品设置页新增对象口径说明：
  - `当前对象：产品`
  - `当前对象：广告来源`
  - `当前对象：归因规则`
- 搜索词分析页在产品维度模式下明确后续记录会作为产品级组判断。
- 组级人工判断弹窗新增当前记录口径：产品级组判断或全局组判断。
- 组级人工记录列表新增当前列表口径，避免把全局组判断误读为产品级记录。
- 组级人工记录表新增记录口径列，区分产品级组判断和全局组判断。
- 产品维度复核清单将组级人工记录文案收敛为当前产品级记录。
- 新增 [smoke_frontend_object_scope_clarity.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_object_scope_clarity.py)，先红灯后转绿。
- 当前真实事实：
  - 全局 `decision_id=1` 可复盘。
  - 产品级 `group_decision_count=0` 仍如实保留。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_object_scope_clarity.py
python scripts\smoke_frontend_search_term_group_decisions.py
python scripts\smoke_frontend_product_dimension_review_checklist.py
python scripts\smoke_real_post_attribution_workflow.py
python scripts\smoke_real_search_term_group_decision_review.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 浏览器工具未能附着到当前 in-app browser 标签，本轮以源码 smoke、真实 API smoke 和构建结果验收。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-046：人工保存产品级组判断后的真实复盘验收`。需要运营在产品维度搜索词分析下重新保存一条产品级组判断后，再验收产品级复盘闭环。

### TASK-046 启动

- 已启动 `TASK-046：产品级组判断待办入口收敛`。
- 真实检查结果：`active_binding_count=1`，产品维度搜索词已就绪，但产品级 `group_decision_count=0`。
- 因此不能直接做复盘验收，也不能替运营写入产品级组判断。
- 本任务只在前端增加更明确的人工待办入口，不改后端、不改真实 `data/app.db`。

### TASK-046 完成

- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，产品维度复核清单新增“待记录产品级组判断”提示。
- 待办入口只在产品维度模式、存在产品归类组且当前产品级组判断为 0 时展示。
- 点击“记录产品级组判断”只调用 `openSearchTermGroupDecisionModal(searchTermAnalysis.group_summary[0])` 打开人工判断弹窗。
- 提交组级判断仍使用 `product_id: searchTermProductId ?? undefined`。
- 新增 [smoke_frontend_product_group_decision_todo.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_group_decision_todo.py)，先红灯后转绿。
- 当前真实事实仍如实保留：
  - `workflow_status=needs_group_decision`
  - `active_binding_count=1`
  - `product_group_count=3`
  - `group_decision_count=0`
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_group_decision_todo.py
python scripts\smoke_frontend_object_scope_clarity.py
python scripts\smoke_frontend_search_term_group_decisions.py
python scripts\smoke_frontend_product_dimension_review_checklist.py
python scripts\smoke_real_post_attribution_workflow.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-047：人工保存产品级组判断后的真实复盘验收`。需要运营点击“记录产品级组判断”并人工保存后，再验收产品级复盘闭环。

### TASK-047 启动

- 已启动 `TASK-047：真实闭环演练产品级入口收敛`。
- 真实检查结果仍为 `workflow_status=needs_group_decision`、产品级 `group_decision_count=0`。
- 不能直接做复盘验收，也不能替运营写入产品级组判断。
- 本任务只收敛产品设置页“真实闭环演练”的入口，让第 3 步直接带入已归因产品。

### TASK-047 完成

- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增 `openFirstBoundProductSearchTermAnalysis`。
- 产品设置页“真实闭环演练”的第 3 步改为“记录产品级组判断”。
- 第 3 步点击后复用 `openProductSearchTermAnalysis(productAdBindings[0])`，直接带入已归因产品的搜索词分析。
- 第 3 步状态基于当前产品维度记录数，不再把全局组判断误报为产品级已记录。
- 第 4 步“查看产品级组记录”也优先带入已归因产品。
- 新增 [smoke_frontend_real_workflow_product_group_entry.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_real_workflow_product_group_entry.py)，先红灯后转绿。
- 当前真实事实仍如实保留：
  - `workflow_status=needs_group_decision`
  - `active_binding_count=1`
  - `product_group_count=3`
  - `group_decision_count=0`
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_real_workflow_product_group_entry.py
python scripts\smoke_frontend_product_group_decision_todo.py
python scripts\smoke_frontend_object_scope_clarity.py
python scripts\smoke_real_post_attribution_workflow.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-048：人工保存产品级组判断后的真实复盘验收`。需要运营在产品维度下人工保存产品级组判断后再验收。

### TASK-048 启动

- 已启动 `TASK-048：当前 MVP 缺口基线复核收敛`。
- 直接运行旧基线脚本发现：真实状态已进入产品归因后阶段，但 `next_task_recommended` 仍停留在“产品与广告归因闭环”。
- 本任务只修正只读基线 smoke，不改后端、不改前端、不写真实 `data/app.db`。

### TASK-048 完成

- 更新 [smoke_current_mvp_baseline.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_current_mvp_baseline.py)，新增 `current_workflow` 输出。
- `current_workflow` 当前真实结果：
  - `workflow_status=needs_group_decision`
  - `active_binding_count=1`
  - `product_id=2`
  - `bound_search_term_rows=26`
  - `product_group_count=3`
  - `product_group_decision_count=0`
- `baseline_gaps` 当前明确列出三个缺口：
  - 产品级组判断未保存；
  - 真实库暂无异常人工处理记录；
  - 真实库暂无复盘记录。
- `next_task_recommended` 已更新为“人工保存产品级组判断后的真实复盘验收”。
- 新增 [smoke_current_mvp_gap_baseline.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_current_mvp_gap_baseline.py)，先红灯后转绿。
- 验收命令已通过：

```powershell
python scripts\smoke_current_mvp_gap_baseline.py
python scripts\smoke_current_mvp_baseline.py
python scripts\smoke_real_post_attribution_workflow.py
python scripts\smoke_real_e2e.py
```

- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、前端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-049：人工保存产品级组判断后的真实复盘验收`。需要运营先人工保存产品级组判断。

### TASK-049 启动

- 已启动 `TASK-049：产品中心与广告归因页签拆分`。
- 用户指出当前“产品设置”页面里混入 Campaign / Ad Group，产品对象边界不清。
- 只做前端信息架构拆分，不改后端模型、不写真实库。

### TASK-049 完成

- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，将原“产品设置”拆为“产品中心”和“广告归因”两个一级页签。
- “广告归因”页签承载真实闭环演练、高可信归因候选、未归因广告数据池和已确认归因规则。
- “产品中心”页签只保留产品基础设置表，不再展示“绑定广告活动”输入列。
- 搜索词产品维度未就绪入口已改为“前往广告归因确认归因”。
- 新增 [smoke_frontend_product_attribution_navigation_split.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_attribution_navigation_split.py)，先红灯后转绿。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_attribution_navigation_split.py
python scripts\smoke_frontend_object_scope_clarity.py
python scripts\smoke_frontend_product_dimension_search_terms.py
python scripts\smoke_frontend_search_term_product_readiness.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_post_attribution_workflow.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 当前真实事实仍如实保留：产品级 `group_decision_count=0`，本任务没有写入真实 `data/app.db`。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-050：人工保存产品级组判断后的真实复盘验收`。需要运营先在产品维度下人工保存产品级组判断。

### TASK-050 启动

- 已启动 `TASK-050：产品中心归因状态入口补齐`。
- 真实状态仍为 `workflow_status=needs_group_decision`、产品级 `group_decision_count=0`，不能替运营做产品级组判断。
- 本任务只在产品中心露出归因状态和跳转入口，不写库、不改后端。

### TASK-050 完成

- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，产品中心表格新增“归因状态”列。
- 归因状态基于 `productAdBindings` 按 `product_id` 统计当前产品归因规则数量。
- 产品行显示“已归因 N”或“待归因”。
- “管理归因”按钮会设置当前产品并切换到“广告归因”页签。
- 新增 [smoke_frontend_product_center_attribution_status.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_center_attribution_status.py)，先红灯后转绿。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_center_attribution_status.py
python scripts\smoke_frontend_product_attribution_navigation_split.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_post_attribution_workflow.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 当前真实事实仍如实保留：产品级 `group_decision_count=0`，本任务没有写入真实 `data/app.db`。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-051：人工保存产品级组判断后的真实复盘验收`。需要运营先在产品维度下人工保存产品级组判断。

### TASK-051 启动

- 已将当前任务切换为 `TASK-051：销售表现接口产品源验证`。
- 用户明确当前讨论对象是积加开放平台 `统计 > 销售表现` 接口。
- 前端“产品中心直达搜索词”入口暂缓，先确认产品中心的基础数据源。
- 本任务只查官方文档详情，不调用真实业务数据接口、不写真实库、不改前端。

### TASK-051 完成

- 新增 [smoke_sales_performance_product_source.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_sales_performance_product_source.py)，只读读取官方文档详情 `https://open.gerpgo.com/api/openAdmin/doc/detail?id=3375`。
- 验证销售表现接口：
  - `apiUrl=/operation/sts/salesAnalysis/page`
  - `erpUrl=/v2/statistics/openPlatform/salesAnalysis`
  - 请求方式：`Post`
- 验证请求字段包含：
  - `groupByType`
  - `showCurrencyType`
  - `beginDate`
  - `endDate`
  - `page`
  - `pagesize`
- 验证 `groupByType` 文档说明 MSKU 维度应传 `seller_sku`。
- 验证 `data.rows` 共 177 个字段，覆盖：
  - 产品基础档案：`asin`、`msku`、`sku`、`productName`、`variationAsin`、`imgUrl`、`brand`、`category`、`marketId`、`marketName`、`countryName`
  - 销售表现：`unitsOrdered`、`orders`、`orderProductSales`、`averageDailySales`、`averagePrice`
  - 流量转化：`sessions`、`pageViews`、`orderCvr`、`buyBoxPercentage`
  - 广告辅助：`adsImpressions`、`adsClicks`、`adsOrders`、`adsSpend`、`adsSales`、`acos`、`cpc`、`adsCvr`、`spAdsOrders`、`spAdsSales`
  - 利润成本：`salesGrossProfit`、`grossProfitRate`、`salesNetProfit`、`salesNetProfitRate`、`roi`、`purchaseCost`、`commissionCost`、`fbaShippingCost`
  - 库存生命周期：`onInventory`、`salesTurnoverRate`、`listingLevel`、`listingState`、`addDate`、`firstSalesDate`
  - 促销影响：`coupon`、`promotionDiscount`、`couponPromotionUnitsOrdered`、`promotionOrderProductSales`
- 第一版产品基础字段映射确定为：
  - `products.asin <- asin`
  - `products.msku <- msku`
  - `products.sku <- sku`
  - `products.product_name <- productName`
  - `products.image_url <- imgUrl`
  - `products.brand <- brand`
  - `products.category <- category`
  - `products.market_id <- marketId`
- 边界已明确：销售表现接口用于产品基础档案、产品经营表现快照和广告判断背景证据，不替代 SP 关键词 / 搜索词报表，不做广告自动执行。
- 验收命令已通过：

```powershell
python scripts\smoke_sales_performance_product_source.py
python scripts\smoke_real_e2e.py
```

- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、前端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-052：销售表现接口真实数据只读探测`。等待积加限流恢复后，用 `groupByType=seller_sku`、`pagesize=10` 只读拉取 1 页真实 rows，再决定是否写入产品基础档案。

### TASK-052 启动

- 已启动 `TASK-052：销售表现接口真实数据只读探测`。
- 用户补充截图确认销售表现接口限流规则为“每 5 秒 1 次”。
- 本任务只做真实接口只读探测，不写库、不保存原始响应、不改前端。

### TASK-052 完成

- 新增 [smoke_sales_performance_real_probe.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_sales_performance_real_probe.py)。
- 探针使用真实积加配置只读调用 `/operation/sts/salesAnalysis/page`。
- 请求参数：
  - `groupByType=seller_sku`
  - `showCurrencyType=USD`
  - `beginDate=2026-05-12`
  - `endDate=2026-06-10`
  - `page=1`
  - `pagesize=10`
  - `marketId=1`
- 探针调用前固定等待 `5.2` 秒，符合限流规则。
- 真实接口返回成功：
  - `row_count=10`
  - `total=209`
  - `page=1`
- 真实样本包含：
  - `asin`
  - `msku`
  - `sku`
  - `productName`
  - `marketId`
  - `marketName`
- 真实样本字段覆盖销售、流量、广告辅助三类表现字段。
- 验收命令已通过：

```powershell
python scripts\smoke_sales_performance_real_probe.py
python scripts\smoke_real_e2e.py
```

- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、前端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-053：销售表现产品基础档案同步`。按 `market_id + msku` 最小 upsert 到 `products`，只写产品基础字段，销售表现指标先不混入产品表。

### TASK-053 启动

- 已启动 `TASK-053：销售表现产品基础档案同步`。
- 本任务目标是把销售表现接口里的真实产品基础档案写入本地 `products`。
- 只写产品基础字段，不把销售额、订单、ACOS、Sessions 等表现指标混入产品表。

### TASK-053 完成

- 新增 [smoke_sales_performance_product_sync.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_sales_performance_product_sync.py)，用临时 SQLite 验证：
  - `market_id + msku` upsert；
  - 产品基础字段映射；
  - 跳过缺失 `msku` 的行；
  - `products` 表不包含销售表现指标字段。
- 新增 [sync_sales_performance_products.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/sync_sales_performance_products.py)，从销售表现接口同步产品基础档案。
- 同步脚本固定使用：
  - `api_path=/operation/sts/salesAnalysis/page`
  - `groupByType=seller_sku`
  - `pagesize=10`
  - `PAGE_DELAY_SECONDS=5.2`
- 同步脚本只写：
  - `asin`
  - `msku`
  - `sku`
  - `product_name`
  - `image_url`
  - `brand`
  - `category`
  - `market_id`
- 真实完整同步已完成：
  - `pages_synced=21`
  - `rows_seen=209`
  - `reported_total=209`
  - `created=199`
  - `updated=10`
  - `skipped=0`
- 本地 API `/api/products?market_id=1` 当前返回 `count=218`，其中包含销售表现接口同步的真实产品。
- 验收命令已通过：

```powershell
python scripts\smoke_sales_performance_product_sync.py
python scripts\sync_sales_performance_products.py --max-pages 1
python scripts\sync_sales_performance_products.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_e2e.py
```

- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、前端。
- 未新增任何广告自动执行能力。
- 当前剩余问题：`/api/products?market_id=1` 前几行仍包含历史 DEMO/广告反推产品，产品中心第一眼可能仍然不够清楚。
- 下一步建议：`TASK-054：产品中心真实产品优先展示`。让前端或展示层优先露出销售表现同步的真实产品，并隔离历史演示/草稿产品。

### TASK-054 启动

- 已启动 `TASK-054：产品中心真实产品优先展示`。
- 本任务只改产品列表 API 展示排序和只读 smoke 脚本。
- 不改前端、不写真实 `data/app.db`、不删除历史 DEMO / 广告反推产品。

### TASK-054 完成

- 新增 [smoke_product_center_real_product_priority.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_product_center_real_product_priority.py)，只读验收 `/api/products?market_id=1` 首屏产品优先级。
- 先红灯确认产品列表前 5 行仍包含 `DEMO-ACOS`。
- 收紧 smoke 后确认真实产品内部仍可能优先展示 `product_name=msku`、类目为 `-` 的弱识别产品。
- 更新 [products.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/products.py)，产品列表排序改为：
  - 真实销售表现产品优先；
  - 产品名不等于 MSKU、类目有效的真实产品更优先；
  - DEMO / 测试产品排后。
- 重新启动本地后端后，真实 API 返回：
  - `product_count=218`
  - `real_product_count=208`
  - `non_real_product_count=2`
- 产品列表前 5 行已变为真实 ASIN、MSKU、可读产品名和有效类目的产品。
- 验收命令已通过：

```powershell
python scripts\smoke_product_center_real_product_priority.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_e2e.py
python -m py_compile backend\app\api\routes\products.py scripts\smoke_product_center_real_product_priority.py
```

- 未修改 `.env`、`.env.example`、`frontend/.env.local`、前端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-055：产品中心真实身份字段前端收敛`。产品列表 API 已优先返回真实且可读的产品；下一步可在前端产品中心强化 ASIN、MSKU、产品名、类目、店铺来源的展示口径。

### TASK-055 启动

- 已启动 `TASK-055：产品中心真实身份字段前端收敛`。
- 本任务只改产品中心前端展示层和源码 smoke。
- 不改后端、不写真实 `data/app.db`、不新增任何广告执行能力。

### TASK-055 完成

- 新增 [smoke_frontend_product_center_identity_fields.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_center_identity_fields.py)，先红灯确认产品中心缺少真实身份字段层级、类目标签和销售表现来源口径。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，产品中心第一列从 `ASIN / MSKU / 产品名称` 改为 `真实产品身份`。
- 产品身份单元格现在分层展示：
  - 产品名
  - ASIN
  - MSKU
  - 类目
- `店铺 / 站点` 列收敛为 `店铺 / 数据来源`，显示 `market_id` 和 `销售表现` 来源标签。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，新增产品身份单元格样式，支持长 MSKU / 类目换行。
- 保留归因状态和 `管理归因` 入口。
- 未恢复广告活动绑定输入。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_center_identity_fields.py
python scripts\smoke_frontend_product_center_attribution_status.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-056：产品中心真实页面截图验收`。用真实浏览器页面确认产品中心首屏没有视觉拥挤或误读。

### TASK-056 启动

- 已启动 `TASK-056：产品中心真实页面截图验收`。
- 本任务只做真实浏览器页面验收、截图保存和进展记录。
- 不改业务代码、不写真实库、不新增任何广告执行能力。

### TASK-056 完成

- 使用真实本地页面 `http://127.0.0.1:5173/` 进入产品中心。
- 浏览器 DOM 验收确认当前激活页签为 `产品中心`。
- 首屏可见 `真实产品身份`、`店铺 / 数据来源`、`归因状态`、`管理归因`。
- 首屏产品行可见真实产品名、ASIN、MSKU、类目、`market_id 1`、`销售表现` 来源标签。
- 首屏未出现 `DEMO` / `SMOKE` / `TASK` 测试产品。
- 页面未出现 `ASIN / MSKU / 产品名称` 旧列名或广告活动绑定输入。
- 截图已保存：[product-center-real-identity.png](D:/DataProject/coedx_project/Amazon广告分析/tmp/task056/product-center-real-identity.png)，文件大小 `112708` bytes。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_center_identity_fields.py
python scripts\smoke_frontend_product_center_attribution_status.py
python scripts\smoke_real_e2e.py
```

- 观察点：当前产品中心表格仍偏宽，存在横向滚动；如继续处理 UI，需要单独开任务。
- 未修改业务代码、未写真实库、未新增任何广告执行能力。
- 下一步建议：`TASK-057：销售表现产品经营快照接入`。用户截图确认销售表现接口为 `/operation/sts/salesAnalysis/page`；下一步应接入产品级销售、流量、利润和广告辅助背景证据，先写独立快照，不污染 `products`。

### TASK-057 启动

- 已启动 `TASK-057：销售表现产品经营快照接入`。
- 本任务目标是把销售表现接口中的产品经营指标接入为独立快照，而不是写进 `products` 表。
- 边界保持不变：只做经营背景证据，不替代 SP 关键词 / 搜索词报表，不新增任何广告执行能力。

### TASK-057 完成

- 新增 `ProductSalesSnapshot` 独立快照表，`products` 表仍只保存产品基础档案。
- 更新 [sync_sales_performance_products.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/sync_sales_performance_products.py)，同步产品基础档案时同步写入经营快照。
- 快照字段覆盖：
  - `units_ordered`
  - `orders`
  - `sales`
  - `sessions`
  - `order_cvr`
  - `ads_spend`
  - `ads_sales`
  - `acos`
  - `gross_profit`
  - `net_profit`
- 更新 [products.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/products.py)，产品列表 API 返回 `sales_snapshot`。
- 更新 [api.ts](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/api.ts)、[App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)、[styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，产品中心新增 `销售表现快照` 列。
- 产品中心快照列展示：
  - 销售额
  - 订单
  - Sessions
  - 净利
  - 广告辅助
  - 快照周期
- 新增 smoke：
  - [smoke_sales_performance_snapshot_sync.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_sales_performance_snapshot_sync.py)
  - [smoke_product_sales_snapshot_api.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_product_sales_snapshot_api.py)
  - [smoke_frontend_product_sales_snapshot.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_sales_snapshot.py)
- 真实销售表现完整同步成功：
  - `pages_synced=21`
  - `rows_seen=209`
  - `reported_total=209`
  - `snapshots_created=199`
  - `snapshots_updated=10`
- 真实库当前：
  - `product_sales_snapshots=209`
  - `nonzero_snapshots=167`
  - `market_1_products=218`
- 运行中 API `/api/products?market_id=1&start_date=2026-05-12&end_date=2026-06-10` 验证：
  - `total=218`
  - `with_snapshot=209`
  - `nonzero_snapshot=167`
  - 样本 `RB831- Full black`：销售额 `4319.06`、订单 `185`、Sessions `831`
- 浏览器真实页面确认产品中心可见 `销售表现快照`，首屏可见非 0 销售额、订单、Sessions 和净利。
- 验收命令已通过：

```powershell
python scripts\smoke_sales_performance_snapshot_sync.py
python scripts\smoke_product_sales_snapshot_api.py
python scripts\smoke_frontend_product_sales_snapshot.py
python scripts\smoke_sales_performance_product_sync.py
python scripts\sync_sales_performance_products.py --max-pages 1
python scripts\sync_sales_performance_products.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `python scripts\sync_sales_performance_products.py` 和 `python scripts\smoke_product_settings.py` 因真实库写入 / Windows 临时库权限需要非沙箱执行。
- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- `python -m py_compile ...` 因 Windows 拒绝写 `__pycache__` 未使用；已改用内存 `compile(...)` 完成语法检查并通过。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、SP 同步服务。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-058：经营快照辅助广告健康判断`。把产品级销售、流量、利润背景纳入异常证据和 AI 建议说明，让运营判断广告问题时能同时看到产品经营状态。

### TASK-058 启动

- 已根据用户反馈启动 `TASK-058：market_id 前端隐藏与产品数据全量核验`。
- 用户明确边界：`market_id` 仅作为数据管理字段，不要展示在前端。
- 本任务同时核验 `market_id=1` 销售表现产品数据是否已按接口返回总数全量同步。

### TASK-058 完成

- 新增 [smoke_product_center_market_identity_api.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_product_center_market_identity_api.py)，先红灯确认 `/api/products` 未返回 `market` 店铺映射，再转绿。
- 新增 [smoke_frontend_product_center_market_identity.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_center_market_identity.py)，先红灯确认前端缺少店铺名展示逻辑，再转绿。
- 更新 [products.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/products.py)，为产品列表、详情、创建和更新返回 `market` 映射对象。
- 更新 [api.ts](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/api.ts)，为 `Product` 增加 `market` 类型。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增店铺展示格式化逻辑，产品中心显示 `rivbos / US`，不再显示 `market_id`。
- 前端移除用户可见的店铺 ID 筛选输入、驾驶舱 `market_id` 标签、异常详情店铺 ID 和产品创建店铺 ID 表单项。
- 完整同步销售表现产品数据通过：
  - `pages_synced=21`
  - `rows_seen=209`
  - `reported_total=209`
  - `updated=209`
  - `snapshots_updated=209`
- 真实库当前：
  - `products_market_1=218`
  - `snapshots_market_1=209`
  - `snapshot_mskus=209`
  - 当前快照周期 `2026-05-12` 至 `2026-06-10`
- 结论：销售表现接口当前周期返回 `209` 个 MSKU 维度产品行，已全部同步；前端看到“一部分”主要是表格分页 / 滚动展示问题，不是同步只拉了一页。
- 已强制重启本地后端 `8001` 并复用前端 `5173`。
- 浏览器真实页面复查产品中心前 5 行店铺列均显示 `rivbos / US 销售表现`，页面不再包含 `market_id` 或 `店铺 / 站点 ID` 文案。
- 验收命令已通过：

```powershell
python scripts\smoke_product_center_market_identity_api.py
python scripts\smoke_frontend_product_center_market_identity.py
python scripts\smoke_frontend_product_center_identity_fields.py
python scripts\sync_sales_performance_products.py
python scripts\smoke_product_sales_snapshot_api.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、SP 同步服务。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-059：经营快照辅助广告健康判断`。把产品级销售、流量、利润背景纳入异常证据和 AI 建议说明。

### TASK-059 启动

- 已启动 `TASK-059：经营快照辅助广告健康判断`。
- 本任务目标是把产品级销售表现快照接入异常证据和 AI 建议溯源。
- 边界保持不变：经营快照只做背景证据，不改变规则阈值、严重级别、建议等级，不新增任何广告执行能力。

### TASK-059 完成

- 新增 [smoke_rule_engine_sales_snapshot_context.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_rule_engine_sales_snapshot_context.py)，先红灯确认异常证据缺少 `product_sales_snapshot`，再转绿。
- 新增 [smoke_ai_suggestion_sales_snapshot_context.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_ai_suggestion_sales_snapshot_context.py)，先红灯确认 AI 建议摘要和溯源缺少产品经营背景，再转绿。
- 更新 [rule_service.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/services/rule_service.py)，为 7 类异常证据挂载同产品、同周期 `ProductSalesSnapshot`。
- `evidence_json.product_sales_snapshot` 和 `rule_result_json.evidence.product_sales_snapshot` 保持一致。
- `product_sales_snapshot` 字段包含：
  - `period_start`
  - `period_end`
  - `units_ordered`
  - `orders`
  - `sales`
  - `sessions`
  - `order_cvr`
  - `ads_spend`
  - `ads_sales`
  - `acos`
  - `gross_profit`
  - `net_profit`
- 更新 [suggestion_service.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/services/suggestion_service.py)，AI 建议 `evidence_summary` 增加“产品经营背景 销售额 / 订单 / Sessions / 净利”。
- `source_trace_json.source_context.product_sales_snapshot` 可直接溯源产品经营背景。
- 明确边界：销售表现快照只作为经营背景证据，不参与异常规则筛选、阈值、严重级别或建议等级判断。
- 验收命令已通过：

```powershell
python scripts\smoke_rule_engine_sales_snapshot_context.py
python scripts\smoke_ai_suggestion_sales_snapshot_context.py
python scripts\smoke_rule_engine.py
python scripts\smoke_ai_suggestion.py
python scripts\smoke_real_e2e.py
```

- Python 内存 `compile(...)` 语法检查通过。
- 已重启本地后端 `8001` 并确认前端 `5173` 指向最新后端；重启后 `python scripts\smoke_real_e2e.py` 仍通过。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、SP 同步服务、前端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-060：经营背景前端溯源可读化`。把建议详情里的经营快照做成可读区块，减少运营查看 JSON 的成本。

### TASK-060 启动

- 已启动 `TASK-060：经营背景前端溯源可读化`。
- Product Design 指导结论：建议详情应把产品经营背景前置为可读证据区块，JSON 只作为深层溯源，不作为运营主入口。
- 本任务只改建议详情抽屉，不扩大到整体 UI 重构。

### TASK-060 完成

- 新增 [smoke_frontend_suggestion_business_context.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_suggestion_business_context.py)，先红灯确认前端缺少“产品经营背景”区块，再转绿。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增：
  - `productSalesSnapshotFromSourceTrace`
  - `productSalesSnapshotFromUnknown`
  - `businessSnapshotMetrics`
- 建议详情抽屉新增“产品经营背景”区块。
- 读取优先级：
  - 优先读取 `selectedSuggestion.source_trace_json.source_context.product_sales_snapshot`
  - 没有建议快照时退回 `selected.evidence_json.product_sales_snapshot`
- 可读展示字段：
  - 快照周期
  - 销售额
  - 订单
  - Sessions
  - 净利
  - 广告花费
  - 广告销售
  - 产品 ACOS
- 原始 `source_trace_json`、`rule_result_json`、`evidence_json` 继续保留在下方。
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，新增紧凑经营背景网格样式。
- 浏览器只读复查：观察中异常详情抽屉已显示“产品经营背景”；该历史异常无经营快照时显示明确空态。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_suggestion_business_context.py
python scripts\smoke_frontend_suggestion_detail.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 新增 smoke 和原有建议详情 smoke 在沙箱中因 esbuild `spawn EPERM` 失败，已按权限规则非沙箱重跑通过。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：经营背景区块位于 AI 解释 / 证据摘要之后、人工处理按钮之前，符合“先读证据、再人工确认”的工作流；无快照时空态清楚。
- Product Design 复核发现的新问题：驾驶舱显示异常总数 `1`，但异常队列默认“待处理”为空，切换到“观察中”才可打开历史异常；下一步应收敛状态口径和入口跳转。
- 下一步建议：`TASK-061：异常队列状态口径与入口收敛`。浏览器只读复查发现驾驶舱显示异常总数 1，但异常队列默认“待处理”为空，切换到“观察中”才看到历史异常；需要收敛状态口径和入口跳转。

### TASK-061 启动

- 已启动 `TASK-061：异常队列状态口径与入口收敛`。
- 根因确认：后端 `/api/anomalies` 已支持不传 `status` 返回全部状态异常，问题在前端默认使用 `pending` 筛选。
- 本任务只改前端状态筛选口径和 smoke，不改后端、不写库。

### TASK-061 完成

- 新增 [smoke_frontend_anomaly_status_scope.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_anomaly_status_scope.py)，先红灯确认前端缺少“全部状态”口径，再实现转绿。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增：
  - `AnomalyStatusFilter`
  - `anomalyStatusFilterOptions`
  - `anomalyStatusParam`
- 异常队列默认状态从 `pending` 改为 `all`。
- `fetchAnomalies` 和 `generateSuggestions` 均通过 `anomalyStatusParam(status)` 转换请求参数，不会向后端发送 `status=all`。
- 驾驶舱“查看异常队列”入口会同步筛选条件，并显式设置为“全部状态”。
- 浏览器只读复查：从驾驶舱进入异常队列后，状态筛选显示“全部状态”，表格直接显示当前“观察中”异常。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_anomaly_status_scope.py
python scripts\smoke_frontend_anomaly_queue.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：入口状态口径已收敛，驾驶舱异常总数和异常队列默认列表不再互相打架。
- Product Design 复核发现的新问题：驾驶舱默认周期滚到 `2026-05-13 至 2026-06-11`，但最近真实同步周期是 `2026-05-12 至 2026-06-10`，导致首页经营指标显示 0。
- 下一步建议：`TASK-062：驾驶舱默认周期对齐最近成功同步周期`。

### TASK-062 启动

- 已启动 `TASK-062：驾驶舱默认周期对齐最近成功同步周期`。
- 根因确认：驾驶舱直接使用 `defaultDateRange()` 的滚动 30 天周期；搜索词和产品归因已具备“默认周期优先对齐最近成功同步周期”的逻辑。
- 本任务只改驾驶舱前端周期解析和 smoke，不改后端、不写库。

### TASK-062 完成

- 新增 [smoke_frontend_dashboard_latest_sync_period.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_dashboard_latest_sync_period.py)，先红灯确认驾驶舱缺少最近成功同步周期解析，再实现转绿。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增：
  - `DashboardWorkflowFilters`
  - `resolveDashboardFilters`
- 当驾驶舱周期仍为默认滚动周期时，前端会读取最近成功同步记录，并用 `period_start` / `period_end` 请求 dashboard。
- 用户手动改过驾驶舱周期时，`resolveDashboardFilters` 不覆盖用户输入。
- `loadDashboard` 已改为使用解析后的 filters 请求：
  - `fetchDashboardHealth`
  - `fetchDashboardTrends`
  - `fetchDashboardAnomalySummary`
- 浏览器只读复查：驾驶舱周期输入框显示 `2026-05-12` 至 `2026-06-10`，页面展示 `rivbos / US`、销售额 `2,912.62`、订单 `239`、ACOS `30.4%`。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_dashboard_latest_sync_period.py
python scripts\smoke_dashboard_identity.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：驾驶舱首屏已回到最近真实同步周期，核心经营指标不再因日期漂移显示 0。
- Product Design 复核暂未发现新的阻断项。
- 下一步建议：`TASK-063：真实人工确认与复盘闭环验收`。

### TASK-063 启动

- 已启动 `TASK-063：真实人工确认与复盘闭环验收`。
- 根因确认：真实本地 API 当前已有异常建议和人工处理记录，但 `/api/reviews` 暂无复盘记录。
- 本任务只验收广告异常建议的人工处理与复盘闭环；允许写本地 `data/app.db` 的复盘记录，不改积加同步、不改投放相关代码。

### TASK-063 完成

- 新增 [smoke_real_manual_review_loop.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_manual_review_loop.py)，先红灯确认文件缺失，再实现转绿。
- smoke 复用已有人工处理记录 `decision_id=1`，不自动创建人工确认记录。
- smoke 已创建或更新 `7d` 复盘记录 `review_id=1`，结果为 `unchanged`。
- `/api/decisions` 可追溯到 `suggestion_id=1`、`anomaly_event_id=1` 和异常证据。
- `/api/reviews?decision_id=1` 可读取刚保存的复盘记录。
- 浏览器复查：处理记录与复盘页可见 `7 天复盘 无明显变化`、`14 天复盘 未复盘`，复盘弹窗可打开。
- 验收命令已通过：

```powershell
python scripts\smoke_real_manual_review_loop.py
python scripts\smoke_frontend_decision_review.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、SP 同步服务。
- 未新增任何广告执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：真实处理记录页已经能让运营看到“已人工观察 -> 7 天复盘结果 -> 14 天待复盘”的闭环状态，复盘入口可打开。
- Product Design 复核发现的新问题：复盘弹窗内缺少单独的“只记录复盘，不自动修改广告”边界提示。
- 下一步建议：`TASK-064：复盘弹窗人工边界提示补齐`。

### TASK-064 启动

- 已启动 `TASK-064：复盘弹窗人工边界提示补齐`。
- 本任务只补复盘弹窗内的人工边界提示，不改 API、不写真实库、不改变复盘保存逻辑。

### TASK-064 完成

- 新增 [smoke_frontend_review_modal_manual_boundary.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_review_modal_manual_boundary.py)，先红灯确认复盘弹窗缺少人工边界提示，再实现转绿。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，在复盘弹窗表单前新增 `人工复盘边界` 提示。
- 提示文案明确说明：复盘只记录人工判断和指标快照，不自动修改广告，也不会自动调整竞价、预算、否词、暂停或新增广告。
- 浏览器复查：复盘弹窗内已可见人工边界提示，复盘周期、结果、备注等人工记录字段仍可见。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_review_modal_manual_boundary.py
python scripts\smoke_frontend_decision_review.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：复盘弹窗的人工边界已放在操作表单前，能在保存前提醒运营这是记录和复盘，不是广告执行。
- Product Design 复核发现的新问题：当前可见异常 / 建议对象仍带有 `demo high acos keyword` 等演示对象，真实日常使用时会降低可信度。
- 下一步建议：`TASK-065：真实异常对象替换 DEMO 演示异常`。

### TASK-065 启动

- 已启动 `TASK-065：真实异常对象替换 DEMO 演示异常`。
- 只读核查确认：当前真实库 `anomaly_events` 只有 1 条记录，且为 `DEMO-KW-1 / demo high acos keyword`。
- 真实 SP 关键词已有归因行，但对应真实产品尚未配置人工产品目标和规则；本任务不自动设置产品目标，不伪造真实异常。
- 本任务目标是让主工作流 API 不再展示 DEMO / SMOKE / TASK 测试异常和建议；没有真实异常时展示真实空态。

### TASK-065 完成

- 新增 [smoke_real_no_demo_anomaly_objects.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_no_demo_anomaly_objects.py)，先红灯确认 `/api/anomalies` 仍返回 `DEMO-KW-1 / demo high acos keyword`，再实现转绿。
- 更新 [anomalies.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/anomalies.py)，新增真实异常过滤条件，主列表不再返回 `DEMO` / `SMOKE` / `TASK` 测试异常对象。
- 更新 [suggestions.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/suggestions.py)，建议列表只从真实异常对象联查，避免测试异常生成的建议进入主工作流。
- 更新 [dashboard.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/dashboard.py)，驾驶舱异常统计复用同一过滤口径，不再把测试异常计入真实主工作流。
- 当前真实 API 返回：
  - `anomaly_count=0`
  - `suggestion_count=0`
  - 驾驶舱异常总数 `0`
- 浏览器复核确认驾驶舱仍展示真实数据身份和经营指标：
  - 店铺 `rivbos / US`
  - 周期 `2026-05-12 至 2026-06-10`
  - 销售额 `2,912.62`
  - 订单 `239`
  - ACOS `30.4%`
- 浏览器复核确认异常队列显示 `暂无异常`，未出现 `demo high acos keyword`、`DEMO`、`SMOKE`、`TASK`。
- 验收命令已通过：

```powershell
python scripts\smoke_real_no_demo_anomaly_objects.py
python scripts\smoke_frontend_anomaly_queue.py
python scripts\smoke_frontend_suggestion_detail.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未删除历史 DEMO 记录，未写 `data/app.db`。
- 未新增任何广告执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：主流程从“演示异常”切回“真实空态”，可信度提升；驾驶舱仍可看到真实经营指标和店铺身份，异常队列不再展示测试对象。
- Product Design 复核发现的新问题：真实已归因产品有 SP 指标，但缺少人工产品目标和规则，因此不会生成真实异常；下一步应引导运营为真实产品配置目标与规则。
- 下一步建议：`TASK-066：真实产品目标与规则设置引导`。

### TASK-066 启动

- 已启动 `TASK-066：真实产品目标与规则设置引导`。
- 只读 API 核查确认：当前真实产品 `RBK004 kids sunglasses / MSKU RBK004 / product_id=2` 有 SP 花费 `150.47`、订单 `52`、ACOS `27.57%`，但没有产品目标，也没有规则门槛。
- 本任务只做产品中心前端引导和 smoke，不自动生成目标、不写真实库、不改后端。

### TASK-066 完成

- 新增 [smoke_frontend_product_goal_rule_guidance.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_goal_rule_guidance.py)，先红灯确认前端缺少产品目标 / 规则设置引导，再实现转绿。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增：
  - `hasProductSpMetrics`
  - `needsProductGoalRuleSetup`
  - `productsNeedingGoalRuleSetup`
  - `productCenterProducts`
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，新增产品目标 / 规则引导区样式。
- 产品中心新增“待设置目标 / 规则”引导区，明确提示已有 SP 指标但缺少产品目标或目标 ACOS 的产品。
- 待设置产品会优先排在产品中心表格前面，避免运营只看到销售表现产品、看不到广告调优下一步。
- 浏览器复核确认 `RBK004 kids sunglasses / MSKU RBK004` 排在首行，SP 花费 `150.47`、订单 `52`、ACOS `27.6%`，行内显示“目标 / 规则待设置”。
- 页面文案明确要求运营人工设置产品目标和规则门槛，并说明系统不会自动生成目标或修改广告。
- 保存入口仍为人工点击“保存设置”。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_goal_rule_guidance.py
python scripts\smoke_frontend_product_center_identity_fields.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_no_demo_anomaly_objects.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `python scripts\smoke_product_settings.py` 首次在受限沙箱中因临时 SQLite 文件权限失败，按权限规则非沙箱重跑通过。
- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：引导区放在产品基础设置表格前，先解释为什么真实异常为空，再把运营带到首行待设置产品和人工保存入口，符合“AI 不执行、人工设置目标”的边界。
- Product Design 复核发现的新问题：目标 / 规则字段仍在横向大表内，后续可把单产品目标规则设置收敛成右侧编辑抽屉，但不是本任务阻断项。
- 下一步建议：`TASK-067：真实产品目标规则保存后的异常生成验收`。需要运营先人工保存真实产品目标和规则。

### TASK-067 启动

- 已启动 `TASK-067：产品目标规则编辑抽屉`。
- 只读 API 精确核查确认：`product_id=2 / RBK004 kids sunglasses / MSKU RBK004` 仍没有 `goal` 和 `rules`。
- 直接做“目标保存后的异常生成验收”前提不成立；本任务先解决人工设置入口藏在横向大表里的可用性问题。

### TASK-067 完成

- 新增 [smoke_frontend_product_goal_rule_drawer.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_goal_rule_drawer.py)，先红灯确认缺少产品目标 / 规则编辑抽屉，再实现转绿。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增：
  - `selectedGoalRuleProduct`
  - `openGoalRuleDrawer`
  - 产品目标 / 规则设置抽屉
- 更新 [styles.css](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/styles.css)，新增抽屉身份区和 SP 指标区样式。
- 待设置引导卡片和产品表格操作列都提供“设置目标 / 规则”入口。
- 抽屉展示产品身份、SP 花费、订单、ACOS、CVR、人工设置边界、产品目标和规则门槛字段。
- 抽屉保存入口为“人工保存设置”，复用现有 `saveProductSettings(selectedGoalRuleProduct.id)`。
- 浏览器复核确认首屏引导卡片可直接打开 `RBK004 kids sunglasses / MSKU RBK004` 的抽屉。
- 浏览器复核确认抽屉可见：
  - SP 花费 `150.47`
  - 订单 `52`
  - ACOS `27.6%`
  - CVR `37.7%`
  - 产品目标、目标 ACOS、目标 CVR、最小点击、最小花费、最小订单、最大 CPC、库存阈值、目标备注
  - “人工保存设置”
- 抽屉文案明确说明只保存运营人工设置的产品目标和规则门槛，不会自动生成目标，也不会自动修改广告。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_goal_rule_drawer.py
python scripts\smoke_frontend_product_goal_rule_guidance.py
python scripts\smoke_frontend_product_center_identity_fields.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `python scripts\smoke_product_settings.py` 首次在受限沙箱中因临时 SQLite 文件权限失败，按权限规则非沙箱重跑通过。
- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：入口已从横向大表前置到首屏引导卡片，抽屉把产品身份、SP 证据、人工边界和保存动作放在同一个操作面板内，比原表格直接编辑更适合日常运营。
- Product Design 复核发现的新问题：当前可设置目标的 `RBK004 kids sunglasses` 仍是 `SP广告来源草稿` 产品，没有 ASIN 和销售表现快照；销售表现接口里还有真实 RBK004 系列产品，后续需要核查广告草稿产品与销售表现产品的身份关系。
- 下一步建议：`TASK-068：广告草稿产品与销售表现产品身份关系核查`。

### TASK-068 启动

- 已启动 `TASK-068：广告草稿产品与销售表现产品身份关系核查`。
- 本任务只做前端只读核查提示和候选展示，不自动合并产品、不迁移归因、不写真实库、不改广告。

### TASK-068 完成

- 新增 [smoke_frontend_ad_draft_product_identity.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_ad_draft_product_identity.py)，先红灯确认缺少广告草稿产品身份核查提示，再实现转绿。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增：
  - `AD_DRAFT_PRODUCT_CATEGORY`
  - `isAdDraftProduct`
  - `needsAdDraftIdentityReview`
  - `findAdDraftIdentityCandidates`
- 产品中心“店铺 / 数据来源”不再把所有产品都标成销售表现；`SP广告来源草稿` 产品显示为“广告草稿”。
- 产品目标 / 规则抽屉新增“产品身份待核查”提示，说明当前对象来自 SP 广告来源草稿，不等同于完整销售表现商品。
- 抽屉新增“可能相关的销售表现产品”候选区，只读展示候选 ASIN、MSKU、销售额和订单。
- 浏览器复核确认 `RBK004 kids sunglasses / MSKU RBK004` 抽屉展示 3 个候选销售产品，包括 `RBK004-RBK004-2 天蓝 / ASIN B016EXMW4S / MSKU 004-Wayfarer Blue`。
- 候选区文案明确说明候选只读展示，不会自动合并产品、迁移归因或改广告。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_ad_draft_product_identity.py
python scripts\smoke_frontend_product_goal_rule_drawer.py
python scripts\smoke_frontend_product_center_identity_fields.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：身份风险提示放在产品身份之后、规则表单之前，运营在保存目标前能先看到“这是广告草稿，不是完整销售产品身份”和真实候选产品，方向正确。
- Product Design 复核发现的新问题：抽屉里已经能发现身份风险，但还缺少一个安全的“去广告归因页核查 / 修正归因”动作；否则运营仍可能直接给草稿对象保存目标。
- 下一步建议：`TASK-069：草稿产品身份核查后的归因修正入口`。

### TASK-069 启动

- 已启动 `TASK-069：草稿产品身份核查后的归因修正入口`。
- 本任务只做前端导航和人工核查上下文带入，不自动合并产品、不迁移归因、不写真实库、不改广告。

### TASK-069 完成

- 新增 [smoke_frontend_ad_draft_attribution_review_entry.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_ad_draft_attribution_review_entry.py)，先红灯确认缺少归因核查入口，再实现转绿。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增：
  - `openAdDraftAttributionReview`
  - `adDraftAttributionReviewProduct`
- 产品身份待核查区域新增“去广告归因页核查 / 修正归因”按钮，点击后关闭目标 / 规则抽屉并切到“广告归因”页。
- 跳转会带入首个候选销售表现产品到 `attributionProductId`，并在广告归因页顶部展示“从草稿产品身份核查进入”横幅。
- 浏览器复核确认横幅展示候选产品 `RBK004-RBK004-2 天蓝 / ASIN B016EXMW4S / MSKU 004-Wayfarer Blue`。
- 横幅文案说明只用于人工核查归因，不会自动保存归因规则，也不会改广告。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_ad_draft_attribution_review_entry.py
python scripts\smoke_frontend_ad_draft_product_identity.py
python scripts\smoke_frontend_product_goal_rule_drawer.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：保存目标前先分流到归因核查，能降低把目标配置到草稿对象上的风险；归因页顶部横幅保留了候选产品上下文。
- Product Design 复核发现的新问题：归因页已经带入候选销售产品，但还没有自动定位对应广告来源证据行，运营仍需自己在候选列表里找来源。
- 下一步建议：`TASK-070：草稿产品归因核查的广告来源候选定位`。
### TASK-070 启动

- 本轮任务：草稿产品归因核查的广告来源候选定位。
- 边界：只做前端证据定位和人工核查引导，不自动保存归因规则、不写真实库、不改广告。
- 触发原因：TASK-069 已能带入候选销售表现产品，但归因页仍未自动定位对应广告来源候选。

### TASK-070 完成

- 新增 [smoke_frontend_ad_draft_attribution_source_focus.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_ad_draft_attribution_source_focus.py)，先红灯确认缺少广告来源候选定位，再实现转绿。
- 更新 [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx)，新增：
  - `adDraftAttributionReviewSourceProduct`
  - `sourceMatchesProductIdentity`
- 从草稿产品身份核查进入广告归因页时，页面会同时保留“草稿广告来源线索”和“候选销售表现产品”。
- 归因页新增“建议优先核查的广告来源”区块，展示候选销售表现产品、广告来源证据行和“查看该广告来源证据”入口。
- 高可信归因候选表和未归因广告数据池会对匹配草稿广告来源线索的广告来源行做视觉标记。
- 浏览器复核确认定位区展示候选销售产品 `RBK004-RBK004-2 天蓝 / ASIN B016EXMW4S / MSKU 004-Wayfarer Blue`，并定位广告来源 `RBK004-Auto`。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_ad_draft_attribution_source_focus.py
python scripts\smoke_frontend_ad_draft_attribution_review_entry.py
python scripts\smoke_frontend_product_attribution_candidates.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核和项目级深度设计。
- Product Design 复核结论：双对象表达更清楚，草稿广告来源线索负责定位广告来源，候选销售表现产品负责人工确认目标，降低了把销售产品和广告来源混成一个对象的风险。
- Product Design 深度设计结论：产品中心应区分“全部销售产品、已投放 / 有 SP 数据产品、未投放 / 无 SP 数据产品”；未投放产品不应被催促设置广告调优目标。
- 下一步建议：`TASK-071：产品中心广告覆盖状态分层`。
