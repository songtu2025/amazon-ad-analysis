# 开发任务清单

## 执行原则

- 每次只执行一个任务。
- 任务必须有明确目标、允许文件、禁止文件、验收命令和成功标准。
- 验收通过前不能进入下一任务。
- 本系统只做决策辅助，不做广告自动执行。

## TASK-081：产品中心已配置未触发异常状态可视化

状态：已完成。

背景：
- 用户已在前端为真实广告调优对象保存产品目标和目标 ACOS。
- TASK-080 已确认真实规则能被读取：`goal_type=profit`、`target_acos=0.3`。
- 当前真实 ACOS `27.57%` 未高于目标 ACOS `30%`，所以不进入异常队列是正确结果。
- 但产品中心如果只显示“匹配”，运营仍可能误解为空队列或系统无效。

目标：
- 在产品中心广告调优对象中，把“已配置目标 / 当前未触发异常”展示清楚。
- 明确展示当前 ACOS 未高于目标 ACOS。
- 保留缺目标 / 规则时的人工设置引导优先级。
- 不改后端、不写真实库、不新增任何广告自动执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_configured_no_anomaly_state.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_configured_no_anomaly_state.py
python scripts\smoke_frontend_real_goal_rule_gate_guidance.py
python scripts\smoke_frontend_product_goal_rule_guidance.py
python scripts\smoke_real_goal_rule_queue_gate.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 前端存在“已配置目标 / 当前未触发异常”的独立展示逻辑。
- 该状态只在产品目标、规则、SP 指标都存在且 `target_match.status === "matched"` 时出现。
- 缺目标 / 规则时仍优先显示人工设置引导。
- 页面文案说明“ACOS 未高于目标 ACOS”，不制造异常。
- 源码和脚本不出现任何自动执行广告动作字段或按钮。

完成依据：
- 新增 [smoke_frontend_configured_no_anomaly_state.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_configured_no_anomaly_state.py)，先红灯确认产品中心缺少“已配置未触发异常”状态，再实现转绿。
- [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 新增 `isConfiguredNoAnomalyTargetMatch`。
- 产品中心“目标与数据表现是否匹配”列新增“目标已配置 / 当前未触发异常”状态。
- 该状态只在产品目标、规则、目标 ACOS、SP 指标都存在，且 `target_match.status === "matched"` 时出现。
- 缺目标 / 规则时仍优先显示“目标 / 规则待设置”。
- 当前真实门禁仍返回 `workflow_status=no_anomaly_under_current_rule`、`queue_ready=false`，说明不入队来自真实规则判断。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_configured_no_anomaly_state.py
python scripts\smoke_frontend_real_goal_rule_gate_guidance.py
python scripts\smoke_frontend_product_goal_rule_guidance.py
python scripts\smoke_real_goal_rule_queue_gate.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：把“空队列”的原因放回产品行里，能让运营知道这是目标规则生效后的正常结果，不是系统没跑起来。
- Product Design 复核发现：下一步可以把异常队列空态也改成同一口径，显示“当前所有已配置对象均未越线”。
- 下一步建议：`TASK-082：异常队列空态原因可解释化`。

## TASK-080：真实目标规则保存后异常建议入队验收

状态：已完成。

背景：
- 用户已在前端为真实广告调优对象保存产品目标和目标 ACOS。
- 当前真实对象为 `RBK004 kids sunglasses / MSKU RBK004 / product_id=2`。
- 第一版必须尊重人工目标规则：如果当前指标未触发规则，应明确不入队；不能为了看到异常而改规则或制造异常。

目标：
- 只读验证真实 `data/app.db` 中人工目标和规则已经保存。
- 按真实规则判断当前对象是否应该进入异常队列。
- 如果当前 ACOS 高于目标 ACOS，则继续验收真实异常和 AI 建议是否可入队。
- 如果当前 ACOS 未高于目标 ACOS，则明确输出“不应入队”，并确认异常 / 建议主流程不出现误报。
- 不改真实规则、不写真实库、不新增任何广告自动执行能力。

允许修改：
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `frontend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_real_goal_rule_queue_gate.py
python scripts\smoke_real_no_demo_anomaly_objects.py
python scripts\smoke_real_e2e.py
```

成功标准：
- smoke 只读真实库，不写 `data/app.db`。
- smoke 能读取真实人工 `product_goal` 和 `target_acos`。
- 如果真实指标触发规则，应输出 `ready_for_anomaly_queue`。
- 如果真实指标未触发规则，应输出 `no_anomaly_under_current_rule`，且明确不应入队。
- 真实异常 / 建议主流程不出现 DEMO / SMOKE / TASK 对象。
- 源码和脚本不出现任何自动执行广告动作字段。

完成依据：
- 用户已在前端人工保存目标规则。
- `python scripts\smoke_real_goal_rule_queue_gate.py` 已只读读取真实目标规则：`goal_type=profit`、`target_acos=0.3`、`min_clicks=0`、`min_spend=0.0`。
- 当前真实广告指标：点击 `138`、花费 `150.47`、订单 `52`、销售额 `545.79`、ACOS `27.57%`。
- 当前真实 ACOS 未高于人工目标 ACOS `30%`，因此输出 `workflow_status=no_anomaly_under_current_rule`、`queue_ready=false`。
- 该结果说明真实规则已生效，且当前不应生成异常或 AI 建议，不属于系统无数据。
- `python scripts\smoke_real_no_demo_anomaly_objects.py` 返回真实异常数 `0`、建议数 `0`，且未出现 DEMO / SMOKE / TASK 对象。
- `python scripts\smoke_real_e2e.py` 通过。
- 验收命令已通过：

```powershell
python scripts\smoke_real_goal_rule_queue_gate.py
python scripts\smoke_real_no_demo_anomaly_objects.py
python scripts\smoke_real_e2e.py
```

- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、前端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：本轮“不入队”是正确的日常运营反馈，说明系统能按人工目标判断当前广告表现未越线。
- Product Design 复核发现：后续可在前端补一个“已配置目标 / 当前未触发异常”的可视状态，避免运营把空队列误读成系统无效。
- 下一步建议：`TASK-081：产品中心已配置未触发异常状态可视化`。

## TASK-079：前端人工目标规则设置引导收敛

状态：已完成。

背景：
- TASK-078 已确认真实队列为空的原因是 `RBK004 kids sunglasses / MSKU RBK004 / product_id=2` 缺少人工 `product_goal` 和 `target_acos`。
- 当前前端虽然有“设置目标 / 规则”抽屉，但运营需要从页面里明确知道：真实异常队列为空是因为缺目标 / 规则，而不是没有广告数据。
- AI 不能自动生成或保存真实目标 / 规则。

目标：
- 产品中心广告调优对象视图里，新增真实目标 / 规则缺口引导。
- 引导显示真实对象身份、当前 SP 指标、缺失项和下一步人工动作。
- 引导入口复用现有“设置目标 / 规则”抽屉，不自动填值、不自动保存。
- 文案明确：只引导运营人工设置，不会自动修改广告。
- 不改后端、不写真实库、不新增任何广告自动执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_real_goal_rule_gate_guidance.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_real_goal_rule_gate_guidance.py
python scripts\smoke_frontend_product_goal_rule_drawer.py
python scripts\smoke_real_goal_rule_queue_gate.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 产品中心源码存在真实目标 / 规则缺口引导逻辑。
- 引导只针对广告调优对象且缺 `goal` 或 `rules.target_acos` 的产品。
- 引导展示“需要人工设置产品目标 / 规则”“缺少产品目标”“缺少目标 ACOS”等明确文案。
- 引导展示当前 SP 指标，并提供“设置目标 / 规则”入口。
- 设置入口复用现有抽屉，不自动保存目标 / 规则。
- 页面源码不出现任何自动执行广告动作字段或按钮。

完成依据：
- 先运行 `python scripts\smoke_frontend_real_goal_rule_gate_guidance.py`，红灯确认验收脚本不存在。
- 新增 [smoke_frontend_real_goal_rule_gate_guidance.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_real_goal_rule_gate_guidance.py)。
- [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 新增 `getProductGoalRuleMissingItems`，明确展示 `缺少产品目标` 和 `缺少目标 ACOS`。
- `needsProductGoalRuleSetup` 已收敛为：必须具备广告调优资格、存在 SP 指标、且缺少人工目标或目标 ACOS。
- `productsNeedingGoalRuleSetup` 只在 `广告调优对象` 视图中从当前 `productCenterProducts` 表格范围筛选，不再从全量销售表现产品中提示。
- 引导卡片展示“需要人工设置产品目标 / 规则”、当前 SP 指标、缺失项标签和“设置目标 / 规则”入口。
- 文案明确只引导运营人工设置，不会自动保存目标 / 规则，也不会自动修改广告。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_real_goal_rule_gate_guidance.py
python scripts\smoke_frontend_product_goal_rule_drawer.py
python scripts\smoke_real_goal_rule_queue_gate.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：目标 / 规则缺口现在只暴露给广告调优对象，销售表现同步来的普通产品继续作为销售档案，不会被催促设置广告规则。
- Product Design 复核发现：下一步应由运营为 `RBK004 kids sunglasses / MSKU RBK004` 人工保存目标和目标 ACOS 后，再验收真实异常与 AI 建议是否入队。
- 下一步建议：`TASK-080：真实目标规则保存后异常建议入队验收`。

## TASK-078：真实目标规则保存后的异常建议入队门禁

状态：已完成。

背景：
- TASK-077 已证明：复制真实库到临时库后，真实广告对象在有人工目标 / 规则时可以生成 `acos_worse` 异常和可溯源 AI 建议。
- 当前真实主流程仍为空，不是因为广告数据缺失，而是因为真实库里的广告调优对象尚未设置人工目标 / 规则。
- AI 不能替运营设置真实产品目标或规则门槛。

目标：
- 新增只读 smoke，检查真实 `data/app.db` 中广告调优对象是否已具备人工目标 / 规则。
- 如果目标 / 规则缺失，输出明确 `workflow_status=needs_manual_goal_rule`，并给出需要运营在前端设置的产品对象。
- 如果目标 / 规则已存在，则只读判断当前指标是否满足异常入队条件；本任务不写真实库。
- 确认真实队列为空时的原因可解释，不再把“无异常”误读成“系统不可用”。
- 不修改后端、前端、真实数据库，不新增任何广告自动执行能力。

允许修改：
- `scripts/smoke_real_goal_rule_queue_gate.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `frontend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_real_goal_rule_queue_gate.py
python scripts\smoke_real_no_demo_anomaly_objects.py
python scripts\smoke_real_e2e.py
```

成功标准：
- smoke 只读真实库，不写 `data/app.db`。
- smoke 能找到真实广告调优对象。
- 当前缺人工目标 / 规则时，输出 `workflow_status=needs_manual_goal_rule` 和产品身份。
- 如未来已有人工目标 / 规则，smoke 能输出当前是否会触发异常入队，而不是擅自改规则。
- smoke 不包含任何自动执行广告动作字段。

完成依据：
- 先运行 `python scripts\smoke_real_goal_rule_queue_gate.py`，红灯确认验收脚本不存在。
- 新增 [smoke_real_goal_rule_queue_gate.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_goal_rule_queue_gate.py)。
- 脚本使用 SQLite 只读 URI 打开真实 `data/app.db`，并通过修改时间确认未写真实库。
- 真实库当前找到 1 个广告调优对象：`RBK004 kids sunglasses / MSKU RBK004 / product_id=2`，活跃归因规则数为 1。
- 当前真实广告指标：点击 138、花费 150.47、订单 52、销售额 545.79、ACOS 27.57%。
- 当前门禁输出 `workflow_status=needs_manual_goal_rule`、`queue_ready=false`。
- 当前缺口为 `product_goal` 和 `target_acos`，需要运营在前端产品中心人工保存产品目标和目标 ACOS 等规则门槛。
- 验收命令已通过：

```powershell
python scripts\smoke_real_goal_rule_queue_gate.py
python scripts\smoke_real_no_demo_anomaly_objects.py
python scripts\smoke_real_e2e.py
```

- `python scripts\smoke_real_no_demo_anomaly_objects.py` 仍返回真实异常数和建议数为 0，符合“缺人工目标 / 规则，不应入队”的当前状态。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、前端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：门禁把“当前队列为空”翻译成可执行前置任务，即为 RBK004 广告调优对象补产品目标和目标 ACOS，而不是让运营猜系统哪里坏了。
- Product Design 复核发现：下一步应把该前置缺口做进前端引导，让运营从真实页面直接完成人工目标 / 规则设置。
- 下一步建议：`TASK-079：前端人工目标规则设置引导收敛`。

## TASK-077：真实广告调优对象目标规则后的异常建议链路验收

状态：已完成。

背景：
- TASK-076 已把产品中心对象边界固化到 API：销售表现产品可以保留为档案，但默认不进入广告调优。
- MVP 主链路还需要证明：真实广告调优对象在有人工目标 / 规则后，可以生成真实异常，并生成可溯源 AI 建议。
- 第一版不能让 AI 替运营写真实目标 / 规则，因此本任务不得直接修改真实 `data/app.db`。

目标：
- 新增 smoke，复制真实 `data/app.db` 到临时库。
- 在真实库只读检查当前是否已有广告调优对象、人工目标和规则。
- 如真实库缺少人工目标 / 规则，在临时库模拟“人工已设置目标 / 规则”，只用于链路验收。
- 在临时库运行 `acos_worse` 异常生成和 AI 建议生成。
- 验证生成的异常不是 DEMO / SMOKE / TASK 对象。
- 验证 AI 建议包含 `source_trace_json`、`evidence_summary`、`recommended_manual_decision`。
- 确认真实 `data/app.db` 未被写入。
- 不新增任何广告自动执行能力。

允许修改：
- `scripts/smoke_real_ad_object_anomaly_chain.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `frontend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_real_ad_object_anomaly_chain.py
python scripts\smoke_real_no_demo_anomaly_objects.py
python scripts\smoke_real_e2e.py
```

成功标准：
- smoke 使用真实 `data/app.db` 的拷贝库执行，不写真实库。
- smoke 能找到至少一个真实广告调优对象或如实输出前置缺口。
- 临时库中能基于真实 SP 指标和人工目标 / 规则生成至少一个真实异常。
- 临时库中能为该异常生成至少一个 AI 建议。
- 异常和建议均不包含 DEMO / SMOKE / TASK 测试对象。
- AI 建议可追溯到规则结果和证据快照。
- 源码不出现任何自动执行广告动作字段。

完成依据：
- 先运行 `python scripts\smoke_real_ad_object_anomaly_chain.py`，红灯确认验收脚本不存在。
- 新增 [smoke_real_ad_object_anomaly_chain.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_real_ad_object_anomaly_chain.py)。
- 脚本复制真实 `data/app.db` 到系统临时目录 `%TEMP%\amazon_ad_analysis_task077\app_task077.db`，所有目标 / 规则写入和异常建议生成都只发生在临时库。
- 首次使用项目 `tmp/` 目录时出现 SQLite `disk I/O error`；已定位为项目 `tmp/` 目录不适合当前环境 SQLite 写事务，改用 `%TEMP%` 后通过。
- 真实库当前存在活跃归因广告对象：`RBK004 kids sunglasses / MSKU RBK004 / product_id=2`。
- 真实库当前缺少该产品的人工目标和规则：`real_had_goal=false`、`real_had_rule=false`。
- 临时库模拟人工目标 / 规则：`goal_type=profit`、`target_acos=0.1378`。
- 临时库基于真实 SP 指标生成 `acos_worse` 异常：点击 138、花费 150.47、订单 52、销售额 545.79、ACOS 27.57%。
- 临时库为该异常生成 AI 建议，包含 `source_trace_json`、`evidence_summary`、`recommended_manual_decision=adopt`。
- 脚本确认真实 `data/app.db` 修改时间未变化。
- 验收命令已通过：

```powershell
python scripts\smoke_real_ad_object_anomaly_chain.py
python scripts\smoke_real_no_demo_anomaly_objects.py
python scripts\smoke_real_e2e.py
```

- `python scripts\smoke_real_no_demo_anomaly_objects.py` 仍返回真实主流程异常数和建议数为 0，说明临时链路验收没有污染真实队列。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、前端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：真实队列为空的原因现在被清楚定位为“运营尚未人工设置目标 / 规则”，而不是数据链路或 AI 建议链路不可用；后续应把真实广告调优对象的人工目标 / 规则设置引导接入日常工作流。
- Product Design 复核发现：下一步应验证运营在前端保存真实目标 / 规则后，真实队列能从空态进入异常与建议待确认状态。
- 下一步建议：`TASK-078：真实目标 / 规则保存后的异常建议入队验收`。

## TASK-076：产品中心广告调优对象 API 状态固化

状态：已完成。

背景：
- 销售表现接口同步的是销售产品池，不等于 SP 广告投放产品池。
- TASK-071 到 TASK-075 已在前端把产品中心拆成“广告调优对象 / 销售档案 / 全部产品”。
- 当前对象边界主要由前端根据归因数量和 SP 指标临时计算，后续页面或脚本仍可能误把销售档案产品当作广告调优对象。

目标：
- `/api/products` 和 `/api/products/{product_id}` 直接返回产品广告覆盖状态。
- 状态只做派生判断，不新增表、不写真实库：
  - `attributed`：有活跃人工归因规则。
  - `sp_unattributed`：无活跃归因规则，但当前周期有 SP 指标。
  - `not_advertised`：当前系统暂无 SP 覆盖证据，只作为销售档案。
- API 同时返回 `is_ad_tuning_eligible`，只有 `attributed` 和 `sp_unattributed` 为真。
- 前端优先使用 API 字段，保留现有本地计算作为兼容兜底。
- 不新增任何广告自动执行能力。

允许修改：
- `backend/app/api/routes/products.py`
- `frontend/src/api.ts`
- `frontend/src/App.tsx`
- `scripts/smoke_product_ad_object_scope_api.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_product_ad_object_scope_api.py
python scripts\smoke_frontend_product_center_view_scope.py
python scripts\smoke_frontend_product_ad_coverage_status.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 产品 API 返回 `ad_coverage_status` 和 `is_ad_tuning_eligible`。
- 有活跃归因规则的产品返回 `attributed` 且可进入广告调优。
- 无归因但有 SP 指标的产品返回 `sp_unattributed` 且可进入广告调优。
- 无归因且无 SP 指标的销售产品返回 `not_advertised` 且不可进入广告调优。
- 前端 `getProductAdCoverageStatus` 优先使用 API 的 `ad_coverage_status`。
- 前端 `isProductAdTuningEligible` 优先使用 API 的 `is_ad_tuning_eligible`。
- 前端仍保留本地计算兜底，避免旧接口响应导致页面崩溃。
- 源码不出现任何自动执行广告动作字段或按钮。

完成依据：
- 新增 [smoke_product_ad_object_scope_api.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_product_ad_object_scope_api.py)，先红灯确认产品 API 缺少 `ad_coverage_status`，再实现转绿。
- [products.py](D:/DataProject/coedx_project/Amazon广告分析/backend/app/api/routes/products.py) 新增 API 派生字段 `ad_coverage_status` 和 `is_ad_tuning_eligible`。
- API 状态规则已固化：有活跃人工归因规则为 `attributed`；无归因但有当前周期 SP 指标为 `sp_unattributed`；无归因且无 SP 指标为 `not_advertised`。
- [api.ts](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/api.ts) 的 `Product` 类型新增 `ad_coverage_status` 和 `is_ad_tuning_eligible`。
- [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 的 `getProductAdCoverageStatus` 和 `isProductAdTuningEligible` 优先使用 API 字段，保留本地计算兜底。
- 真实本地 API `/api/products?market_id=1` 已确认销售表现产品返回 `ad_coverage_status=not_advertised`、`is_ad_tuning_eligible=false`。
- 已重启本地开发服务：后端 `http://127.0.0.1:8001`，前端 `http://127.0.0.1:5173`。
- 验收命令已通过：

```powershell
python scripts\smoke_product_ad_object_scope_api.py
python scripts\smoke_frontend_product_center_view_scope.py
python scripts\smoke_frontend_product_ad_coverage_status.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- 重启后已再次通过 `python scripts\smoke_real_e2e.py`。
- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：产品中心对象边界已从“前端猜测”提升为“API 明确返回”，更利于后续搜索词分析、异常队列和复盘页面复用同一口径；销售表现产品可以保留为档案，但默认不会进入广告调优。
- Product Design 复核发现：下一步可以回到真实广告调优对象目标 / 规则后的异常生成验收。
- 下一步建议：`TASK-077：真实广告调优对象目标规则后的异常生成验收`。

## TASK-075：产品中心筛选层级继续减负

状态：已完成。

背景：
- TASK-074 已新增“广告调优对象 / 销售档案 / 全部产品”视图。
- Product Design 复核发现：“产品中心视图”和“广告覆盖状态”两个筛选仍有认知重叠。
- 在默认“广告调优对象”和“销售档案”视图里继续展示广告覆盖状态筛选，会让运营困惑筛选条件之间的关系。

目标：
- 只在“全部产品”视图中展示“广告覆盖状态”筛选。
- 默认“广告调优对象”视图不展示广告覆盖状态筛选，避免出现和视图范围冲突的选项。
- “销售档案”视图不展示广告覆盖状态筛选，因为该视图已经等价于“本系统暂无 SP 覆盖证据”范围。
- 保留产品广告覆盖状态汇总卡，继续说明销售表现产品不等于广告投放产品。
- 不写真实库，不改后端，不新增任何广告自动执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_product_center_filter_reduction.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_product_center_filter_reduction.py
python scripts\smoke_frontend_product_center_view_scope.py
python scripts\smoke_frontend_product_ad_coverage_filter.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 前端源码存在 `shouldShowProductAdCoverageFilter`。
- `shouldShowProductAdCoverageFilter` 只在 `productCenterView === "all"` 时为真。
- “广告覆盖状态”筛选控件只在 `shouldShowProductAdCoverageFilter` 为真时渲染。
- 切换产品中心视图仍会把广告覆盖状态筛选重置为 `all`。
- 产品广告覆盖状态汇总卡仍保留。
- 前端源码不出现任何自动执行广告动作字段或按钮。

完成依据：
- 新增 [smoke_frontend_product_center_filter_reduction.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_center_filter_reduction.py)，先红灯确认缺少筛选层级减负逻辑，再实现转绿。
- [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 新增 `shouldShowProductAdCoverageFilter`，仅在 `productCenterView === "all"` 时展示广告覆盖状态筛选。
- 默认“广告调优对象”视图不再展示广告覆盖状态筛选，避免出现和视图范围冲突的选项。
- “销售档案”视图不再展示广告覆盖状态筛选，因为该视图已等价于“本系统暂无 SP 覆盖证据”范围。
- 产品广告覆盖状态汇总卡继续保留，仍说明销售表现产品不等于广告投放产品。
- 浏览器复核确认默认“广告调优对象”视图 `coverageControlCount=0`；切到“全部产品”后 `coverageControlCount=1` 且显示“全部覆盖状态”。
- 浏览器复核确认从“全部产品”切回“广告调优对象”后 `coverageControlCount=0`，且“全部覆盖状态”不再显示。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_center_filter_reduction.py
python scripts\smoke_frontend_product_center_view_scope.py
python scripts\smoke_frontend_product_ad_coverage_filter.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：筛选层级更轻，默认视图只剩“产品中心视图”这个主决策，不再让运营在广告调优对象里看到互相冲突的覆盖状态筛选。
- Product Design 复核发现：产品中心筛选已基本收敛，下一步应回到真实广告对象目标 / 规则设置后的异常生成和 AI 建议主链路验收。
- 下一步建议：`TASK-076：真实广告调优对象目标规则后的异常生成验收`。

## TASK-074：产品中心广告调优对象与销售档案视图收敛

状态：已完成。

背景：
- TASK-073 已让“本系统暂无 SP 覆盖证据”的产品不再显示广告目标 / 规则输入框。
- Product Design 复核发现：只读说明仍在宽表多列重复出现，运营日常仍需要从大量销售档案产品里找真正需要广告动作判断的对象。
- 当前更重要的是收敛产品中心对象视图，不是新增后端判断。

目标：
- 产品中心新增“产品中心视图”切换，默认展示“广告调优对象”。
- “广告调优对象”只展示有广告覆盖或有 SP 数据待归因的产品。
- “销售档案”只展示“本系统暂无 SP 覆盖证据”的产品，并隐藏广告目标 / 规则相关列。
- 保留“全部产品”视图，方便运营完整查找销售表现产品。
- 切换视图时重置广告覆盖状态筛选，避免筛选条件互相打架。
- 不写真实库，不改后端，不新增任何广告自动执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_product_center_view_scope.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_product_center_view_scope.py
python scripts\smoke_frontend_product_no_sp_readonly_rules.py
python scripts\smoke_frontend_product_ad_coverage_filter.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 前端源码存在 `ProductCenterView`、`productCenterViewOptions` 和默认 `ad_tuning` 视图。
- 产品中心页面存在“产品中心视图”控件，包含“广告调优对象”“销售档案”“全部产品”。
- `ad_tuning` 视图过滤掉 `not_advertised` 产品。
- `sales_profile` 视图只展示 `not_advertised` 产品。
- 销售档案视图隐藏广告目标 / 规则列，避免重复只读说明刷屏。
- 切换视图时会把广告覆盖状态筛选重置为 `all`。
- 前端源码不出现任何自动执行广告动作字段或按钮。

完成依据：
- 新增 [smoke_frontend_product_center_view_scope.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_center_view_scope.py)，先红灯确认产品中心缺少广告调优对象 / 销售档案视图，再实现转绿。
- [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 新增 `ProductCenterView`、`productCenterViewOptions` 和 `productMatchesProductCenterView`。
- 产品中心默认视图为“广告调优对象”，仅展示有广告覆盖或有 SP 数据待归因的产品。
- “销售档案”视图仅展示“本系统暂无 SP 覆盖证据”的产品。
- “销售档案”视图隐藏广告目标 / 规则相关列，避免重复只读说明刷屏。
- 切换产品中心视图时会重置广告覆盖状态筛选为“全部覆盖状态”。
- 浏览器复核确认默认“广告调优对象”表格只有 1 条 RBK004 广告对象，表格行不包含“本系统暂无 SP 覆盖证据”。
- 浏览器复核确认“销售档案”视图首屏显示 CHG001 等销售表现产品，且表头不再包含广告目标 / 规则相关列。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_center_view_scope.py
python scripts\smoke_frontend_product_no_sp_readonly_rules.py
python scripts\smoke_frontend_product_ad_coverage_filter.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：默认进入广告调优对象视图能把运营注意力聚焦到真正需要广告判断的产品，销售档案视图隐藏广告规则列后不再用重复只读说明制造噪音。
- Product Design 复核发现的新问题：“产品中心视图”和“广告覆盖状态”两个筛选仍有一定认知重叠，后续可继续合并或弱化覆盖状态筛选。
- 下一步建议：`TASK-075：产品中心筛选层级继续减负`。

## TASK-073：无 SP 覆盖产品目标字段只读收敛

状态：已完成。

背景：
- TASK-072 已把无 SP 覆盖产品标记为“本系统暂无 SP 覆盖证据”，并支持按广告覆盖状态筛选。
- Product Design 复核发现：这类产品虽然不再展示“管理归因”和“设置目标 / 规则”主操作，但行内仍能看到产品目标和规则字段输入框。
- 这会让运营误以为销售档案产品也需要配置广告调优目标。

目标：
- 对“本系统暂无 SP 覆盖证据”的产品，产品目标和广告规则字段改为只读展示。
- 只读文案明确说明：只读销售档案，不配置广告目标 / 规则。
- 保留有广告覆盖或有 SP 数据待归因产品的人工目标 / 规则编辑能力。
- 不写真实库，不改后端，不新增任何广告自动执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_product_no_sp_readonly_rules.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_product_no_sp_readonly_rules.py
python scripts\smoke_frontend_product_ad_coverage_filter.py
python scripts\smoke_frontend_product_goal_rule_guidance.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 前端源码存在无 SP 覆盖产品广告目标 / 规则只读渲染逻辑。
- 产品目标、目标 ACOS、目标 CVR、最小点击、最小花费、最小订单、最大 CPC、库存阈值、备注这些广告目标 / 规则列对无 SP 覆盖产品不再渲染可编辑控件。
- 只读单元格文案包含“只读销售档案”和“不配置广告目标 / 规则”。
- 有广告覆盖或有 SP 数据待归因产品仍保留人工设置目标 / 规则能力。
- 前端源码不出现任何自动执行广告动作字段或按钮。

完成依据：
- 新增 [smoke_frontend_product_no_sp_readonly_rules.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_no_sp_readonly_rules.py)，先红灯确认无 SP 覆盖产品仍缺少只读规则保护，再实现转绿。
- [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 新增 `isProductAdRuleEditable` 和 `renderNoSpCoverageAdRuleReadonly`。
- 产品目标、目标 ACOS、目标 CVR、最小点击、最小花费、最小订单、最大 CPC、库存阈值、备注这些广告目标 / 规则列对无 SP 覆盖产品改为只读展示。
- “本系统暂无 SP 覆盖证据”的销售档案产品显示“只读销售档案”和“本系统暂无 SP 覆盖证据，不配置广告目标 / 规则”。
- 有广告覆盖或有 SP 数据待归因产品仍保留人工目标 / 规则编辑能力。
- 浏览器 DOM 复核确认真实产品中心首屏无 SP 覆盖产品显示只读规则文案。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_no_sp_readonly_rules.py
python scripts\smoke_frontend_product_ad_coverage_filter.py
python scripts\smoke_frontend_product_goal_rule_guidance.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：无 SP 覆盖产品现在被表达为只读销售档案，能避免运营把销售表现产品误当成广告调优对象。
- Product Design 复核发现的新问题：同一句只读说明在多列重复出现会增加横向表格噪音，后续应把产品中心收敛为“广告调优对象 / 销售档案”两个更轻视图。
- 下一步建议：`TASK-074：产品中心广告调优对象与销售档案视图收敛`。

## TASK-072：产品中心广告覆盖筛选与术语精修

状态：已完成。

背景：
- TASK-071 已让产品中心区分“有广告覆盖 / 有 SP 数据待归因 / 未投放或无 SP 数据”。
- Product Design 复核发现：后续最好把广告覆盖状态做成可筛选视图。
- “未投放 / 无 SP 数据”容易被理解成业务上确认未投放，但当前系统只能证明“本系统暂无 SP 覆盖证据”。

目标：
- 产品中心新增广告覆盖状态筛选。
- 筛选项包含全部、有广告覆盖、有 SP 数据待归因、本系统暂无 SP 覆盖证据。
- 将“未投放 / 无 SP 数据”的前端主文案收敛为“本系统暂无 SP 覆盖证据”。
- 文案继续说明：这类产品只作为销售产品档案，不进入广告调优待办。
- 不写真实库，不改后端，不新增任何广告自动执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_product_ad_coverage_filter.py`
- `scripts/smoke_frontend_product_ad_coverage_status.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_product_ad_coverage_filter.py
python scripts\smoke_frontend_product_ad_coverage_status.py
python scripts\smoke_frontend_product_goal_rule_guidance.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 前端源码存在广告覆盖状态筛选状态和筛选选项。
- 产品中心页面存在“广告覆盖状态”筛选控件。
- 筛选逻辑会按 `ProductAdCoverageStatus` 过滤 `productCenterProducts`。
- 主文案使用“本系统暂无 SP 覆盖证据”，避免把系统证据不足写成业务确认未投放。
- 该类产品仍明确说明只作为销售产品档案，不进入广告调优待办。
- 前端源码不出现任何自动执行广告动作字段或按钮。

完成依据：
- 新增 [smoke_frontend_product_ad_coverage_filter.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_ad_coverage_filter.py)，先红灯确认缺少广告覆盖状态筛选，再实现转绿。
- [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 新增 `ProductAdCoverageFilter`、`productAdCoverageFilterOptions`、`productAdCoverageFilter` 和 `setProductAdCoverageFilter`。
- 产品中心新增“广告覆盖状态”筛选控件，默认“全部覆盖状态”，可按“有广告覆盖”“有 SP 数据待归因”“本系统暂无 SP 覆盖证据”过滤产品列表。
- `productCenterProducts` 会按 `ProductAdCoverageStatus` 过滤后再排序。
- 前端主文案从“未投放 / 无 SP 数据”收敛为“本系统暂无 SP 覆盖证据”，避免把系统证据不足写成业务已确认未投放。
- “本系统暂无 SP 覆盖证据”的产品继续显示“只作为销售产品档案，不进入广告调优待办”。
- 浏览器 DOM 复核确认产品中心可见“广告覆盖状态 / 全部覆盖状态”筛选控件，首屏销售产品行显示“本系统暂无 SP 覆盖证据”和“只作为销售产品档案，不进入广告调优待办”。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_ad_coverage_filter.py
python scripts\smoke_frontend_product_ad_coverage_status.py
python scripts\smoke_frontend_product_goal_rule_guidance.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：筛选位置合适，放在覆盖状态汇总和表格之间；术语改成“本系统暂无 SP 覆盖证据”更准确，降低了误解为业务已确认未投放的风险。
- Product Design 复核发现的新问题：“本系统暂无 SP 覆盖证据”的产品行仍可看到产品目标和规则字段输入框，后续应单独收敛为只读展示，避免运营误以为需要配置广告调优目标。
- 下一步建议：`TASK-073：无 SP 覆盖产品目标字段只读收敛`。

## TASK-071：产品中心广告覆盖状态分层

状态：已完成。

背景：
- 销售表现接口同步的是销售产品池，不等于 SP 广告投放产品池。
- 当前产品中心把所有未绑定产品都显示为“待归因”，会把没有 SP 广告数据的销售产品误导成广告调优对象。
- 第一版不判断产品是否应该投广告，只按本系统已有证据做展示分层。

目标：
- 产品中心展示“产品广告覆盖状态”汇总。
- 按人工归因规则和 SP 指标区分“有广告覆盖”“有 SP 数据待归因”“未投放 / 无 SP 数据”。
- 未投放 / 无 SP 数据产品只作为销售产品档案展示，不进入广告调优待办。
- 未投放 / 无 SP 数据产品不展示“管理归因”和“设置目标 / 规则”主操作。
- 不写真实库，不改后端，不新增任何广告自动执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_product_ad_coverage_status.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_product_ad_coverage_status.py
python scripts\smoke_frontend_product_goal_rule_guidance.py
python scripts\smoke_frontend_product_center_attribution_status.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 产品中心源码存在广告覆盖状态判断逻辑。
- 页面存在“产品广告覆盖状态”汇总，并说明销售表现产品不等于广告投放产品。
- 未投放 / 无 SP 数据产品显示为“未投放 / 无 SP 数据”。
- 未投放产品文案明确说明“仅作为销售产品档案，不进入广告调优待办”。
- 未投放 / 无 SP 数据产品不显示可点击的“管理归因”和“设置目标 / 规则”主操作。
- 前端源码不出现任何自动执行广告动作字段或按钮。

完成依据：
- 新增 [smoke_frontend_product_ad_coverage_status.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_ad_coverage_status.py)，先红灯确认产品中心缺少广告覆盖状态分层，再实现转绿。
- [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 新增 `ProductAdCoverageStatus`、`getProductAdCoverageStatus`、`isProductAdTuningEligible` 和 `getProductAdCoveragePriority`。
- 产品中心新增“产品广告覆盖状态”汇总，说明销售表现产品不等于广告投放产品。
- 产品中心按人工归因规则和 SP 指标展示“有广告覆盖”“有 SP 数据待归因”“未投放 / 无 SP 数据”三类状态。
- 未投放 / 无 SP 数据产品显示“无需归因”和“仅销售档案”，不再提供“管理归因”和“设置目标 / 规则”主操作。
- 浏览器复核确认当前真实页面显示“有广告覆盖 1 个”“有 SP 数据待归因 0 个”“未投放 / 无 SP 数据 217 个”。
- 浏览器复核确认 CHG001 等销售表现产品行显示“未投放 / 无 SP 数据”“无需归因”“仅销售档案”和“不进入广告调优待办”。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_ad_coverage_status.py
python scripts\smoke_frontend_product_goal_rule_guidance.py
python scripts\smoke_frontend_product_center_attribution_status.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：分层解决了销售产品池和广告投放产品池混淆的问题，未覆盖产品不会再被误推入广告调优待办。
- Product Design 深度设计结论：后续可增加广告覆盖状态筛选，并把“未投放 / 无 SP 数据”的解释进一步收敛为“本系统暂无 SP 覆盖证据”，避免和销售表现里的广告辅助字段混淆。
- 下一步建议：`TASK-072：产品中心广告覆盖筛选与术语精修`。

## TASK-070：草稿产品归因核查的广告来源候选定位

状态：已完成。

背景：
- TASK-069 已能从“产品身份待核查”区域跳到“广告归因”页，并带入候选销售表现产品。
- Product Design 复核发现：归因页虽然带入了候选产品，但没有自动定位对应广告来源证据行，运营仍要自己在候选列表里找来源。
- 当前更重要的是减少人工查找成本，不是新增后端归因算法。

目标：
- 当从草稿产品身份核查进入广告归因页时，同时保留草稿广告来源线索和候选销售表现产品。
- 用草稿广告来源线索自动筛出可能相关的广告来源候选，用候选销售表现产品作为人工确认目标。
- 在归因页顶部展示“建议优先核查的广告来源”，提供查看证据入口。
- 高可信归因候选表和未归因广告数据池中对对应广告来源行做视觉标记。
- 只做证据定位和人工核查引导，不自动保存归因规则、不自动改广告、不写真实库。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_ad_draft_attribution_source_focus.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_ad_draft_attribution_source_focus.py
python scripts\smoke_frontend_ad_draft_attribution_review_entry.py
python scripts\smoke_frontend_product_attribution_candidates.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 前端源码存在保存草稿广告来源线索并按该线索过滤广告来源候选的逻辑。
- 广告归因页在草稿核查上下文中展示“建议优先核查的广告来源”。
- 该区域展示候选销售表现产品、广告来源证据行和“查看该广告来源证据”入口。
- 高可信归因候选表或未归因广告数据池中对应广告来源行有视觉标记。
- 文案明确说明只做证据定位和人工核查，不自动保存归因规则，也不会改广告。
- 前端源码不出现任何自动执行广告动作字段或按钮。

完成依据：
- 新增 [smoke_frontend_ad_draft_attribution_source_focus.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_ad_draft_attribution_source_focus.py)，先红灯确认缺少广告来源候选定位，再实现转绿。
- [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 新增 `adDraftAttributionReviewSourceProduct`，用草稿广告来源线索定位广告来源，用候选销售表现产品作为人工确认目标。
- [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 新增 `sourceMatchesProductIdentity`，按草稿产品 ASIN / MSKU / SKU / 产品名 token 匹配未归因广告来源。
- 广告归因页新增“建议优先核查的广告来源”区块，展示候选销售表现产品、广告来源证据行和“查看该广告来源证据”入口。
- 高可信归因候选表和未归因广告数据池会对匹配草稿广告来源线索的广告来源行做视觉标记。
- 浏览器复核确认从产品中心进入广告归因后，定位区展示候选销售产品 `RBK004-RBK004-2 天蓝 / ASIN B016EXMW4S / MSKU 004-Wayfarer Blue`，并定位广告来源 `RBK004-Auto`。
- 定位区文案说明只做证据定位和人工核查，不会自动保存归因规则，也不会改广告。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_ad_draft_attribution_source_focus.py
python scripts\smoke_frontend_ad_draft_attribution_review_entry.py
python scripts\smoke_frontend_product_attribution_candidates.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核和项目级深度设计。
- Product Design 复核结论：双对象表达更清楚，草稿广告来源线索负责定位广告来源，候选销售表现产品负责人工确认目标，降低了把销售产品和广告来源混成一个对象的风险。
- Product Design 深度设计结论：产品中心的下一层对象边界应区分“全部销售产品、已投放 / 有 SP 数据产品、未投放 / 无 SP 数据产品”；未投放产品不应被催促设置广告调优目标。
- 下一步建议：`TASK-071：产品中心广告覆盖状态分层`。

## TASK-069：草稿产品身份核查后的归因修正入口

状态：已完成。

背景：
- TASK-068 已能在产品目标 / 规则抽屉中识别 `SP广告来源草稿` 产品，并展示可能相关的销售表现产品候选。
- Product Design 复核发现：抽屉里已经能发现身份风险，但还缺少安全的“去广告归因页核查 / 修正归因”动作。
- 如果只提示风险但仍保留目标保存作为主要动作，运营可能继续给草稿对象设置目标，后续异常仍会落到模糊产品对象上。

目标：
- 在“产品身份待核查”区域提供明确的“去广告归因页核查 / 修正归因”入口。
- 点击后关闭产品目标 / 规则抽屉，切到“广告归因”页，并带入候选销售表现产品作为人工核查对象。
- 入口只做导航和人工核查引导，不自动合并产品、不自动迁移归因、不自动写真实库。
- 不新增任何广告自动执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_ad_draft_attribution_review_entry.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_ad_draft_attribution_review_entry.py
python scripts\smoke_frontend_ad_draft_product_identity.py
python scripts\smoke_frontend_product_goal_rule_drawer.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 抽屉“产品身份待核查”区域存在“去广告归因页核查 / 修正归因”入口。
- 入口点击逻辑会关闭目标 / 规则抽屉，并切到 `attribution` 页。
- 有候选销售表现产品时，入口会把候选产品设为 `attributionProductId`，供运营人工核查。
- 文案明确说明这是人工核查 / 修正归因，不自动合并、不自动迁移、不改广告。
- 前端源码不出现任何自动执行广告动作字段或按钮。

完成依据：
- 新增 [smoke_frontend_ad_draft_attribution_review_entry.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_ad_draft_attribution_review_entry.py)，先红灯确认缺少归因核查入口，再实现转绿。
- [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 新增 `openAdDraftAttributionReview` 和 `adDraftAttributionReviewProduct`。
- 产品身份待核查区域新增“去广告归因页核查 / 修正归因”按钮，点击后关闭目标 / 规则抽屉并切到“广告归因”页。
- 跳转会带入首个候选销售表现产品到 `attributionProductId`，并在广告归因页顶部展示“从草稿产品身份核查进入”横幅。
- 浏览器复核确认横幅展示候选产品 `RBK004-RBK004-2 天蓝 / ASIN B016EXMW4S / MSKU 004-Wayfarer Blue`。
- 横幅文案说明只用于人工核查归因，不会自动保存归因规则，也不会改广告。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_ad_draft_attribution_review_entry.py
python scripts\smoke_frontend_ad_draft_product_identity.py
python scripts\smoke_frontend_product_goal_rule_drawer.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：保存目标前先分流到归因核查，能降低把目标配置到草稿对象上的风险；归因页顶部横幅保留了候选产品上下文。
- Product Design 复核发现的新问题：归因页已经带入候选销售产品，但还没有自动定位对应广告来源证据行，运营仍需自己在候选列表里找来源。
- 下一步建议：`TASK-070：草稿产品归因核查的广告来源候选定位`。

## TASK-068：广告草稿产品与销售表现产品身份关系核查

状态：已完成。

背景：
- TASK-067 已让运营能从产品中心首屏打开产品目标 / 规则设置抽屉。
- Product Design 复核发现：当前可设置目标的 `RBK004 kids sunglasses / MSKU RBK004` 属于 `SP广告来源草稿`，缺少 ASIN 和销售表现快照。
- 销售表现接口已同步出真实 RBK004 系列产品，若不先把“草稿产品”和“真实销售产品”的身份关系讲清楚，后续异常和 AI 建议会落到模糊对象上。

目标：
- 在产品目标 / 规则设置抽屉中识别 `SP广告来源草稿` 且缺少 ASIN 或销售表现快照的产品。
- 展示只读的“产品身份待核查”提示，说明当前对象来自广告来源草稿，不等同于真实销售表现商品。
- 展示同店铺内可能相关的销售表现产品候选，帮助运营人工判断是否需要重新归因。
- 不自动合并产品、不自动迁移归因、不自动写真实库、不新增任何广告自动执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_ad_draft_product_identity.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_ad_draft_product_identity.py
python scripts\smoke_frontend_product_goal_rule_drawer.py
python scripts\smoke_frontend_product_center_identity_fields.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 前端源码存在广告草稿产品身份判断逻辑。
- 目标 / 规则抽屉中存在“产品身份待核查”提示。
- 提示文案明确说明 `SP广告来源草稿` 不是完整销售产品身份，需要人工核查。
- 抽屉能列出可能相关的销售表现产品候选，并展示候选的 ASIN、MSKU、销售额、订单。
- 候选展示只读，不包含自动合并、自动迁移、自动执行广告动作入口。

完成依据：
- 新增 [smoke_frontend_ad_draft_product_identity.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_ad_draft_product_identity.py)，先红灯确认缺少广告草稿产品身份核查提示，再实现转绿。
- [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 新增 `AD_DRAFT_PRODUCT_CATEGORY`、`isAdDraftProduct`、`needsAdDraftIdentityReview` 和 `findAdDraftIdentityCandidates`。
- 产品中心“店铺 / 数据来源”不再把所有产品都标成销售表现；`SP广告来源草稿` 产品显示为“广告草稿”。
- 产品目标 / 规则抽屉新增“产品身份待核查”提示，说明当前对象来自 SP 广告来源草稿，不等同于完整销售表现商品。
- 抽屉新增“可能相关的销售表现产品”候选区，只读展示候选 ASIN、MSKU、销售额和订单。
- 浏览器复核确认 `RBK004 kids sunglasses / MSKU RBK004` 抽屉展示 3 个候选销售产品，包括 `RBK004-RBK004-2 天蓝 / ASIN B016EXMW4S / MSKU 004-Wayfarer Blue`。
- 候选区文案明确说明候选只读展示，不会自动合并产品、迁移归因或改广告。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_ad_draft_product_identity.py
python scripts\smoke_frontend_product_goal_rule_drawer.py
python scripts\smoke_frontend_product_center_identity_fields.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：身份风险提示放在产品身份之后、规则表单之前，运营在保存目标前能先看到“这是广告草稿，不是完整销售产品身份”和真实候选产品，方向正确。
- Product Design 复核发现的新问题：抽屉里已经能发现身份风险，但还缺少一个安全的“去广告归因页核查 / 修正归因”动作；否则运营仍可能直接给草稿对象保存目标。
- 下一步建议：`TASK-069：草稿产品身份核查后的归因修正入口`。

## TASK-067：产品目标规则编辑抽屉

状态：已完成。

背景：
- TASK-066 已把有 SP 指标但缺产品目标 / 规则的真实产品排到产品中心前面。
- 只读 API 精确核查确认：`product_id=2 / RBK004 kids sunglasses / MSKU RBK004` 仍没有 `goal` 和 `rules`。
- 直接做“目标保存后的异常生成验收”前提不成立；当前更实际的缺口是人工设置入口仍藏在横向大表里。

目标：
- 产品中心每行提供明确的“设置目标 / 规则”入口。
- 点击后打开右侧抽屉，展示产品身份、SP 表现、人工边界提示、产品目标和规则门槛表单。
- 抽屉复用现有 `productDrafts` 和 `saveProductSettings`，保存仍必须由运营人工点击。
- 不自动生成目标、不写真实库默认值、不新增任何广告自动执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_product_goal_rule_drawer.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_product_goal_rule_drawer.py
python scripts\smoke_frontend_product_goal_rule_guidance.py
python scripts\smoke_frontend_product_center_identity_fields.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 产品中心表格出现“设置目标 / 规则”入口。
- 抽屉标题、人工边界提示、产品身份、SP 表现、产品目标、目标 ACOS、目标 CVR、最小点击、最小花费、最小订单、最大 CPC、库存阈值、备注和“人工保存设置”入口都存在。
- 抽屉字段复用当前产品草稿状态，保存调用现有人工保存函数。
- 前端源码不出现任何自动执行广告动作字段或按钮。

完成依据：
- 新增 [smoke_frontend_product_goal_rule_drawer.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_product_goal_rule_drawer.py)，先红灯确认缺少产品目标 / 规则编辑抽屉，再实现转绿。
- [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 新增 `selectedGoalRuleProduct`、`openGoalRuleDrawer` 和产品目标 / 规则设置抽屉。
- 待设置引导卡片和产品表格操作列都提供“设置目标 / 规则”入口。
- 抽屉展示产品身份、SP 花费、订单、ACOS、CVR、人工设置边界、产品目标和规则门槛字段。
- 抽屉保存入口为“人工保存设置”，复用现有 `saveProductSettings(selectedGoalRuleProduct.id)`。
- 浏览器复核确认首屏引导卡片可直接打开 `RBK004 kids sunglasses / MSKU RBK004` 的抽屉，抽屉可见 SP 花费 `150.47`、订单 `52`、ACOS `27.6%`、CVR `37.7%`。
- 抽屉文案明确说明只保存运营人工设置的产品目标和规则门槛，不会自动生成目标，也不会自动修改广告。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_goal_rule_drawer.py
python scripts\smoke_frontend_product_goal_rule_guidance.py
python scripts\smoke_frontend_product_center_identity_fields.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `python scripts\smoke_product_settings.py` 首次在受限沙箱中因临时 SQLite 文件权限失败，按权限规则非沙箱重跑通过。
- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：入口已从横向大表前置到首屏引导卡片，抽屉把产品身份、SP 证据、人工边界和保存动作放在同一个操作面板内，比原表格直接编辑更适合日常运营。
- Product Design 复核发现的新问题：当前可设置目标的 `RBK004 kids sunglasses` 仍是 `SP广告来源草稿` 产品，没有 ASIN 和销售表现快照；销售表现接口里还有真实 RBK004 系列产品，后续需要核查广告草稿产品与销售表现产品的身份关系。
- 下一步建议：`TASK-068：广告草稿产品与销售表现产品身份关系核查`。

## TASK-066：真实产品目标与规则设置引导

状态：已完成。

背景：
- TASK-065 已把主工作流从演示异常切回真实空态。
- 只读核查确认：当前真实已归因产品有 SP 指标，但缺少人工产品目标和规则。
- 异常规则引擎对目标 ACOS、目标 CVR、库存阈值等判断依赖人工配置；不能由系统自动生成产品目标。

目标：
- 产品中心明确提示“有 SP 指标但缺目标 / 规则”的真实产品。
- 运营能从产品中心直接看出下一步要人工设置产品目标和规则门槛。
- 保留现有人工保存设置流程，不自动生成目标，不自动写库。
- 不新增任何广告自动执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_product_goal_rule_guidance.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_product_goal_rule_guidance.py
python scripts\smoke_frontend_product_center_identity_fields.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_no_demo_anomaly_objects.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 产品中心出现“待设置目标 / 规则”的引导区。
- 引导区只说明需要人工配置产品目标和规则，不暗示 AI 自动设置。
- 有 SP 指标但缺目标或关键规则的产品行能显示待设置状态。
- 保存动作仍是人工点击“保存设置”。
- 前端源码不出现任何自动执行广告动作字段或按钮。

完成依据：
- [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 新增 `hasProductSpMetrics`、`needsProductGoalRuleSetup`、`productsNeedingGoalRuleSetup` 和 `productCenterProducts`。
- 产品中心新增“待设置目标 / 规则”引导区，提示已有 SP 指标但缺少产品目标或目标 ACOS 的产品。
- 待设置产品会优先排在产品中心表格前面，行内显示“目标 / 规则待设置”。
- 浏览器复核确认 `RBK004 kids sunglasses / MSKU RBK004` 排在首行，SP 花费 `150.47`、订单 `52`、ACOS `27.6%`，且显示待设置状态。
- 页面文案明确要求运营人工设置产品目标和规则门槛，并说明系统不会自动生成目标或修改广告。
- 保存入口仍为人工点击“保存设置”。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_goal_rule_guidance.py
python scripts\smoke_frontend_product_center_identity_fields.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_no_demo_anomaly_objects.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `python scripts\smoke_product_settings.py` 首次在受限沙箱中因临时 SQLite 文件权限失败，按权限规则非沙箱重跑通过。
- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：引导区放在产品基础设置表格前，先解释为什么真实异常为空，再把运营带到首行待设置产品和人工保存入口，符合“AI 不执行、人工设置目标”的边界。
- Product Design 复核发现的新问题：目标 / 规则字段仍在横向大表内，后续可把单产品目标规则设置收敛成右侧编辑抽屉，但不是本任务阻断项。
- 下一步建议：`TASK-067：真实产品目标规则保存后的异常生成验收`。需要运营先人工保存真实产品目标和规则。

## TASK-065：真实异常对象替换 DEMO 演示异常

状态：已完成。

背景：
- TASK-064 Product Design 复核发现：当前可见异常 / 建议对象仍带有 `demo high acos keyword` 等演示对象，真实日常使用时会降低可信度。
- 只读核查确认：当前真实库 `anomaly_events` 只有 1 条记录，且为 `DEMO-KW-1 / demo high acos keyword`。
- 真实 SP 关键词已有归因行，但对应真实产品尚未配置人工产品目标和规则；不能为了生成真实异常而自动设置产品目标。

目标：
- 主工作流 API 不再返回 DEMO / SMOKE / TASK 测试异常和建议对象。
- 如果当前没有满足规则条件的真实异常，应返回真实空态，而不是继续展示演示异常。
- 保留历史 DEMO 数据，不删除真实库记录。
- 不自动设置产品目标，不自动生成广告动作。

允许修改：
- `backend/app/api/routes/anomalies.py`
- `backend/app/api/routes/suggestions.py`
- `backend/app/api/routes/dashboard.py`
- `scripts/smoke_real_no_demo_anomaly_objects.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_real_no_demo_anomaly_objects.py
python scripts\smoke_frontend_anomaly_queue.py
python scripts\smoke_frontend_suggestion_detail.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- `/api/anomalies` 不返回 `DEMO` / `SMOKE` / `TASK` 测试异常对象。
- `/api/suggestions` 不返回来自测试异常对象的 AI 建议。
- 驾驶舱异常统计不把测试异常算入真实主工作流。
- 当前没有真实异常时，API 返回空列表而不是 DEMO 异常。
- 不删除历史库数据，不写 `data/app.db`。
- 不新增任何广告自动执行能力。

完成依据：
- `backend/app/api/routes/anomalies.py` 新增真实异常过滤条件，主列表不再返回测试异常对象。
- `backend/app/api/routes/suggestions.py` 基于真实异常过滤建议来源，避免测试异常生成的建议进入主工作流。
- `backend/app/api/routes/dashboard.py` 的异常统计复用同一过滤口径，驾驶舱异常总数不再计入测试异常。
- 新增 `scripts/smoke_real_no_demo_anomaly_objects.py`，验证 anomalies、suggestions、dashboard summary / health 均不暴露测试对象。
- 当前真实 API 返回 `anomaly_count=0`、`suggestion_count=0`，没有真实异常时展示真实空态。
- 浏览器复核确认驾驶舱仍展示 `rivbos / US`、周期 `2026-05-12 至 2026-06-10`、销售额 `2,912.62`、订单 `239`、ACOS `30.4%`。
- 浏览器复核确认异常队列显示 `暂无异常`，未出现 `demo high acos keyword`、`DEMO`、`SMOKE`、`TASK`。
- 验收命令已通过：

```powershell
python scripts\smoke_real_no_demo_anomaly_objects.py
python scripts\smoke_frontend_anomaly_queue.py
python scripts\smoke_frontend_suggestion_detail.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未删除历史库数据，未写 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：主流程从“演示异常”切回“真实空态”，可信度提升；驾驶舱仍可看到真实经营指标和店铺身份，异常队列不再展示测试对象。
- Product Design 复核发现的新问题：真实已归因产品有 SP 指标，但缺少人工产品目标和规则，因此不会生成真实异常；下一步应引导运营为真实产品配置目标与规则。

## TASK-001：积加 SP 数据同步

状态：已完成，后续只允许在明确任务中修改。

目标：
- 使用积加开放平台同步 SP 关键词和搜索词相关数据。
- 写入 SQLite。
- 保留原始响应，便于溯源。

验收依据：
- 已跑通真实 SP 关键词报表同步。
- 已写入 `sp_keyword_metrics`。
- 已生成原始响应文件。

## TASK-002：审查并收敛 SP 异常规则引擎

状态：已完成。

目标：
- 基于近 14 天 SP 关键词 / 搜索词数据生成异常事件。
- 规则结果必须可溯源。
- 输出只允许是异常和建议线索，不允许出现广告自动执行动作。

前置条件：
- TASK-001 已完成。
- SQLite 中已有 SP 指标数据。

允许修改：
- `backend/app/services/rule_service.py`
- `backend/app/models/anomaly.py`
- `backend/app/api/routes/anomalies.py`
- `scripts/smoke_rule_engine.py`
- `tests/test_rule_service.py`

禁止修改：
- `.env`
- `.env.example`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `frontend/`

输入：
- `sp_keyword_metrics`
- `sp_search_term_metrics`
- 默认周期：近 14 天
- 产品目标：人工设置

输出：
- `anomaly_events`
- `rule_result_json`
- `evidence_json`

第一版异常类型：
- ACOS 异常
- 点击多无订单
- CVR 下滑
- 花费异常
- 曝光异常
- 库存目标冲突

验收命令：

```powershell
python scripts/smoke_rule_engine.py
```

成功标准：
- 能基于 SQLite 生成异常事件。
- 每个异常事件包含周期、对象、指标、规则命中原因。
- 规则输出不包含自动执行动作字段。
- smoke 脚本输出成功状态和异常数量。

失败停止条件：
- 缺少真实字段导致规则无法判断。
- 需要改动积加 API 同步逻辑。
- 需要改动前端。
- 验收命令失败。

## TASK-003：异常事件 API 收敛

状态：已完成。

目标：
- 提供异常列表、详情、筛选能力。
- 支持按站点、产品、异常类型、建议等级、状态筛选。

允许修改：
- `backend/app/api/routes/anomalies.py`
- `backend/app/api/routes/normalization.py`
- `scripts/smoke_anomaly_api.py`

验收命令：

```powershell
python scripts/smoke_anomaly_api.py
```

## TASK-004：AI 建议结构化输出

状态：已完成。

目标：
- 为异常事件生成结构化 AI 建议。
- 输出可信度、证据、风险和人工确认选项。

禁止：
- 不允许调用任何广告执行接口。

## TASK-005：人工确认与动作记录

状态：已完成。

目标：
- 支持采纳、修改后采纳、拒绝、加入观察。
- 记录操作者、时间、理由和最终处理状态。

## TASK-006：前端异常队列

状态：已完成。

目标：
- 做可筛选、可点击的异常队列。
- 展示异常等级、产品目标、建议等级、状态。

## TASK-007：建议详情与溯源页

状态：已完成。

目标：
- 展示 AI 建议、规则证据、原始指标和人工确认入口。

## TASK-008：处理记录与复盘页

状态：已完成。

目标：
- 展示历史处理记录。
- 支持按产品、异常类型、状态筛选。

## TASK-009：真实端到端验收

状态：已完成。

目标：
- 启动本地后端和前端。
- 验证前端入口、后端健康接口、核心只读 API 可以真实访问。
- 再次确认系统只做建议、证据和人工确认，不暴露广告自动执行字段。

允许修改：
- `scripts/smoke_real_e2e.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_real_e2e.py
```

成功标准：
- `GET /health` 返回 `status=ok`。
- `GET /api/anomalies`、`GET /api/suggestions`、`GET /api/decisions`、`GET /api/dashboard/health` 返回成功。
- 前端 `http://127.0.0.1:5173` 可以返回 HTML 入口。
- API 返回字段不包含自动执行广告动作字段。

## TASK-010：产品目标与规则设置闭环验收

状态：已完成。

目标：
- 验证产品创建、目标设置、规则门槛设置、广告活动绑定可以通过 API 完成闭环。
- 验证前端“产品目标与规则设置”页面具备筛选、编辑、保存、批量应用入口。
- 确认这些设置只影响规则判断，不包含任何广告自动执行动作字段。

允许修改：
- `scripts/smoke_product_settings.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_product_settings.py
python scripts/smoke_real_e2e.py
```

成功标准：
- 产品目标和规则可通过 API 写入、读取、更新。
- 广告活动绑定只更新本地指标归属，不调用广告执行接口。
- 前端源码具备对应交互入口。
- API 返回字段不包含自动执行广告动作字段。

## TASK-011：UI 方案对齐与核心页面重构

状态：已完成。

目标：
- 将核心页面从默认功能型后台，调整为更贴近精品运营场景的专业运营工作台。
- 优先重构广告健康驾驶舱、异常与 AI 建议队列、建议详情与溯源抽屉的视觉层级。
- 保持原有业务功能和 API 不变，不新增广告执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_ui_alignment.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_ui_alignment.py
npm run build
python scripts/smoke_real_e2e.py
```

成功标准：
- 页面具备明确的运营系统定位、顶部状态标签和统一筛选工具条。
- 指标卡、表格、抽屉、AI 建议区域有统一视觉层级。
- UI 不再只依赖默认 Ant Design 样式。
- 构建和真实端到端 smoke 仍通过。

## TASK-012：前端 API 指向修正与截图验收

状态：已完成。

目标：
- 不修改真实 `.env.local`。
- 使用临时环境变量将前端运行时 API 指向真实后端 `http://127.0.0.1:8001`。
- 重新截图验证驾驶舱能展示真实数据，不再因为 API 指向错误显示空态。

允许修改：
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_real_e2e.py
```

成功标准：
- 后端 `http://127.0.0.1:8001/health` 正常。
- 前端 `http://127.0.0.1:5173/` 正常。
- 截图中驾驶舱展示真实指标或同步状态，不再显示 `暂无驾驶舱数据`。
- 不修改 `.env.local`，不新增任何广告自动执行能力。

## TASK-013：本地启动配置收敛

状态：已完成。

目标：
- 提供一个固定本地端口和 API 指向的启动脚本。
- 后端固定为 `http://127.0.0.1:8001`。
- 前端固定为 `http://127.0.0.1:5173`。
- 前端运行时固定注入 `VITE_API_BASE_URL=http://127.0.0.1:8001`。
- 避免重启前端后再次读到 `frontend/.env.local` 中的错误端口。

允许修改：
- `scripts/start_local_dev.ps1`
- `scripts/smoke_local_dev_config.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_local_dev_config.py
python scripts/smoke_real_e2e.py
```

成功标准：
- 启动脚本存在，且明确注入 `VITE_API_BASE_URL=http://127.0.0.1:8001`。
- 前端运行时模块实际使用 `http://127.0.0.1:8001`。
- 真实端到端 smoke 仍通过。
- 不修改 `.env`、`.env.example`、`frontend/.env.local`。

## TASK-014：驾驶舱首屏视觉二次打磨

状态：已完成。

目标：
- 基于真实数据截图优化驾驶舱首屏。
- 首屏优先展示待人工处理、高风险异常、ACOS、销售订单等决策指标。
- 将同步状态压缩为辅助信息，避免挤占首屏判断空间。
- 保持现有业务功能和 API 不变，不新增广告执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_ui_alignment.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_ui_alignment.py
npm run build
python scripts/smoke_real_e2e.py
```

成功标准：
- 驾驶舱首屏出现明确的决策摘要区。
- 同步记录不再占据首屏主要视觉权重。
- 指标卡、趋势图、异常分布仍可见且不破坏原有交互。
- 构建和真实端到端 smoke 仍通过。

## TASK-015：引入近 30 天真实 SP 数据

状态：已完成。

目标：
- 使用积加只读 API 同步近 30 天真实 SP 数据。
- 同步 SP 关键词报表和 SP 搜索词报表。
- 保存原始响应到 `data/raw`，并写入 SQLite。
- 同步成功后清理同站点、同周期内更早同步的旧行，避免重复统计。

允许修改：
- `scripts/sync_recent_30_days.py`
- `scripts/smoke_recent_30_days_data.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/sync_recent_30_days.py
python scripts/smoke_recent_30_days_data.py
python scripts/smoke_real_e2e.py
```

成功标准：
- 近 30 天 SP 关键词真实数据写入 `sp_keyword_metrics`。
- 近 30 天 SP 搜索词真实数据写入 `sp_search_term_metrics`。
- 原始响应保存到 `data/raw`。
- dashboard 能读取近 30 天数据。
- 不修改 `.env`，不新增任何广告自动执行能力。

## TASK-016：前端数据口径修正为近 30 天聚合数据

状态：已完成。

目标：
- 前端默认时间范围改为近 30 天。
- 顶部文案从近 14 天改为近 30 天。
- 驾驶舱趋势区域明确提示当前积加数据为周期聚合数据，不误导为逐日趋势。
- 保留指标卡、异常分布和人工确认流程。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_ui_alignment.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_ui_alignment.py
python scripts/smoke_recent_30_days_data.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- 前端默认开始日期覆盖近 30 天真实数据。
- 页面文案明确显示近 30 天。
- 趋势区域明确提示周期聚合数据，避免按逐日趋势误读。
- 真实数据 smoke、端到端 smoke 和构建仍通过。

## TASK-017：同步店铺名称并展示数据身份

状态：已完成。

目标：
- 使用积加只读接口 `/middle/base/marketNames/query` 查询真实店铺名称。
- 在 SQLite 保存 `market_id` 到店铺名称、站点的映射。
- 驾驶舱首屏明确展示当前数据身份：店铺名称、market_id、数据来源、数据周期、产品绑定状态。
- 驾驶舱展示广告活动 Top 列表，让运营知道当前指标来自哪些 Campaign。

允许修改：
- `backend/app/models/market.py`
- `backend/app/core/database.py`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/market_service.py`
- `backend/app/api/routes/dashboard.py`
- `frontend/src/api.ts`
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/sync_market_names.py`
- `scripts/smoke_dashboard_identity.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_dashboard_identity.py
python scripts/sync_market_names.py
python scripts/smoke_recent_30_days_data.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- 能从积加真实接口拿到 `market_id=1` 的店铺名称。
- dashboard API 返回店铺名称、站点、数据来源、产品绑定状态和广告活动 Top 列表。
- 前端首屏能直接看出当前是什么店铺、什么周期、哪些广告活动的数据。
- 不新增任何广告自动执行能力。

## TASK-018：驾驶舱首屏 UI 收敛

状态：已完成。

目标：
- 基于当前真实页面截图收敛驾驶舱首屏信息层级。
- 首屏优先回答：是谁的数据、当前最该处理什么、关键经营指标如何。
- 将同步状态、数据身份、经营指标明细和广告活动来源压缩为更轻量的展示。
- 保持现有业务功能和 API 不变，不新增广告执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_ui_alignment.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_ui_alignment.py
python scripts/smoke_dashboard_identity.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- 驾驶舱首屏出现轻量数据身份条，不再使用大面积身份卡片。
- 首屏保留 4 个核心决策指标，并明确提供查看异常队列入口。
- 经营指标明细降级为次级区域，不抢占首屏主判断。
- 广告活动来源 Top 使用紧凑列表展示。
- 构建和 smoke 验收通过。
- 不新增任何广告自动执行能力。

## TASK-019：当前 MVP 基线验收

状态：已完成。

目标：
- 不新增业务功能，先验收当前系统距离真实日常使用的差距。
- 验证真实 30 天数据、店铺身份、Campaign 来源、异常队列、AI 建议、人工确认入口和复盘记录链路。
- 明确下一步是否可以进入“产品与广告归因闭环”。
- 保持现有业务功能和 API 不变，不新增广告执行能力。

允许修改：
- `scripts/smoke_current_mvp_baseline.py`
- `scripts/smoke_recent_30_days_data.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `frontend/`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_current_mvp_baseline.py
python scripts/smoke_recent_30_days_data.py
python scripts/smoke_dashboard_identity.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- 真实 30 天 SP 数据可用。
- 验收脚本使用最新成功同步周期，不受本机日期跨天影响。
- dashboard 可返回店铺身份、数据来源、产品绑定状态和广告活动来源 Top。
- 异常、建议、人工决策、复盘相关 API 可访问。
- 前端源码包含主工作流入口。
- 基线结论明确指出下一步是否进入产品归因。
- 不新增任何广告自动执行能力。

## TASK-020：产品与广告归因规则闭环

状态：已完成。

目标：
- 新增人工确认后的产品广告归因规则。
- 支持 Campaign 和 Ad Group 两种归因颗粒度。
- 提供未归因广告数据池，帮助运营按真实 SP 数据确认归属。
- 保存归因规则后，回填当前关键词和搜索词指标的 `product_id`。
- 后续同步新 SP 数据时，继续套用已确认归因规则。
- 前端产品设置页能完成查看未归因数据、选择产品、确认归因、查看归因结果。

允许修改：
- `backend/app/models/product.py`
- `backend/app/core/database.py`
- `backend/app/services/product_attribution_service.py`
- `backend/app/services/sync_service.py`
- `backend/app/api/routes/products.py`
- `frontend/src/api.ts`
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_product_attribution.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_product_attribution.py
python scripts/smoke_product_settings.py
python scripts/smoke_dashboard_identity.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- 可以查询未归因 Campaign / Ad Group 数据池。
- 可以保存 Campaign 级归因规则。
- 可以保存 Ad Group 级归因规则。
- 保存规则后，关键词和搜索词指标都会回填 `product_id`。
- 新同步写入的指标可以继续套用已确认归因规则。
- dashboard 产品绑定状态能反映已归因行数。
- 前端产品设置页包含未归因数据池、产品选择、归因颗粒度和人工确认入口。
- API 和前端都不包含自动执行广告动作字段。

## TASK-021：本地启动脚本校验后端归因接口

状态：已完成。

目标：
- 修正本地启动脚本只检查 `/health` 导致旧后端继续运行的问题。
- 启动时必须确认后端已提供 TASK-020 的归因接口。
- 如果 8001 上是旧后端，脚本应停止旧监听进程并启动新后端。
- 保持前端端口和 API 指向逻辑不变。

允许修改：
- `scripts/start_local_dev.ps1`
- `scripts/smoke_local_dev_config.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `backend/app/api/routes/products.py`
- `frontend/`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_local_dev_config.py
python scripts/smoke_product_attribution.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- `scripts/start_local_dev.ps1` 会校验 `/api/products/unbound-ad-sources`。
- 旧后端只返回 `/health` 正常时，启动脚本不会误判为可用。
- 前端仍固定指向 `http://127.0.0.1:8001`。
- 不修改任何真实 env 文件。
- 不新增任何广告自动执行能力。

## TASK-022：真实产品归因预览验收

状态：已完成。

目标：
- 基于真实 `data/app.db` 和当前本地 API，只读预览产品归因条件。
- 确认当前真实店铺、真实周期、产品列表、未归因 Campaign / Ad Group 数据池是否可用。
- 输出下一步人工归因所需的最小证据：产品候选、未归因广告来源、花费 / 销售 / ACOS / 行数。
- 不创建产品、不保存归因规则、不回填 `product_id`。

允许修改：
- `scripts/smoke_real_product_attribution_preview.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `frontend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_real_product_attribution_preview.py
python scripts/smoke_real_e2e.py
```

成功标准：
- 能读取真实 dashboard 周期、店铺身份和产品绑定状态。
- 能读取真实产品候选列表。
- 能读取未归因 Campaign 和 Ad Group 数据池。
- 输出人工归因所需的 Top 未归因广告来源。
- 不产生任何写库动作。
- API 返回字段不包含自动执行广告动作字段。

## TASK-023：产品归因确认闭环细化

状态：已完成。

目标：
- 在人工确认产品归因前，提供归因对象的证据详情、产品候选、可信度说明和冲突提示。
- 保存归因规则时，把确认时的证据快照写入 `evidence_json`，便于后续溯源。
- 前端从“直接确认”调整为“查看证据 -> 人工确认 -> 保存证据快照”的流程。
- 支持已确认规则在前端查看证据摘要。
- 保持只写本地归因规则和指标 `product_id` 回填，不新增任何广告自动执行能力。

允许修改：
- `backend/app/services/product_attribution_service.py`
- `backend/app/api/routes/products.py`
- `frontend/src/api.ts`
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_product_attribution.py`
- `scripts/smoke_product_attribution_evidence.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_product_attribution_evidence.py
python scripts/smoke_product_attribution.py
python scripts/smoke_real_product_attribution_preview.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- 后端可以按 Campaign / Ad Group 查询归因证据详情。
- 归因证据包含对象指标、Top 关键词、Top 搜索词、产品候选、可信度分数、判断理由和冲突提示。
- 保存归因规则时，`evidence_json` 包含确认周期、归因对象快照、候选产品快照、可信度和人工确认信息。
- 前端未归因数据池必须先进入证据详情，再由人工确认归因。
- 已确认归因规则能展示证据摘要。
- 所有新增 API 和前端文案都不包含自动执行广告动作能力。

## TASK-024：搜索词真实字段映射修复

状态：已完成。

目标：
- 修复积加 SP 搜索词 raw 字段映射：`query` 写入 `search_term`。
- 同步补齐 `targetingText -> keyword_text`、`targetId -> keyword_id`、`targetingType -> match_type` 的兼容映射。
- 基于已保存 raw payload 回填当前 `data/app.db` 中空的搜索词字段。
- 让近 30 天真实数据验收必须确认存在非空用户搜索词。
- 为后续“用户搜索词归类聚合分析”提供真实搜索词输入。

允许修改：
- `backend/app/services/sync_service.py`
- `scripts/smoke_search_term_mapping.py`
- `scripts/backfill_search_terms_from_raw.py`
- `scripts/smoke_recent_30_days_data.py`
- `data/app.db`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `frontend/`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_search_term_mapping.py
python scripts/backfill_search_terms_from_raw.py
python scripts/smoke_recent_30_days_data.py
python scripts/smoke_real_e2e.py
```

成功标准：
- 新同步映射能从 raw `query` 得到 `search_term`。
- 新同步映射能从 raw `targetingText`、`targetId`、`targetingType` 补齐关键词和匹配类型字段。
- 当前真实库 `sp_search_term_metrics.search_term` 有非空真实用户搜索词。
- 近 30 天真实数据验收会检查非空搜索词数量。
- 不修改真实环境配置，不新增任何广告自动执行能力。

## TASK-025：搜索词归类聚合分析接口

状态：已完成。

目标：
- 基于真实 `sp_search_term_metrics.search_term` 做只读搜索词聚合分析。
- 第一版按搜索词维度聚合点击、花费、订单、销售、ACOS、CVR。
- 第一版用可解释规则做语义分类和表现分类，不引入复杂 AI 分类。
- 输出分类汇总、表现汇总、搜索词明细和证据字段，供后续前端页面使用。
- 不保存任何广告动作，不新增任何广告自动执行能力。

允许修改：
- `backend/app/main.py`
- `backend/app/api/routes/search_terms.py`
- `backend/app/services/search_term_analysis_service.py`
- `scripts/smoke_search_term_analysis.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `data/app.db`
- `frontend/`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_search_term_analysis.py
python scripts/smoke_recent_30_days_data.py
python scripts/smoke_real_e2e.py
```

成功标准：
- `/api/search-terms/analysis` 可返回搜索词聚合分析。
- 返回结果包含周期、汇总、语义分类汇总、表现分类汇总和搜索词明细。
- 搜索词明细包含语义分类、表现分类、指标、来源 Campaign / Ad Group / 关键词证据。
- 支持按 `market_id`、周期、语义分类、表现分类过滤。
- 接口只读，不产生写库动作。
- API 返回字段不包含自动执行广告动作字段。

## TASK-026：搜索词归类聚合分析前端页

状态：已完成。

目标：
- 新增独立“搜索词分析”主工作流入口。
- 接入 `/api/search-terms/analysis`，展示真实搜索词聚合、语义分类、表现分类和来源证据。
- 支持按店铺、周期、语义分类、表现分类、最小点击、最小花费筛选。
- 页面只做查看、筛选和证据整理，不提供任何广告自动执行入口。

允许修改：
- `frontend/src/api.ts`
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_search_terms.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_frontend_search_terms.py
python scripts/smoke_search_term_analysis.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- 前端包含“搜索词分析”Tab。
- 页面调用 `/api/search-terms/analysis`。
- 页面展示汇总指标、语义分类汇总、表现分类汇总和搜索词明细。
- 搜索词明细包含分类标签、ACOS、CVR、花费、订单、销售额、Campaign / Ad Group / 关键词证据。
- 支持分类和门槛筛选。
- 前端源码不包含任何自动执行广告动作入口。

## TASK-027：搜索词人工处理候选池

状态：已完成。

目标：
- 基于 `/api/search-terms/analysis` 的真实搜索词聚合结果，派生人工处理候选池。
- 第一版只包含三类候选：高转化放量候选、高花费无单处理候选、高 ACOS 处理候选。
- 候选只输出证据、理由、推荐人工判断类型和风险提示，不写广告动作、不写人工处理记录。
- 前端在“搜索词分析”页展示候选池，并支持候选类型筛选。

允许修改：
- `backend/app/api/routes/search_terms.py`
- `backend/app/services/search_term_analysis_service.py`
- `frontend/src/api.ts`
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_search_term_candidates.py`
- `scripts/smoke_frontend_search_term_candidates.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_search_term_candidates.py
python scripts/smoke_frontend_search_term_candidates.py
python scripts/smoke_search_term_analysis.py
python scripts/smoke_frontend_search_terms.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- `/api/search-terms/candidates` 可返回人工处理候选池。
- 候选来源于真实搜索词聚合，不新增或修改广告数据。
- 候选包含类型、标题、推荐人工判断类型、建议人工处理说明、理由、风险提示、指标和来源证据。
- 支持按 `candidate_type`、店铺、周期、语义分类、表现分类、门槛过滤。
- 前端“搜索词分析”页展示候选池和候选类型筛选。
- API 和前端源码不包含任何自动执行广告动作字段或入口。

## TASK-028：搜索词候选转人工记录

状态：已完成。

目标：
- 让运营可以从“搜索词分析”的人工处理候选池中，把候选项记录为人工判断结果。
- 第一版只支持本地记录：加入观察、修改后采纳、拒绝。
- 记录必须保存候选当时的指标、证据、来源追踪和人工备注，便于后续复盘。
- 不调用任何广告执行 API，不自动新增 / 否定关键词，不自动调整竞价，不自动暂停或开启广告。

允许修改：
- `backend/app/models/decision.py`
- `backend/app/core/database.py`
- `backend/app/api/routes/search_terms.py`
- `backend/app/services/search_term_analysis_service.py`
- `frontend/src/api.ts`
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_search_term_candidate_decisions.py`
- `scripts/smoke_frontend_search_term_candidate_decisions.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：
```powershell
python scripts/smoke_search_term_candidate_decisions.py
python scripts/smoke_frontend_search_term_candidate_decisions.py
python scripts/smoke_search_term_candidates.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- 后端可以把搜索词候选写入本地人工处理记录。
- 支持按候选 ID、处理类型、处理人筛选搜索词候选处理记录。
- 处理记录包含候选证据快照、人工备注、观察周期或修改后处理说明。
- 前端候选池提供“记录人工判断”入口，并能刷新看到记录结果。
- API 和前端源码不包含任何自动执行广告动作字段或入口。

## TASK-029：搜索词处理记录复盘入口

状态：已完成。

目标：
- 让运营可以基于已保存的搜索词候选人工记录，查看后续 7 天 / 14 天表现变化。
- 第一版只做只读复盘计算，不新增复盘写库表。
- 复盘结果对比人工记录时保存的候选指标快照和后续同搜索词聚合指标。
- 输出复盘结论只用于人工判断，不自动修改广告。

允许修改：
- `backend/app/api/routes/search_terms.py`
- `frontend/src/api.ts`
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_search_term_candidate_review.py`
- `scripts/smoke_frontend_search_term_candidate_review.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：
```powershell
python scripts/smoke_search_term_candidate_review.py
python scripts/smoke_frontend_search_term_candidate_review.py
python scripts/smoke_search_term_candidate_decisions.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- 后端可以按搜索词候选处理记录 ID 返回 7 天 / 14 天只读复盘结果。
- 复盘结果包含记录时指标快照、后续指标、指标变化和人工判断用结论。
- 前端候选处理记录表提供查看复盘入口，并展示 7 天 / 14 天复盘结果。
- API 和前端源码不包含任何自动执行广告动作字段或入口。

## TASK-030：真实产品基础数据接入与归因可用性复核

状态：已完成。

目标：
- 解决真实库缺少真实产品候选导致产品归因无法日常使用的问题。
- 让运营可以从未归因广告来源创建产品草稿，手动补齐 ASIN / MSKU / 品牌 / 分类等基础信息。
- 创建产品草稿后，归因证据能把该产品纳入候选产品并给出可信度理由。
- 不自动保存归因规则，不自动回填 `product_id`，归因仍必须由人工确认。

允许修改：
- `frontend/src/App.tsx`
- `scripts/seed_products_from_unbound_ad_sources.py`
- `scripts/smoke_product_seed_from_ad_source.py`
- `scripts/smoke_frontend_product_seed_from_ad_source.py`
- `scripts/smoke_real_product_seed_ready.py`
- `data/app.db`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_product_seed_from_ad_source.py
python scripts/smoke_frontend_product_seed_from_ad_source.py
python scripts/seed_products_from_unbound_ad_sources.py --apply
python scripts/smoke_real_product_seed_ready.py
python scripts/smoke_real_product_attribution_preview.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- 前端未归因广告来源和归因证据抽屉提供“从广告来源创建产品草稿”入口。
- 产品草稿表单能预填广告来源名称和店铺 / 站点 ID。
- 创建产品后能重新拉取归因证据，并把新产品设为当前归因候选。
- 基于真实未归因广告来源能生成本地真实产品草稿，真实库 `ready_for_manual_binding=true`。
- 后端现有产品创建接口和归因证据接口能证明：创建产品不会自动保存归因规则，只有人工确认归因才会写本地规则。
- API 和前端源码不包含任何自动执行广告动作字段或入口。

完成依据：
- 已创建 8 个真实产品草稿：`RBK004`、`RB833`、`RB832`、`RBK037`、`RB831`、`RBK003`、`RBK0231`、`RBK068`。
- `smoke_real_product_seed_ready.py` 返回 `ready_for_manual_binding=true`、`real_product_count=8`、`active_binding_count=0`。
- `smoke_real_product_attribution_preview.py` 返回 `ready_for_manual_binding=true`，阻塞原因为空。

## TASK-031：产品归因候选队列

状态：已完成。

目标：
- 基于真实未归因广告来源和真实产品草稿，生成只读产品归因候选队列。
- 每条候选展示归因对象、推荐产品、可信度、理由和核心指标。
- 运营必须点击查看证据后再人工确认归因；系统不自动保存归因规则。
- 为后续搜索词按产品维度聚合准备清晰产品归因入口。

允许修改：
- `backend/app/services/product_attribution_service.py`
- `backend/app/api/routes/products.py`
- `frontend/src/api.ts`
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_product_attribution_candidates.py`
- `scripts/smoke_frontend_product_attribution_candidates.py`
- `scripts/smoke_real_product_attribution_candidates.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_product_attribution_candidates.py
python scripts/smoke_frontend_product_attribution_candidates.py
python scripts/smoke_real_product_attribution_candidates.py
python scripts/smoke_real_product_attribution_preview.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- `/api/products/attribution-candidates` 返回只读归因候选队列。
- 候选来自未归因广告来源和产品候选可信度，不新增归因规则，不回填 `product_id`。
- 前端产品设置页展示“高可信归因候选”面板。
- 前端候选只能进入“查看证据 -> 人工确认归因”流程。
- API 和前端源码不包含任何自动执行广告动作字段或入口。

完成依据：
- `/api/products/attribution-candidates` 已返回只读归因候选队列。
- 真实库返回 20 条高可信候选，Top 为 `RBK004-kids sunglasses-广泛 -> RBK004 kids sunglasses`，可信度 100。
- 真实库 `active_binding_count=0`，候选接口没有自动保存归因规则。
- 前端产品设置页已展示“高可信归因候选”面板。

## TASK-032：产品维度搜索词分析就绪检查

状态：已完成。

目标：
- 搜索词分析页支持按产品筛选。
- 新增只读就绪检查，判断当前是否已有人工确认产品归因规则和已归因搜索词数据。
- 如果没有已确认归因规则，前端明确提示先去产品设置页人工确认归因，不强行给出产品维度结论。

允许修改：
- `backend/app/api/routes/search_terms.py`
- `backend/app/services/search_term_analysis_service.py`
- `frontend/src/api.ts`
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_search_term_product_readiness.py`
- `scripts/smoke_frontend_search_term_product_readiness.py`
- `scripts/smoke_real_search_term_product_readiness.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_search_term_product_readiness.py
python scripts/smoke_frontend_search_term_product_readiness.py
python scripts/smoke_real_search_term_product_readiness.py
python scripts/smoke_search_term_analysis.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- `/api/search-terms/product-readiness` 返回产品维度搜索词分析就绪状态。
- 就绪判断基于已确认产品归因规则和已归因搜索词行数。
- 搜索词分析接口继续支持 `product_id` 筛选。
- 前端搜索词分析页提供产品筛选和产品维度就绪提示。
- 无已确认归因规则时，提示先去产品设置页人工确认归因。
- API 和前端源码不包含任何自动执行广告动作字段或入口。

完成依据：
- 新增 `/api/search-terms/product-readiness`，返回产品维度搜索词分析就绪状态。
- 就绪判断基于 `product_ad_bindings.status=active` 和 `sp_search_term_metrics.product_id` 已归因搜索词行数。
- `frontend/src/api.ts` 新增 `SearchTermProductReadiness` 和 `fetchSearchTermProductReadiness`。
- `frontend/src/App.tsx` 搜索词分析页新增“产品筛选”和“产品维度分析未就绪 / 已就绪”提示。
- 当前真实库检查结果：`active_binding_count=0`、`bound_search_term_rows=0`、`ready=false`，页面会提示“请先在产品设置页人工确认产品归因规则”。
- 浏览器已确认搜索词分析页能看到产品筛选、产品维度分析未就绪和人工归因提示。
- 验收命令已通过：

```powershell
python scripts/smoke_search_term_product_readiness.py
python scripts/smoke_frontend_search_term_product_readiness.py
python scripts/smoke_real_search_term_product_readiness.py
python scripts/smoke_search_term_analysis.py
python scripts/smoke_real_e2e.py
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。

## TASK-064：复盘弹窗人工边界提示补齐

状态：已完成。

背景：
- TASK-063 已验收真实异常从人工处理记录到 `7d` 复盘记录的闭环。
- Product Design 复核发现：处理记录列表能看到复盘状态，但复盘弹窗内缺少单独的“只记录复盘，不自动修改广告”边界提示。

目标：
- 在复盘弹窗内增加清晰的人工边界提示。
- 提示必须说明：复盘只记录人工判断和指标快照，不会自动修改竞价、预算、否词、暂停或新增广告。
- 不改 API、不写真实库、不改变复盘保存逻辑。

允许修改：
- `frontend/src/App.tsx`
- `scripts/smoke_frontend_review_modal_manual_boundary.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_review_modal_manual_boundary.py
python scripts\smoke_frontend_decision_review.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 前端源码的复盘弹窗内存在人工边界提示。
- 提示文案明确包含“不自动修改广告”和复盘只记录人工判断 / 指标快照的含义。
- 原有处理记录与复盘 smoke 仍通过。
- 真实端到端 smoke 仍通过。
- 前端构建通过。
- 前端源码不出现任何自动执行广告动作按钮。

完成依据：
- `frontend/src/App.tsx` 的复盘弹窗新增 `人工复盘边界` 提示。
- 提示说明复盘只记录人工判断和指标快照，不自动修改广告，也不会自动调整竞价、预算、否词、暂停或新增广告。
- 新增 `scripts/smoke_frontend_review_modal_manual_boundary.py`，先红灯确认缺口，再实现转绿。
- 浏览器复查确认复盘弹窗内可见人工边界提示，且表单仍可见。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_review_modal_manual_boundary.py
python scripts\smoke_frontend_decision_review.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：复盘弹窗在表单前展示人工边界，运营保存复盘前能明确知道系统只记录人工判断和指标快照，不会自动改广告。
- Product Design 复核发现的新问题：当前可见异常 / 建议对象仍带有 `demo high acos keyword` 等演示对象，不适合真实日常使用。
- 下一步建议：`TASK-065：真实异常对象替换 DEMO 演示异常`。

## TASK-063：真实人工确认与复盘闭环验收

状态：已完成。

背景：
- TASK-062 已让驾驶舱默认周期对齐最近成功同步周期。
- 真实库已有异常建议和人工处理记录，但复盘记录仍为空。
- 当前任务只验收广告异常建议的人工确认与复盘闭环，不混入搜索词组级判断。

目标：
- 基于真实本地 API 验证异常、建议、人工处理记录和复盘记录的对象关系。
- 如当前人工处理记录尚无复盘，创建或更新一条明确标记的 `7d` smoke 复盘记录。
- 验证前端“处理记录与复盘”入口仍可用。
- 不调用任何广告执行接口，不自动改广告。

允许修改：
- `scripts/smoke_real_manual_review_loop.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`
- `data/app.db`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_real_manual_review_loop.py
python scripts\smoke_frontend_decision_review.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 真实本地 API 中至少存在一条异常、建议和人工处理记录。
- smoke 能为已有人工处理记录创建或更新 `7d` 复盘记录。
- `/api/decisions` 可追溯到原建议、异常 ID 和异常证据。
- `/api/reviews` 可按 `decision_id` 读取复盘记录。
- 前端源码包含处理记录与复盘入口、复盘弹窗、7 天 / 14 天复盘和复盘指标入口。
- API 返回和前端源码不出现自动执行广告动作字段或按钮。

完成依据：
- 新增 `scripts/smoke_real_manual_review_loop.py`，先红灯确认文件缺失，再实现转绿。
- smoke 复用已有人工处理记录 `decision_id=1`，不自动创建人工确认记录。
- smoke 已创建或更新 `7d` 复盘记录 `review_id=1`，结果为 `unchanged`。
- `/api/decisions` 可追溯到 `suggestion_id=1`、`anomaly_event_id=1` 和异常证据。
- `/api/reviews?decision_id=1` 可读取刚保存的复盘记录。
- 浏览器复查：处理记录与复盘页可见 `7 天复盘 无明显变化`、`14 天复盘 未复盘`，复盘弹窗可打开。
- 验收命令已通过：

```powershell
python scripts\smoke_real_manual_review_loop.py
python scripts\smoke_frontend_decision_review.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、SP 同步服务。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：真实处理记录页已经能让运营看到“已人工观察 -> 7 天复盘结果 -> 14 天待复盘”的闭环状态，复盘入口可打开。
- Product Design 复核发现的新问题：复盘弹窗内缺少单独的“只记录复盘，不自动修改广告”边界提示；下一步应补齐弹窗级人工边界提示。

## TASK-061：异常队列状态口径与入口收敛

状态：已完成。

目标：
- 异常队列默认展示“全部状态”，避免驾驶舱有异常但队列默认空。
- 从驾驶舱进入异常队列时，同步 dashboard 筛选条件，并显式切到“全部状态”。
- `status=all` 只作为前端筛选 UI 值，不传给后端 API。
- 不修改后端、不写真实库、不新增任何广告执行能力。

允许修改：
- `frontend/src/App.tsx`
- `scripts/smoke_frontend_anomaly_status_scope.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_anomaly_status_scope.py
python scripts\smoke_frontend_anomaly_queue.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

完成依据：
- 新增 `scripts/smoke_frontend_anomaly_status_scope.py`，先红灯确认前端缺少“全部状态”口径，再实现转绿。
- `frontend/src/App.tsx` 新增 `AnomalyStatusFilter`、`anomalyStatusFilterOptions` 和 `anomalyStatusParam`。
- 异常队列默认状态从 `pending` 改为 `all`。
- `fetchAnomalies` 和 `generateSuggestions` 均通过 `anomalyStatusParam(status)` 转换请求参数，不会向后端发送 `status=all`。
- 驾驶舱“查看异常队列”入口会同步筛选条件，并显式设置为“全部状态”。
- 浏览器只读复查：从驾驶舱进入异常队列后，状态筛选显示“全部状态”，表格直接显示当前“观察中”异常。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_anomaly_status_scope.py
python scripts\smoke_frontend_anomaly_queue.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：入口状态口径已收敛，驾驶舱异常总数和异常队列默认列表不再互相打架。
- Product Design 复核发现的新问题：驾驶舱默认周期滚到 `2026-05-13 至 2026-06-11`，但最近真实同步周期是 `2026-05-12 至 2026-06-10`，导致首页经营指标显示 0；下一步应让驾驶舱默认使用最近成功同步周期。
- 下一步建议：`TASK-062：驾驶舱默认周期对齐最近成功同步周期`。

## TASK-062：驾驶舱默认周期对齐最近成功同步周期

状态：已完成。

目标：
- 驾驶舱仍处于默认滚动周期时，优先使用最近成功同步记录的 `period_start` / `period_end` 请求数据。
- 同步更新驾驶舱顶部周期输入框，让运营看到真实使用的周期。
- 用户手动改过周期时，不强行覆盖用户输入。
- 不修改后端、不写真实库、不新增任何广告执行能力。

允许修改：
- `frontend/src/App.tsx`
- `scripts/smoke_frontend_dashboard_latest_sync_period.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_dashboard_latest_sync_period.py
python scripts\smoke_dashboard_identity.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

完成依据：
- 新增 `scripts/smoke_frontend_dashboard_latest_sync_period.py`，先红灯确认驾驶舱缺少最近成功同步周期解析，再实现转绿。
- `frontend/src/App.tsx` 新增 `DashboardWorkflowFilters` 和 `resolveDashboardFilters`。
- 当驾驶舱周期仍为默认滚动周期时，前端会读取最近成功同步记录，并用 `period_start` / `period_end` 请求 dashboard。
- 用户手动改过驾驶舱周期时，`resolveDashboardFilters` 不覆盖用户输入。
- `loadDashboard` 已改为使用解析后的 filters 请求 `fetchDashboardHealth`、`fetchDashboardTrends` 和 `fetchDashboardAnomalySummary`。
- 浏览器只读复查：驾驶舱周期输入框显示 `2026-05-12` 至 `2026-06-10`，页面展示 `rivbos / US`、销售额 `2,912.62`、订单 `239`、ACOS `30.4%`。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_dashboard_latest_sync_period.py
python scripts\smoke_dashboard_identity.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：驾驶舱首屏已回到最近真实同步周期，核心经营指标不再因日期漂移显示 0。
- Product Design 复核暂未发现新的阻断项；下一步建议：`TASK-063：真实人工确认与复盘闭环验收`。

## TASK-059：经营快照辅助广告健康判断

状态：已完成。

背景：
- 产品中心已具备真实产品身份和销售表现经营快照。
- 运营判断广告异常时，需要同时看到产品销售、流量、利润背景。
- 经营快照只作为判断背景，不参与广告规则执行或自动动作。

目标：
- 异常规则生成时，如存在同产品、同周期 `ProductSalesSnapshot`，写入 `evidence_json.product_sales_snapshot`。
- `rule_result_json.evidence.product_sales_snapshot` 与 `evidence_json` 保持一致。
- AI 建议摘要增加“产品经营背景”。
- `source_trace_json.source_context.product_sales_snapshot` 可直接溯源。
- 不改变异常规则阈值、严重级别、建议等级。

允许修改：
- `backend/app/services/rule_service.py`
- `backend/app/services/suggestion_service.py`
- `scripts/smoke_rule_engine_sales_snapshot_context.py`
- `scripts/smoke_ai_suggestion_sales_snapshot_context.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `frontend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_rule_engine_sales_snapshot_context.py
python scripts\smoke_ai_suggestion_sales_snapshot_context.py
python scripts\smoke_rule_engine.py
python scripts\smoke_ai_suggestion.py
python scripts\smoke_real_e2e.py
```

完成依据：
- 新增 `scripts/smoke_rule_engine_sales_snapshot_context.py`，先红灯确认异常证据缺少 `product_sales_snapshot`，再实现转绿。
- 新增 `scripts/smoke_ai_suggestion_sales_snapshot_context.py`，先红灯确认建议摘要和溯源缺少产品经营背景，再实现转绿。
- `backend/app/services/rule_service.py` 为 7 类异常证据挂载同产品、同周期销售表现快照。
- `product_sales_snapshot` 只作为证据背景，不参与规则筛选、阈值、严重级别或建议等级判断。
- `backend/app/services/suggestion_service.py` 的 `evidence_summary` 增加“产品经营背景 销售额 / 订单 / Sessions / 净利”。
- `source_trace_json.source_context.product_sales_snapshot` 可直接溯源经营背景。
- 验收命令已通过：

```powershell
python scripts\smoke_rule_engine_sales_snapshot_context.py
python scripts\smoke_ai_suggestion_sales_snapshot_context.py
python scripts\smoke_rule_engine.py
python scripts\smoke_ai_suggestion.py
python scripts\smoke_real_e2e.py
```

- Python 内存 `compile(...)` 语法检查通过。
- 已重启本地后端 `8001` 并确认前端 `5173` 指向最新后端；重启后 `python scripts\smoke_real_e2e.py` 仍通过。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、SP 同步服务、前端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-060：经营背景前端溯源可读化`。把建议详情里的 `product_sales_snapshot` 做成可读区块，避免运营只看 JSON。

## TASK-060：经营背景前端溯源可读化

状态：已完成。

背景：
- Product Design 指导已明确：当前主工作流应让运营在“异常 / AI 建议详情”中直接看到产品经营背景，而不是只看 JSON。
- TASK-059 已把 `product_sales_snapshot` 写入异常证据和 AI 建议 `source_trace_json`。
- 现在前端建议详情抽屉仍主要展示证据摘要和原始 JSON，经营背景不够直观。

目标：
- 在“异常与 AI 建议详情”抽屉中新增可读的“产品经营背景”区块。
- 优先从 `selectedSuggestion.source_trace_json.source_context.product_sales_snapshot` 读取经营快照。
- 没有建议快照时退回 `selected.evidence_json.product_sales_snapshot`。
- 展示周期、销售额、订单、Sessions、净利、广告花费、广告销售、产品 ACOS。
- JSON 溯源继续保留，但经营背景成为运营主阅读入口。
- 不新增、不暗示任何广告自动执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_suggestion_business_context.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_suggestion_business_context.py
python scripts\smoke_frontend_suggestion_detail.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

完成依据：
- 新增 `scripts/smoke_frontend_suggestion_business_context.py`，先红灯确认前端缺少“产品经营背景”区块，再实现转绿。
- `frontend/src/App.tsx` 新增 `productSalesSnapshotFromSourceTrace`、`productSalesSnapshotFromUnknown` 和 `businessSnapshotMetrics`。
- 建议详情抽屉新增“产品经营背景”区块。
- 优先从 `selectedSuggestion.source_trace_json.source_context.product_sales_snapshot` 读取经营快照。
- 没有 AI 建议快照时，退回 `selected.evidence_json.product_sales_snapshot`。
- 区块展示快照周期、销售额、订单、Sessions、净利、广告花费、广告销售、产品 ACOS。
- 原始 `source_trace_json`、`rule_result_json`、`evidence_json` 继续保留在下方。
- 浏览器只读复查：观察中异常详情抽屉已显示“产品经营背景”；该历史异常无经营快照时显示明确空态。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_suggestion_business_context.py
python scripts\smoke_frontend_suggestion_detail.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 新增 smoke 和原有建议详情 smoke 在沙箱中因 esbuild `spawn EPERM` 失败，已按权限规则非沙箱重跑通过。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：经营背景区块位于 AI 解释 / 证据摘要之后、人工处理按钮之前，符合“先读证据、再人工确认”的工作流；无快照时空态清楚。
- Product Design 复核发现的新问题：驾驶舱显示异常总数 1，但异常队列默认“待处理”为空，切换到“观察中”才可打开历史异常；下一步应收敛状态口径和入口跳转。
- 下一步建议：`TASK-061：异常队列状态口径与入口收敛`。浏览器只读复查发现驾驶舱显示异常总数 1，但异常队列默认“待处理”为空，切换到“观察中”才看到历史异常；需要收敛状态口径和入口跳转。

## TASK-058：market_id 前端隐藏与产品数据全量核验

状态：已完成。

目标：
- 核验销售表现接口 `market_id=1` 产品数据是否已按接口 `reported_total` 全量同步。
- 前端用户可见位置不再显示 `market_id` 或店铺 ID。
- 产品列表 API 返回店铺映射，供前端展示店铺名和站点。
- 不新增任何广告自动执行能力。

允许修改：
- `backend/app/api/routes/products.py`
- `frontend/src/api.ts`
- `frontend/src/App.tsx`
- `scripts/smoke_product_center_market_identity_api.py`
- `scripts/smoke_frontend_product_center_market_identity.py`
- `scripts/smoke_frontend_product_center_identity_fields.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`
- `data/app.db`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- 任何广告执行接口或自动投放能力

完成依据：
- 新增 `scripts/smoke_product_center_market_identity_api.py`，先红灯确认 `/api/products` 未返回 `market` 店铺映射，再转绿。
- 新增 `scripts/smoke_frontend_product_center_market_identity.py`，先红灯确认前端缺少店铺名展示逻辑，再转绿。
- `backend/app/api/routes/products.py` 为产品列表、详情、创建和更新返回 `market` 映射对象。
- `frontend/src/api.ts` 为 `Product` 增加 `market` 类型。
- `frontend/src/App.tsx` 新增店铺展示格式化逻辑，产品中心显示 `rivbos / US`，不再显示 `market_id`。
- 前端移除用户可见的店铺 ID 筛选输入、驾驶舱 `market_id` 标签、异常详情店铺 ID 和产品创建店铺 ID 表单项。
- `python scripts\sync_sales_performance_products.py` 完整同步通过：`pages_synced=21`、`rows_seen=209`、`reported_total=209`。
- 真实库 `product_sales_snapshots` 中 `market_id=1` 当前周期快照为 `209` 条，去重 MSKU 为 `209` 个，与接口 `reported_total` 一致。
- 浏览器真实页面复查产品中心前 5 行店铺列均显示 `rivbos / US 销售表现`，页面不再包含 `market_id` 或 `店铺 / 站点 ID` 文案。
- 验收命令已通过：

```powershell
python scripts\smoke_product_center_market_identity_api.py
python scripts\smoke_frontend_product_center_market_identity.py
python scripts\smoke_frontend_product_center_identity_fields.py
python scripts\sync_sales_performance_products.py
python scripts\smoke_product_sales_snapshot_api.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 已强制重启本地后端 `8001` 并复用前端 `5173`。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、SP 同步服务。
- 未新增任何广告自动执行能力。

## TASK-054：产品中心真实产品优先展示

状态：已完成。

背景：
- 销售表现接口已同步真实产品基础档案。
- `/api/products?market_id=1` 原先按 `Product.id` 返回，前几行仍可能出现历史 DEMO / 测试产品。
- 只展示真实产品还不够，首屏还要优先展示产品名和类目可读的产品。

目标：
- 产品中心首屏优先出现真实销售表现产品。
- 真实产品内部优先出现产品名不等于 MSKU、类目不为 `-` 的记录。
- 不删除历史 DEMO / 广告反推产品，只降低展示优先级。
- 不改前端、不写真实库、不新增广告执行能力。

允许修改：
- `backend/app/api/routes/products.py`
- `scripts/smoke_product_center_real_product_priority.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `frontend/`
- `data/app.db`
- 积加客户端与同步服务
- 任何广告执行接口或自动投放能力

完成依据：
- 新增 `scripts/smoke_product_center_real_product_priority.py`。
- 先红灯确认前 5 行仍包含 `DEMO-ACOS`。
- 收紧 smoke 后确认真实产品内部仍可能出现 `product_name=msku`、类目为 `-` 的弱识别记录。
- `backend/app/api/routes/products.py` 新增产品展示排序：真实销售表现产品优先，可读产品身份优先，DEMO / 测试产品排后。
- 真实 API 返回 `product_count=218`、`real_product_count=208`、`non_real_product_count=2`。
- 产品列表前 5 行已变为真实 ASIN、MSKU、可读产品名和有效类目的产品。
- 验收命令已通过：

```powershell
python scripts\smoke_product_center_real_product_priority.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_e2e.py
python -m py_compile backend\app\api\routes\products.py scripts\smoke_product_center_real_product_priority.py
```

- 未修改 `.env`、`.env.example`、`frontend/.env.local`、前端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。

## TASK-055：产品中心真实身份字段前端收敛

状态：已完成。

背景：
- TASK-054 已让产品列表 API 优先返回真实且可读的产品。
- 产品中心前端仍把 ASIN、MSKU、产品名混在同一列标题中，页面对象口径不够直观。
- 运营需要第一眼看清真实产品身份和数据来源。

目标：
- 产品中心第一列改为 `真实产品身份`。
- 产品身份单元格清楚展示产品名、ASIN、MSKU、类目。
- 店铺 / 站点列明确显示 `market_id` 和 `销售表现` 来源口径。
- 保留已有归因状态、管理归因、筛选、批量应用和保存设置入口。
- 不新增后端字段、不写真实库、不新增广告执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_product_center_identity_fields.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 积加客户端与同步服务
- 任何广告执行接口或自动投放能力

完成依据：
- 新增 `scripts/smoke_frontend_product_center_identity_fields.py`，先红灯确认产品中心缺少真实身份字段层级、类目标签和销售表现来源口径。
- `frontend/src/App.tsx` 将产品中心第一列从 `ASIN / MSKU / 产品名称` 收敛为 `真实产品身份`。
- 产品身份单元格分层展示产品名、ASIN、MSKU、类目。
- 店铺 / 站点列收敛为 `店铺 / 数据来源`，显示 `market_id` 和 `销售表现` 来源标签。
- `frontend/src/styles.css` 新增产品身份单元格样式，支持长 MSKU / 类目换行。
- 保留归因状态和 `管理归因` 入口。
- 未恢复广告活动绑定输入。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_center_identity_fields.py
python scripts\smoke_frontend_product_center_attribution_status.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。

## TASK-056：产品中心真实页面截图验收

状态：已完成。

背景：
- TASK-054 已让产品列表 API 优先返回真实且可读的产品。
- TASK-055 已让产品中心前端展示真实产品身份字段。
- 需要用真实浏览器页面确认运营实际看到的产品中心首屏没有对象误读、视觉拥挤或旧字段残留。

目标：
- 用当前本地服务打开产品中心页面。
- 验证页面可见 `真实产品身份`、`店铺 / 数据来源`、`归因状态` 和 `管理归因`。
- 验证首屏产品行可见产品名、ASIN、MSKU、类目、`market_id`、`销售表现` 来源标签。
- 验证产品中心不再出现 `ASIN / MSKU / 产品名称` 旧列名或广告活动绑定输入。
- 保存一张真实页面截图作为验收证据。
- 不改业务代码、不写真实库、不新增任何广告执行能力。

允许修改：
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`
- `tmp/task056/`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `frontend/`
- `data/app.db`
- 积加客户端与同步服务
- 任何广告执行接口或自动投放能力

完成依据：
- 使用真实本地页面 `http://127.0.0.1:5173/` 进入产品中心。
- 浏览器 DOM 验收确认当前激活页签为 `产品中心`。
- 首屏可见 `真实产品身份`、`店铺 / 数据来源`、`归因状态`、`管理归因`。
- 首屏产品行可见真实产品名、ASIN、MSKU、类目、`market_id 1`、`销售表现` 来源标签。
- 首屏未出现 `DEMO` / `SMOKE` / `TASK` 测试产品。
- 页面未出现 `ASIN / MSKU / 产品名称` 旧列名或广告活动绑定输入。
- 截图已保存到 `tmp/task056/product-center-real-identity.png`，文件大小 `112708` bytes。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_center_identity_fields.py
python scripts\smoke_frontend_product_center_attribution_status.py
python scripts\smoke_real_e2e.py
```

- 未修改业务代码、未写真实库、未新增任何广告执行能力。
- 观察点：当前产品中心表格仍偏宽，存在横向滚动；后续如继续 UI 收敛，应单独开任务处理。
- 下一步建议：`TASK-057：销售表现产品经营快照接入`。用销售表现接口补产品级销售、流量、利润和广告辅助背景证据，先写独立快照，不污染 `products`。

## TASK-057：销售表现产品经营快照接入

状态：已完成。

背景：
- TASK-053 已用销售表现接口同步真实产品基础档案。
- TASK-056 已验收产品中心能清楚展示真实产品身份。
- 产品中心仍缺少产品级经营背景证据，运营很难基于产品销售、流量、利润和广告辅助指标判断广告健康。
- 用户已补充确认销售表现接口基础信息：`/operation/sts/salesAnalysis/page`，限流每 5 秒 1 次。

目标：
- 新增独立产品经营快照表，按 `market_id + msku + period_start + period_end` 保存销售表现指标。
- 继续保持 `products` 只存产品基础档案，不混入销售额、订单、ACOS、Sessions 等表现指标。
- 从销售表现接口同步产品基础档案时，同步写入经营快照。
- 产品列表 API 返回每个产品最近匹配周期的 `sales_snapshot`。
- 产品中心前端展示销售表现快照的最小关键指标，让运营知道产品层面经营背景。
- 不替代 SP 关键词 / 搜索词报表，不生成广告动作，不新增任何广告自动执行能力。

允许修改：
- `backend/app/models/product.py`
- `backend/app/api/routes/products.py`
- `scripts/sync_sales_performance_products.py`
- `scripts/smoke_sales_performance_product_sync.py`
- `scripts/smoke_sales_performance_snapshot_sync.py`
- `scripts/smoke_product_sales_snapshot_api.py`
- `scripts/smoke_frontend_product_sales_snapshot.py`
- `frontend/src/api.ts`
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`
- `data/app.db`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- 任何广告执行接口或自动投放能力

完成依据：
- 新增 `ProductSalesSnapshot` 独立快照表，`products` 表仍只保存产品基础档案。
- `scripts/sync_sales_performance_products.py` 同步产品基础档案时同步写入经营快照。
- 快照字段覆盖 `units_ordered`、`orders`、`sales`、`sessions`、`order_cvr`、`ads_spend`、`ads_sales`、`acos`、`gross_profit`、`net_profit`。
- 产品列表 API 返回 `sales_snapshot`，按 `product_id + period_start + period_end` 匹配当前周期。
- 产品中心新增 `销售表现快照` 列，展示销售额、订单、Sessions、净利、广告辅助和快照周期。
- 真实销售表现完整同步成功：
  - `pages_synced=21`
  - `rows_seen=209`
  - `reported_total=209`
  - `snapshots_created=199`
  - `snapshots_updated=10`
- 真实库当前：
  - `product_sales_snapshots=209`
  - `nonzero_snapshots=167`
  - `market_1_products=218`
- 运行中 API `/api/products?market_id=1&start_date=2026-05-12&end_date=2026-06-10` 返回：
  - `with_snapshot=209`
  - `nonzero_snapshot=167`
  - 样本：`RB831- Full black`，销售额 `4319.06`、订单 `185`、Sessions `831`
- 浏览器真实页面确认产品中心可见 `销售表现快照`，首屏可见非 0 销售额、订单、Sessions 和净利。
- 验收命令已通过：

```powershell
python scripts\smoke_sales_performance_snapshot_sync.py
python scripts\smoke_product_sales_snapshot_api.py
python scripts\smoke_frontend_product_sales_snapshot.py
python scripts\smoke_sales_performance_product_sync.py
python scripts\sync_sales_performance_products.py --max-pages 1
python scripts\sync_sales_performance_products.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- `python -m py_compile ...` 因 Windows 拒绝写 `__pycache__` 未使用；已改用内存 `compile(...)` 完成语法检查并通过。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、SP 同步服务。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-058：经营快照辅助广告健康判断`。把产品级销售、流量、利润背景纳入异常证据和 AI 建议说明，让运营判断广告问题时能同时看到产品经营状态。

## TASK-043：人工确认首条归因后的真实闭环验收

状态：已完成。

目标：
- 验收真实库已经存在人工确认归因规则。
- 验收产品维度搜索词分析已经从未就绪变为就绪。
- 验收产品维度归类聚合组可读，下一步状态进入组级人工判断。
- 证据弹窗明确区分“广告对象证据快照”和“当前将归因到的产品”，避免误解为切换产品后广告证据会变化。
- 仍然不自动记录组级判断，不自动生成复盘，不自动修改广告。

允许修改：
- `frontend/src/App.tsx`
- `scripts/smoke_frontend_attribution_evidence_clarity.py`
- `scripts/smoke_real_post_attribution_workflow.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

完成依据：
- 新增 `scripts/smoke_frontend_attribution_evidence_clarity.py`，约束证据弹窗必须区分广告对象证据快照和当前归因产品。
- 新增 `scripts/smoke_real_post_attribution_workflow.py`，只读验收真实人工归因后的产品维度搜索词链路。
- 真实库 `active_binding_count=1`，当前闭环状态为 `needs_group_decision`。
- 产品维度搜索词已就绪：`bound_search_term_rows=26`，产品维度 `product_group_count=3`。
- `frontend/src/App.tsx` 修复高可信归因候选表“查看证据”按钮旧周期闭包问题。
- 前端证据弹窗新增“广告对象证据快照”和“当前将归因到的产品”。
- 浏览器确认首个候选证据弹窗使用周期 `2026-05-12 至 2026-06-10`，且显示非零证据。
- 验收命令已通过：

```powershell
python scripts/smoke_frontend_attribution_evidence_clarity.py
python scripts/smoke_real_post_attribution_workflow.py
python scripts/smoke_real_workflow_rehearsal_readiness.py
python scripts/smoke_real_search_term_product_readiness.py
python scripts/smoke_real_product_attribution_to_search_terms_readiness.py
python scripts/smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-044：人工记录首个组级判断后的真实复盘验收`。需要运营在前端人工记录一个组级判断后，再验收复盘链路。

## TASK-042：真实人工归因与组级复盘演练引导

状态：已完成。

目标：
- 产品设置页展示“真实闭环演练”。
- 演练步骤包含：确认产品归因、查看产品维度搜索词、记录组级人工判断、复盘组级判断。
- 当前真实库没有产品归因规则时，第一步突出首条高可信归因候选，并提供“查看证据并确认归因”入口。
- 已有归因规则时，第二步可跳转到产品维度搜索词分析。
- 后续步骤只引导到搜索词分析页，不自动记录组级判断，不自动生成复盘。
- 页面明确提示：这是人工演练路径，系统不会自动修改广告。

完成依据：
- `frontend/src/App.tsx` 新增 `realWorkflowRehearsalSteps`，把真实闭环拆成四步。
- 产品设置页新增“真实闭环演练”任务条。
- 第一步复用 `openAttributionEvidence(firstAttributionCandidate.source, firstAttributionCandidate.candidate_product.product_id)`，不会直接保存归因规则。
- 第二步复用 `openProductSearchTermAnalysis(productAdBindings[0])` 进入产品维度搜索词分析。
- 第三步和第四步只通过 `setActiveTab("search_terms")` 切到搜索词分析页，不写库。
- `frontend/src/styles.css` 新增 `real-workflow-rehearsal` 样式，并补充移动端单列展示。
- 新增 `scripts/smoke_frontend_real_workflow_rehearsal.py`，约束前端演练入口、四步流程、人工边界和禁止自动执行标记。
- 新增 `scripts/smoke_real_workflow_rehearsal_readiness.py`，真实 API 只读检查当前闭环状态。
- 当前真实状态：`workflow_status=needs_manual_attribution`，`active_binding_count=0`，`candidate_count=20`，`group_count=10`，`group_decision_count=0`。
- 当前真实 Top 候选：`RBK004-kids sunglasses-广泛 -> RBK004 kids sunglasses`，可信度 `100`，可解锁搜索词 `26` 行。
- 浏览器确认产品设置页可见“真实闭环演练”四步和“查看证据并确认归因”；前端控制台无错误。
- 验收命令已通过：

```powershell
python scripts/smoke_frontend_real_workflow_rehearsal.py
python scripts/smoke_real_workflow_rehearsal_readiness.py
python scripts/smoke_frontend_first_real_attribution_guide.py
python scripts/smoke_frontend_product_dimension_review_checklist.py
python scripts/smoke_real_product_attribution_candidates.py
python scripts/smoke_real_search_term_group_aggregation.py
python scripts/smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-043：人工确认首条归因后的真实闭环验收`。需要运营在前端点击“查看证据并确认归因”并人工确认后，再验收产品维度搜索词、组级判断和复盘链路。

## TASK-041：组级人工判断复盘入口

状态：已完成。

目标：
- 组级人工记录列表提供“查看复盘”入口。
- 后端新增只读复盘接口，根据组级人工判断保存的组快照，对比后续 7 天 / 14 天同组表现。
- 复盘展示判断前后花费、订单、销售、ACOS、CVR 和变化值。
- 复盘结论只做辅助判断：改善、恶化、数据不足、继续观察。
- 前端用抽屉展示组级复盘，明确这是人工判断复盘，不代表系统自动改过广告。
- 不自动执行广告动作，不自动新增 / 否定关键词，不自动调整竞价。

允许修改：
- `backend/app/api/routes/search_terms.py`
- `frontend/src/api.ts`
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_search_term_group_decision_review.py`
- `scripts/smoke_frontend_search_term_group_decision_review.py`
- `scripts/smoke_real_search_term_group_decision_review.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `backend/app/services/search_term_analysis_service.py`
- `backend/app/services/product_attribution_service.py`
- `backend/app/api/routes/products.py`
- `backend/app/models/decision.py`
- `data/app.db`
- 任何广告执行接口或自动投放能力

完成依据：
- `backend/app/api/routes/search_terms.py` 新增 `GET /api/search-terms/group-decisions/{decision_id}/review`。
- 组级复盘根据组级人工记录的 `group_snapshot_json` 生成判断前指标，根据后续 7 天 / 14 天同组 `group_summary` 生成判断后指标。
- 复盘返回 `before_metrics`、`after_metrics`、`delta_metrics`、`result`、`result_label`、`manual_hint`。
- `frontend/src/api.ts` 新增 `SearchTermGroupDecisionReview` 和 `fetchSearchTermGroupDecisionReview`。
- `frontend/src/App.tsx` 组级人工记录表新增“查看复盘”和“14 天”入口。
- `frontend/src/App.tsx` 新增“组级复盘”弹窗，展示人工判断复盘、记录时组快照、后续周期指标和变化值。
- 新增 `scripts/smoke_search_term_group_decision_review.py`、`scripts/smoke_frontend_search_term_group_decision_review.py`、`scripts/smoke_real_search_term_group_decision_review.py`。
- 当前真实库暂无组级人工记录，真实只读验收返回 `skipped_no_group_decisions`，产品归因规则数量不变。
- 浏览器确认搜索词分析页可见“组级人工记录”“查看复盘”，并显示“不代表广告已自动修改”；前端控制台无错误。
- 验收命令已通过：

```powershell
python scripts/smoke_search_term_group_decision_review.py
python scripts/smoke_frontend_search_term_group_decision_review.py
python scripts/smoke_real_search_term_group_decision_review.py
python scripts/smoke_search_term_group_decisions.py
python scripts/smoke_real_search_term_group_aggregation.py
python scripts/smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务、搜索词分析服务、产品归因服务、产品接口、决策模型、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-042：真实人工归因与组级复盘演练引导`。当前真实库仍没有产品归因规则和组级人工记录，系统能力已具备，但还需要把一次真实人工操作路径跑通。

## TASK-039：真实人工归因后的产品维度复核清单

状态：已完成。

目标：
- 产品维度搜索词页选择产品后，展示“产品维度复核清单”。
- 复核清单展示已确认归因规则、已归因搜索词、产品维度归类组、组级人工记录四项。
- 当前真实库未确认归因时，清单显示待处理，并提示先去产品设置页人工确认归因。
- 人工确认归因后，清单能基于现有 readiness、analysis、group decisions 自动变为可复核状态。
- 不自动确认归因，不自动保存归因规则，不自动执行广告动作。

完成依据：
- `frontend/src/App.tsx` 新增 `productReviewChecklist`，只读计算产品维度复核状态。
- 搜索词分析页选择产品后展示“产品维度复核清单”。
- 清单四项状态基于 `searchTermProductReadiness`、`searchTermAnalysis.group_summary`、`searchTermGroupDecisions` 计算。
- `frontend/src/styles.css` 新增 `product-review-checklist` 样式，并在移动端断点改为单列。
- 新增 `scripts/smoke_product_dimension_review_checklist.py`，用内存 SQLite 验证人工归因和组判断后四项复核数据均可读。
- 新增 `scripts/smoke_frontend_product_dimension_review_checklist.py`，约束前端复核清单和禁止自动执行标记。
- 浏览器确认选择 `RBK004 kids sunglasses` 后显示“产品维度复核清单”，四项均为待处理，并显示“待人工确认归因”。
- 真实库只读验收：`active_binding_count=0`、`bound_search_term_rows=0`、`ready=false`，本任务未写入真实归因规则。
- 验收命令已通过：

```powershell
python scripts/smoke_product_dimension_review_checklist.py
python scripts/smoke_frontend_product_dimension_review_checklist.py
python scripts/smoke_real_product_attribution_to_search_terms_readiness.py
python scripts/smoke_real_search_term_product_readiness.py
python scripts/smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端产品接口、后端搜索词接口、同步服务、搜索词分析服务、产品归因服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-040：用户搜索词归类聚合分析可用性复核`。现有聚合已经能看，但要继续验证排序、筛选、证据和人工记录是否能真正提升运营决策效率。

## TASK-040：用户搜索词归类聚合分析可用性复核

状态：已完成。

目标：
- 搜索词分析页显示“归类组优先复核顺序”。
- 优先复核顺序基于现有 `searchTermAnalysis.group_summary` 前端只读计算，不新增后端接口。
- 排序规则保持简单：先看高风险组，再看高转化机会组，再看高花费 / 大词量的数据不足组。
- 每个优先组展示组名、优先级、理由、花费、销售、订单、ACOS、代表搜索词。
- 每个优先组复用现有“查看同组明细”和“记录组判断”入口。
- 不自动生成广告动作，不自动新增 / 否定关键词，不自动调竞价。

完成依据：
- `frontend/src/App.tsx` 新增 `groupPriorityReviewItems`，基于 `searchTermAnalysis.group_summary` 前端只读计算优先复核组。
- 搜索词分析页新增“归类组优先复核顺序”，展示优先级、理由、指标、代表搜索词。
- 优先顺序覆盖“先看高风险组”“高转化机会”“数据不足大池”三类。
- 每类至少露出一个样本，再补充高优先级组，避免真实数据里“数据不足大池”被前 4 个高转化组挤掉。
- 优先组复用 `drillDownSearchTermGroup(item.group)` 和 `openSearchTermGroupDecisionModal(item.group)`，不新增动作能力。
- `frontend/src/styles.css` 新增 `search-term-priority-review` 相关样式，并补充移动端单列展示。
- 新增 `scripts/smoke_frontend_search_term_group_usability.py`，约束前端优先复核区和禁止自动执行标记。
- 新增 `scripts/smoke_real_search_term_group_usability.py`，真实 API 只读验证当前数据能计算非空优先复核组。
- 真实数据周期：`2026-05-12` 至 `2026-06-10`，`group_count=10`，`priority_count=8`。
- 真实数据 Top 优先组：`age_spec:high_acos`，即“年龄 / 人群规格词 / 有单但 ACOS 高”，花费 `35.92`，订单 `6`，词数 `4`。
- 浏览器确认“归类组优先复核顺序”可见，实际展示 5 组，并包含“先看高风险组”“高转化机会”“数据不足大池”三类理由。
- 验收命令已通过：

```powershell
python scripts/smoke_frontend_search_term_group_usability.py
python scripts/smoke_real_search_term_group_usability.py
python scripts/smoke_real_search_term_group_aggregation.py
python scripts/smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端产品接口、后端搜索词接口、同步服务、搜索词分析服务、产品归因服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-041：组级人工判断复盘入口`。归类组已经能排序、下钻和记录判断，下一步应把组级人工判断接入复盘闭环。

## TASK-037：真实人工归因验收引导

状态：已完成。

目标：
- 当真实库没有已确认归因规则但存在高可信候选时，产品设置页优先展示首条真实归因引导。
- 引导展示推荐广告来源、推荐产品、可信度和确认后可解锁影响。
- 引导只能进入现有“查看证据 -> 人工确认”流程，不能直接保存归因规则。
- 用真实 API 只读验证候选存在且 `active_binding_count` 不变。
- 不自动确认归因，不自动保存归因规则，不自动执行广告动作。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_first_real_attribution_guide.py`
- `scripts/smoke_real_first_attribution_guide_readiness.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `backend/app/services/search_term_analysis_service.py`
- `backend/app/services/product_attribution_service.py`
- `backend/app/api/routes/products.py`
- `backend/app/api/routes/search_terms.py`
- `data/app.db`
- 任何广告执行接口或自动投放能力

完成依据：
- `frontend/src/App.tsx` 新增 `firstAttributionCandidate` 和“首条真实归因引导”。
- 引导只在 `productAdBindings.length === 0` 且存在首条候选时展示。
- 引导 CTA 只调用 `openAttributionEvidence(firstAttributionCandidate.source, firstAttributionCandidate.candidate_product.product_id)`。
- `frontend/src/styles.css` 新增 `first-attribution-guide` 相关样式。
- 新增 `scripts/smoke_frontend_first_real_attribution_guide.py` 和 `scripts/smoke_real_first_attribution_guide_readiness.py`。
- 真实库 Top 候选：`RBK004-kids sunglasses-广泛 -> RBK004 kids sunglasses`，可信度 100，可解锁 26 行搜索词。
- 真实库 `active_binding_count=0`，本任务未写入真实归因规则。
- 浏览器确认产品设置页可见“首条真实归因引导”“可解锁搜索词 26 行”“查看证据并确认归因”。
- 浏览器点击引导 CTA 后打开“产品归因证据详情”抽屉，仍显示暂无已确认归因规则。
- 验收命令已通过：

```powershell
python scripts/smoke_frontend_first_real_attribution_guide.py
python scripts/smoke_real_first_attribution_guide_readiness.py
python scripts/smoke_real_product_attribution_candidates.py
python scripts/smoke_real_product_attribution_to_search_terms_readiness.py
python scripts/smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端产品接口、后端搜索词接口、同步服务、搜索词分析服务、产品归因服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。

## TASK-038：产品维度搜索词归类聚合分析

状态：已完成。

目标：
- 搜索词分析页在选择产品后明确进入“产品维度搜索词归类聚合”模式。
- 产品维度模式展示所选产品、产品维度就绪状态、已归因搜索词概览和下一步提示。
- 产品维度模式下的归类组标题和说明必须明确“基于人工确认归因后的产品筛选”。
- 用临时 SQLite 验证：人工确认归因后，产品筛选下 `group_summary` 非空，组级人工记录能带入 `product_id`。
- 用真实 API 只读验证：当前真实库仍未确认归因时不写库、不改变 `active_binding_count`。
- 不自动确认归因，不自动保存归因规则，不自动执行广告动作。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_product_dimension_search_term_groups.py`
- `scripts/smoke_frontend_product_dimension_search_terms.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `backend/app/services/search_term_analysis_service.py`
- `backend/app/services/product_attribution_service.py`
- `backend/app/api/routes/products.py`
- `backend/app/api/routes/search_terms.py`
- `data/app.db`
- 任何广告执行接口或自动投放能力

完成依据：
- 确认后端已有 `/api/search-terms/analysis?product_id=...` 和 `/api/search-terms/product-readiness`，本任务未新增后端接口。
- `frontend/src/App.tsx` 新增 `isProductSearchTermMode`、`selectedSearchTermProduct`、`selectedReadinessProduct`。
- 搜索词分析页选择产品后展示“产品维度搜索词归类聚合”上下文区。
- 产品维度模式下归类组标题变为“产品维度归类聚合组”。
- 产品维度说明明确“基于人工确认归因后的产品筛选”。
- 未就绪时提供“前往产品设置确认归因”入口，仅切换到产品设置页，不保存归因规则。
- `frontend/src/styles.css` 新增 `product-search-term-context` 相关样式。
- 新增 `scripts/smoke_product_dimension_search_term_groups.py` 和 `scripts/smoke_frontend_product_dimension_search_terms.py`。
- 临时库验证人工归因后 `active_binding_count=1`、`bound_search_term_rows=2`、产品维度归类组非空。
- 临时库组级人工记录保存 `product_id`。
- 真实库只读验收：`active_binding_count=0`、`bound_search_term_rows=0`、`ready=false`，本任务未写入真实归因规则。
- 浏览器确认真实页面选择 `RBK004 kids sunglasses` 后显示“产品维度搜索词归类聚合”“产品维度归类聚合组”和“前往产品设置确认归因”。
- 验收命令已通过：

```powershell
python scripts/smoke_product_dimension_search_term_groups.py
python scripts/smoke_frontend_product_dimension_search_terms.py
python scripts/smoke_real_product_attribution_to_search_terms_readiness.py
python scripts/smoke_real_search_term_product_readiness.py
python scripts/smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端产品接口、后端搜索词接口、同步服务、搜索词分析服务、产品归因服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。

## TASK-035：搜索词归类组下钻与人工记录

状态：已完成。

目标：
- 在搜索词分析页，运营可以从“归类聚合组”一键下钻到同组搜索词明细。
- 运营可以对一个归类组记录人工判断：修改后采纳、拒绝、加入观察。
- 组级记录保存当时的组快照、筛选条件、人工备注、处理人和观察周期。
- 页面展示组级人工记录列表，明确这些记录不代表广告已自动修改。
- 不自动执行广告动作，不自动新增 / 否定关键词，不自动调整竞价。

完成依据：
- 新增 `search_term_group_decisions` 本地组级人工判断记录表。
- 新增 `POST /api/search-terms/group-decisions` 和 `GET /api/search-terms/group-decisions`。
- 组级判断保存当前归类组快照、周期、筛选条件、处理人、备注和观察周期。
- 前端“归类聚合组”新增“查看同组明细”和“记录组判断”。
- 前端新增“组级人工记录”表，明确只记录人工判断，不代表广告已自动修改。
- 浏览器确认搜索词分析页可见组下钻、组判断入口，并可打开组级人工判断弹窗。
- 真实 API 下钻 Top 组 `age_spec:data_insufficient` 返回 92 行同组搜索词，`active_binding_count=0` 不变。
- 验收命令已通过：

```powershell
python scripts/smoke_search_term_group_decisions.py
python scripts/smoke_frontend_search_term_group_decisions.py
python scripts/smoke_real_search_term_group_drilldown.py
python scripts/smoke_search_term_group_aggregation.py
python scripts/smoke_real_search_term_group_aggregation.py
python scripts/smoke_real_e2e.py
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务、搜索词分析服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-036：产品归因人工确认后打通产品维度搜索词组分析`。

## TASK-036：产品归因确认后联动产品维度搜索词分析

状态：已完成。

目标：
- 产品设置页已确认归因规则列表提供“查看搜索词分析”入口。
- 点击入口后切到搜索词分析页，并带入对应 `product_id`、`market_id` 和当前周期。
- 用临时 SQLite 验证：人工确认归因规则后，搜索词 `product_id` 被回填，产品维度搜索词就绪状态变为 ready，且产品筛选下的 `group_summary` 非空。
- 用真实 API 只读验证：当前真实库仍未确认归因时不写库、不改变 `active_binding_count`。
- 不自动确认归因，不自动保存归因规则，不自动执行广告动作。

完成依据：
- `frontend/src/App.tsx` 新增 `openProductSearchTermAnalysis` 联动函数。
- 已确认归因规则列表新增“查看搜索词分析”入口。
- 点击入口会带入 `product_id`、`market_id`、当前周期，并切到搜索词分析页。
- 产品设置页新增提示：人工确认归因后，可查看该产品搜索词归类组。
- 临时库验证人工确认归因后搜索词 `product_id` 被回填，产品维度搜索词就绪状态变为 ready。
- 临时库验证产品筛选下 `/api/search-terms/analysis` 返回非空 `group_summary`。
- 真实库只读验收 `active_binding_count=0`，本任务没有写入真实归因规则。
- 浏览器确认产品设置页可见已确认归因规则区和联动提示。
- 验收命令已通过：

```powershell
python scripts/smoke_product_attribution_to_search_terms.py
python scripts/smoke_frontend_product_attribution_to_search_terms.py
python scripts/smoke_real_product_attribution_to_search_terms_readiness.py
python scripts/smoke_real_search_term_product_readiness.py
python scripts/smoke_real_search_term_group_drilldown.py
python scripts/smoke_real_e2e.py
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端产品接口、后端搜索词接口、同步服务、搜索词分析服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-037：真实人工归因验收引导`。

## TASK-035：搜索词归类组下钻与人工记录

状态：进行中。

设计收敛：
- 方案 A：只做前端筛选下钻，不新增记录。太轻，不能形成复盘闭环。
- 方案 B：复用单个搜索词候选记录表保存组级判断。实现快，但语义混乱。
- 方案 C：新增独立的组级人工判断记录。推荐，边界清楚，仍保持最小实现。

目标：
- 在搜索词分析页，运营可以从“归类聚合组”一键下钻到同组搜索词明细。
- 运营可以对一个归类组记录人工判断：修改后采纳、拒绝、加入观察。
- 组级记录保存当时的组快照、筛选条件、人工备注、处理人和观察周期。
- 页面展示组级人工记录列表，明确这些记录不代表广告已自动修改。
- 不自动执行广告动作，不自动新增 / 否定关键词，不自动调整竞价。

允许修改：
- `backend/app/models/decision.py`
- `backend/app/api/routes/search_terms.py`
- `frontend/src/api.ts`
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_search_term_group_decisions.py`
- `scripts/smoke_frontend_search_term_group_decisions.py`
- `scripts/smoke_real_search_term_group_drilldown.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `backend/app/services/search_term_analysis_service.py`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_search_term_group_decisions.py
python scripts/smoke_frontend_search_term_group_decisions.py
python scripts/smoke_real_search_term_group_drilldown.py
python scripts/smoke_search_term_group_aggregation.py
python scripts/smoke_real_search_term_group_aggregation.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- 后端支持 `POST /api/search-terms/group-decisions` 和 `GET /api/search-terms/group-decisions`。
- 提交组级人工判断时，后端重新计算当前 `group_summary` 并保存匹配组快照。
- 支持 `adopt_with_changes`、`reject`、`observe` 三种组级判断，必填项校验与搜索词候选记录一致。
- 前端“归类聚合组”提供“查看同组明细”和“记录组判断”入口。
- 前端显示“组级人工记录”，并提示只记录人工判断，不代表广告已自动修改。
- 真实 API 下钻筛选能返回同组搜索词，且不改变产品归因规则数量。
- API 和前端源码不包含任何自动执行广告动作字段或入口。

## TASK-033：人工确认核心产品归因规则

状态：已拆分为 `TASK-033：核心归因候选确认工作台`。

目标：
- 运营在“产品设置 -> 高可信归因候选”中查看证据。
- 对核心 Campaign / Ad Group 人工确认归因规则。
- 归因规则确认后，让后续搜索词按产品维度归类聚合有可靠归属。

前提：
- 不自动保存归因规则。
- 不自动回填产品归因。
- 不自动执行任何广告动作。

## TASK-033：核心归因候选确认工作台

状态：已完成。

目标：
- 让运营优先看到最值得人工确认的产品归因候选。
- 在候选队列里展示确认后可解锁的搜索词行数、花费、销售和订单影响。
- 前端明确标出“建议优先确认”，但仍只能进入“查看证据 -> 人工确认归因”流程。
- 不自动保存归因规则，不自动回填产品归因，不自动执行广告动作。

允许修改：
- `backend/app/services/product_attribution_service.py`
- `backend/app/api/routes/products.py`
- `frontend/src/api.ts`
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_product_attribution_confirmation_workbench.py`
- `scripts/smoke_frontend_product_attribution_confirmation_workbench.py`
- `scripts/smoke_real_product_attribution_confirmation_workbench.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_product_attribution_confirmation_workbench.py
python scripts/smoke_frontend_product_attribution_confirmation_workbench.py
python scripts/smoke_real_product_attribution_confirmation_workbench.py
python scripts/smoke_real_product_attribution_candidates.py
python scripts/smoke_real_search_term_product_readiness.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- 产品归因候选包含 `priority_rank`、`priority_label` 和 `unlock_impact`。
- `unlock_impact` 至少包含可解锁搜索词行数、花费、销售、订单和 ACOS。
- 真实库 Top 候选仍为只读候选，验收前后 `active_binding_count` 不变。
- 前端高可信归因候选面板显示“建议优先确认”和确认后影响。
- 前端候选仍只能进入查看证据流程，不能绕过人工确认。
- API 和前端源码不包含任何自动执行广告动作字段或入口。

完成依据：
- 后端产品归因候选新增 `priority_rank`、`priority_label` 和 `unlock_impact`。
- `unlock_impact` 包含可解锁搜索词行数、花费、销售、订单、ACOS 和 CVR。
- 前端产品设置页候选队列新增“建议优先确认”和“确认后影响”。
- 产品设置页默认周期错位时优先使用最近成功同步周期，避免真实候选显示为空。
- 浏览器确认真实页面显示 Top 候选：`RBK004-kids sunglasses-广泛 -> RBK004 kids sunglasses`，并展示 26 行搜索词、花费 150.47、销售 545.79、订单 52、ACOS 27.6%。
- 真实库 `active_binding_count=0`，本任务未自动保存归因规则或回填 `product_id`。
- 验收命令已通过：

```powershell
python scripts/smoke_product_attribution_confirmation_workbench.py
python scripts/smoke_frontend_product_attribution_confirmation_workbench.py
python scripts/smoke_real_product_attribution_confirmation_workbench.py
python scripts/smoke_real_product_attribution_candidates.py
python scripts/smoke_real_search_term_product_readiness.py
python scripts/smoke_real_e2e.py
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。

## TASK-044：人工记录首个组级判断后的真实复盘验收

状态：已完成。

目标：
- 验收真实库已存在人工保存的首条组级判断。
- 验收组级复盘接口能基于该判断返回复盘指标和结果。
- 验收复盘过程只读，不改变产品归因规则，不自动执行广告动作。
- 明确当前首条组级判断是全局组判断，尚不是产品级组判断。

允许修改：
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `frontend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_real_search_term_group_decision_review.py
python scripts\smoke_real_workflow_rehearsal_readiness.py
python scripts\smoke_real_post_attribution_workflow.py
python scripts\smoke_real_e2e.py
```

完成依据：
- 真实库已存在首条组级人工判断：`decision_id=1`。
- 首条组级人工判断为全局组判断：`product_id=null`，不误报为产品级组判断。
- 组级复盘接口可读，返回 `result=data_pending`。
- `python scripts\smoke_real_search_term_group_decision_review.py` 通过。
- `python scripts\smoke_real_workflow_rehearsal_readiness.py` 通过，`workflow_status=ready_for_review`，`group_decision_count=1`。
- `python scripts\smoke_real_post_attribution_workflow.py` 通过，但产品级 `group_decision_count=0`。
- `python scripts\smoke_real_e2e.py` 通过。
- 复盘验收未改变产品归因规则数量。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、前端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-045：产品级组判断入口口径收敛`。当前前端容易让运营在产品维度已就绪时保存成全局组判断，需要明确全局 / 产品级口径。

## TASK-045：对象口径清晰化

状态：已完成。

目标：
- 产品设置页明确区分产品对象、广告来源对象、归因规则对象。
- 在产品维度搜索词分析中明确“记录组判断将保存为当前产品级判断”。
- 组级人工判断弹窗明确当前记录口径：全局组判断或产品级组判断。
- 组级人工记录列表说明当前筛选口径，避免把 `product_id=null` 的全局判断误读为产品级判断。
- 产品维度复核清单只在存在当前产品的组级判断时显示组级人工记录已就绪。
- 不改后端，不改真实数据库，不自动生成任何人工判断。

允许修改：
- `frontend/src/App.tsx`
- `scripts/smoke_frontend_object_scope_clarity.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_object_scope_clarity.py
python scripts\smoke_frontend_search_term_group_decisions.py
python scripts\smoke_frontend_product_dimension_review_checklist.py
python scripts\smoke_real_post_attribution_workflow.py
python scripts\smoke_real_search_term_group_decision_review.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

完成依据：
- 产品设置页新增对象口径说明：当前对象：产品、当前对象：广告来源、当前对象：归因规则。
- 搜索词分析页在产品维度模式下明确后续记录会作为产品级组判断。
- 组级人工判断弹窗新增当前记录口径：产品级组判断或全局组判断。
- 组级人工记录列表新增当前列表口径。
- 组级人工记录表新增记录口径列，区分产品级组判断和全局组判断。
- 产品维度复核清单将组级人工记录文案收敛为当前产品级记录。
- 新增 `scripts/smoke_frontend_object_scope_clarity.py`，先红灯后转绿。
- `python scripts\smoke_frontend_object_scope_clarity.py` 通过。
- `python scripts\smoke_frontend_search_term_group_decisions.py` 通过。
- `python scripts\smoke_frontend_product_dimension_review_checklist.py` 通过。
- `python scripts\smoke_real_post_attribution_workflow.py` 通过，产品级 `group_decision_count=0` 仍如实保留。
- `python scripts\smoke_real_search_term_group_decision_review.py` 通过，已有全局 `decision_id=1` 可复盘。
- `python scripts\smoke_real_e2e.py` 通过。
- `frontend` 目录 `npm run build` 通过。
- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-046：人工保存产品级组判断后的真实复盘验收`。对象口径已澄清，需要运营在产品维度搜索词分析下重新保存一条产品级组判断后，再验收产品级复盘闭环。

## TASK-046：产品级组判断待办入口收敛

状态：已完成。

目标：
- 当产品维度搜索词分析已就绪、存在产品维度归类组、但当前产品级组判断为 0 时，页面明确展示“待记录产品级组判断”。
- 该入口只能打开已有组级人工判断弹窗，仍由运营填写备注并人工确认。
- 入口使用当前产品筛选下的 `group_summary[0]`，不写库、不自动保存、不生成任何广告动作。
- 保留已有“归类组优先复核顺序”和“归类聚合组”里的记录入口。
- 不改后端，不改真实数据库。

允许修改：
- `frontend/src/App.tsx`
- `scripts/smoke_frontend_product_group_decision_todo.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_product_group_decision_todo.py
python scripts\smoke_frontend_object_scope_clarity.py
python scripts\smoke_frontend_search_term_group_decisions.py
python scripts\smoke_frontend_product_dimension_review_checklist.py
python scripts\smoke_real_post_attribution_workflow.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 前端源码包含“待记录产品级组判断”和“记录产品级组判断”。
- 待办入口只在产品维度模式下基于 `isProductSearchTermMode`、`searchTermAnalysis?.group_summary.length`、`searchTermGroupDecisions.length` 判断。
- 点击入口调用 `openSearchTermGroupDecisionModal(searchTermAnalysis.group_summary[0])`，不直接调用提交 API。
- 提交组级判断时仍使用 `product_id: searchTermProductId ?? undefined`。
- 真实库产品级 `group_decision_count=0` 的事实仍保留，不能伪造验收通过。
- 不新增任何自动执行广告动作字段或入口。

完成依据：
- 产品维度复核清单新增“待记录产品级组判断”提示。
- 待办入口只在产品维度模式、存在产品归类组且当前产品级组判断为 0 时展示。
- 点击“记录产品级组判断”只打开已有人工判断弹窗，不直接提交判断。
- 提交组级判断仍使用 `product_id: searchTermProductId ?? undefined`。
- 新增 `scripts/smoke_frontend_product_group_decision_todo.py`，先红灯后转绿。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_group_decision_todo.py
python scripts\smoke_frontend_object_scope_clarity.py
python scripts\smoke_frontend_search_term_group_decisions.py
python scripts\smoke_frontend_product_dimension_review_checklist.py
python scripts\smoke_real_post_attribution_workflow.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 真实库产品级 `group_decision_count=0` 仍如实保留。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-047：人工保存产品级组判断后的真实复盘验收`。需要运营点击“记录产品级组判断”并人工保存后，再验收产品级复盘闭环。

## TASK-047：真实闭环演练产品级入口收敛

状态：已完成。

目标：
- 产品设置页“真实闭环演练”的“记录组级人工判断”改为“记录产品级组判断”。
- 点击第 3 步时复用 `openProductSearchTermAnalysis(productAdBindings[0])`，直接带入已归因产品，而不是只切到搜索词分析页。
- 第 3 步状态基于当前产品级记录口径，不把全局组判断误报为产品级已记录。
- 第 4 步在可复盘前也优先进入当前产品维度搜索词分析，便于查看产品级组记录。
- 不写真实库，不自动保存组级判断，不新增任何广告执行能力。

允许修改：
- `frontend/src/App.tsx`
- `scripts/smoke_frontend_real_workflow_product_group_entry.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_real_workflow_product_group_entry.py
python scripts\smoke_frontend_product_group_decision_todo.py
python scripts\smoke_frontend_object_scope_clarity.py
python scripts\smoke_real_post_attribution_workflow.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

完成依据：
- 产品设置页“真实闭环演练”的第 3 步改为“记录产品级组判断”。
- 第 3 步点击后复用 `openProductSearchTermAnalysis(productAdBindings[0])`，直接带入已归因产品的搜索词分析。
- 第 3 步状态基于当前产品维度记录数，不再把全局组判断误报为产品级已记录。
- 第 4 步“查看产品级组记录”也优先带入已归因产品。
- 新增 `scripts/smoke_frontend_real_workflow_product_group_entry.py`，先红灯后转绿。
- `python scripts\smoke_frontend_real_workflow_product_group_entry.py` 通过。
- `python scripts\smoke_frontend_product_group_decision_todo.py` 通过。
- `python scripts\smoke_frontend_object_scope_clarity.py` 通过。
- `python scripts\smoke_real_post_attribution_workflow.py` 通过，真实产品级 `group_decision_count=0` 仍如实保留。
- `python scripts\smoke_real_e2e.py` 通过。
- `frontend` 目录 `npm run build` 通过。
- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-048：人工保存产品级组判断后的真实复盘验收`。需要运营在产品维度下人工保存产品级组判断后再验收。

## TASK-048：当前 MVP 缺口基线复核收敛

状态：已完成。

目标：
- `smoke_current_mvp_baseline.py` 不再把下一步误报为“产品与广告归因闭环”。
- 基线输出新增当前产品归因后闭环状态：`workflow_status`、`product_group_count`、`product_group_decision_count`。
- 基线缺口明确列出：
  - 产品级组判断未保存；
  - 真实库暂无异常人工处理记录；
  - 真实库暂无复盘记录。
- 不写库，不改后端，不新增任何广告执行能力。

允许修改：
- `scripts/smoke_current_mvp_baseline.py`
- `scripts/smoke_current_mvp_gap_baseline.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `frontend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_current_mvp_gap_baseline.py
python scripts\smoke_current_mvp_baseline.py
python scripts\smoke_real_post_attribution_workflow.py
python scripts\smoke_real_e2e.py
```

完成依据：
- `smoke_current_mvp_baseline.py` 新增 `current_workflow` 输出，包含 `workflow_status`、`active_binding_count`、`product_id`、`bound_search_term_rows`、`product_group_count`、`product_group_decision_count`。
- 基线脚本当前输出 `workflow_status=needs_group_decision`。
- 基线脚本当前输出 `product_group_count=3`、`product_group_decision_count=0`。
- `baseline_gaps` 明确列出产品级组判断、人工处理记录、复盘记录三个缺口。
- `next_task_recommended` 已从旧的“产品与广告归因闭环”更新为“人工保存产品级组判断后的真实复盘验收”。
- 新增 `scripts/smoke_current_mvp_gap_baseline.py`，先红灯后转绿。
- `python scripts\smoke_current_mvp_gap_baseline.py` 通过。
- `python scripts\smoke_current_mvp_baseline.py` 通过。
- `python scripts\smoke_real_post_attribution_workflow.py` 通过。
- `python scripts\smoke_real_e2e.py` 通过。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、前端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-049：人工保存产品级组判断后的真实复盘验收`。需要运营先人工保存产品级组判断。

## TASK-049：产品中心与广告归因页签拆分

状态：已完成。

目标：
- 将原“产品设置”入口改为“产品中心”，只承载产品资料、产品目标、规则门槛、库存和产品层面的指标。
- 新增“广告归因”页签，承载真实闭环演练、高可信归因候选、未归因广告数据池和已确认归因规则。
- 产品表不再展示“绑定广告活动”输入列，避免把广告活动误认为产品基础字段。
- 搜索词分析里的“前往产品设置确认归因”入口改为进入“广告归因”。
- 不改后端、不写真实库、不新增任何广告执行能力。

允许修改：
- `frontend/src/App.tsx`
- `scripts/smoke_frontend_product_attribution_navigation_split.py`
- `scripts/smoke_product_settings.py`
- `scripts/smoke_frontend_product_dimension_search_terms.py`
- `scripts/smoke_frontend_search_term_product_readiness.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_product_attribution_navigation_split.py
python scripts\smoke_frontend_object_scope_clarity.py
python scripts\smoke_frontend_product_dimension_search_terms.py
python scripts\smoke_frontend_search_term_product_readiness.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_post_attribution_workflow.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

完成依据：
- `frontend/src/App.tsx` 将原“产品设置”拆为“产品中心”和“广告归因”两个一级页签。
- “广告归因”页签承载真实闭环演练、高可信归因候选、未归因广告数据池和已确认归因规则。
- “产品中心”页签只保留产品基础设置表，不再展示“绑定广告活动”输入列。
- 搜索词产品维度未就绪入口已改为“前往广告归因确认归因”，并切换到 `attribution` 页签。
- 新增 `scripts/smoke_frontend_product_attribution_navigation_split.py`，先红灯后转绿。
- 更新 `scripts/smoke_product_settings.py`、`scripts/smoke_frontend_product_dimension_search_terms.py`、`scripts/smoke_frontend_search_term_product_readiness.py` 的前端口径。
- `python scripts\smoke_frontend_product_attribution_navigation_split.py` 通过。
- `python scripts\smoke_frontend_object_scope_clarity.py` 通过。
- `python scripts\smoke_frontend_product_dimension_search_terms.py` 通过。
- `python scripts\smoke_frontend_search_term_product_readiness.py` 通过。
- `python scripts\smoke_product_settings.py` 通过。
- `python scripts\smoke_real_post_attribution_workflow.py` 通过，真实产品级 `group_decision_count=0` 仍如实保留。
- `python scripts\smoke_real_e2e.py` 通过。
- `frontend` 目录 `npm run build` 通过。
- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-050：人工保存产品级组判断后的真实复盘验收`。需要运营先在产品维度下人工保存产品级组判断。

## TASK-050：产品中心归因状态入口补齐

状态：已完成。

目标：
- 产品中心表格新增“归因状态”列。
- 每个产品显示“已归因 N”或“待归因”。
- 每个产品提供“管理归因”按钮，点击后带着当前产品进入“广告归因”页签。
- 不恢复“绑定广告活动”输入列。
- 不改后端、不写真实库、不新增任何广告执行能力。

允许修改：
- `frontend/src/App.tsx`
- `scripts/smoke_frontend_product_center_attribution_status.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_product_center_attribution_status.py
python scripts\smoke_frontend_product_attribution_navigation_split.py
python scripts\smoke_product_settings.py
python scripts\smoke_real_post_attribution_workflow.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

完成依据：
- `frontend/src/App.tsx` 的产品中心表格新增“归因状态”列。
- 归因状态基于 `productAdBindings` 按 `product_id` 统计当前产品归因规则数量。
- 产品行显示“已归因 N”或“待归因”。
- “管理归因”按钮会设置 `attributionProductId` 并切换到 `attribution` 页签。
- 新增 `scripts/smoke_frontend_product_center_attribution_status.py`，先红灯后转绿。
- `python scripts\smoke_frontend_product_center_attribution_status.py` 通过。
- `python scripts\smoke_frontend_product_attribution_navigation_split.py` 通过。
- `python scripts\smoke_product_settings.py` 通过。
- `python scripts\smoke_real_post_attribution_workflow.py` 通过，真实产品级 `group_decision_count=0` 仍如实保留。
- `python scripts\smoke_real_e2e.py` 通过。
- `frontend` 目录 `npm run build` 通过。
- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-051：人工保存产品级组判断后的真实复盘验收`。需要运营先在产品维度下人工保存产品级组判断。

## TASK-051：销售表现接口产品源验证

状态：已完成。

背景：
- 用户明确当前讨论对象是积加开放平台 `统计 > 销售表现` 接口。
- 产品中心当前数据对象仍不够清晰，需要先确认该接口能否提供产品基础字段和经营表现字段。
- 前端“产品中心直达搜索词”入口暂缓，避免在产品主数据来源未收敛前继续堆 UI。

目标：
- 只读读取官方销售表现接口文档详情。
- 验证接口路径、请求参数和响应字段是否能支撑产品中心第一版数据源。
- 输出可用于本项目的数据分组：产品基础档案、销售表现、流量转化、广告辅助、利润成本、库存生命周期、促销。
- 不调用真实业务数据接口，不写真实库，不改前端，不新增任何广告执行能力。

允许修改：
- `scripts/smoke_sales_performance_product_source.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `frontend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_sales_performance_product_source.py
python scripts\smoke_real_e2e.py
```

完成依据：
- 新增 `scripts/smoke_sales_performance_product_source.py`，只读读取官方文档详情 `https://open.gerpgo.com/api/openAdmin/doc/detail?id=3375`。
- 验证销售表现接口 `apiUrl=/operation/sts/salesAnalysis/page`，底层 `erpUrl=/v2/statistics/openPlatform/salesAnalysis`。
- 验证请求字段包含 `groupByType`、`showCurrencyType`、`beginDate`、`endDate`、`page`、`pagesize`。
- 验证 `groupByType` 文档说明 MSKU 维度应传 `seller_sku`。
- 验证 `data.rows` 共 177 个字段，覆盖产品基础档案、销售表现、流量转化、广告辅助、利润成本、库存生命周期、促销影响。
- 明确第一版映射：`products.asin <- asin`、`products.msku <- msku`、`products.sku <- sku`、`products.product_name <- productName`、`products.image_url <- imgUrl`、`products.brand <- brand`、`products.category <- category`、`products.market_id <- marketId`。
- 明确边界：销售表现接口用于产品基础档案、产品经营表现快照和广告判断背景证据，不替代 SP 关键词 / 搜索词报表，不做广告自动执行。
- `python scripts\smoke_sales_performance_product_source.py` 通过。
- `python scripts\smoke_real_e2e.py` 通过。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、前端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-052：销售表现接口真实数据只读探测`。等待积加限流恢复后，用 `groupByType=seller_sku`、`pagesize=10` 只读拉取 1 页真实 rows，再决定是否写入产品基础档案。

## TASK-052：销售表现接口真实数据只读探测

状态：已完成。

背景：
- TASK-051 已确认官方 `销售表现` 文档字段足够支撑产品基础档案和经营表现快照。
- 用户补充截图显示该接口限流规则为：每 5 秒 1 次。
- 下一步只做真实 rows 探测，不写库、不改前端，先确认真实返回字段和值是否与文档一致。

目标：
- 使用真实积加配置只读调用 `/operation/sts/salesAnalysis/page`。
- 请求参数使用 `groupByType=seller_sku`、`pagesize=10`。
- 调用前等待 5.2 秒，符合每 5 秒 1 次限流。
- 验证真实 rows 至少包含产品基础字段和经营表现字段。
- 输出样本字段和值概览，不保存原始响应，不写入 `data/app.db`。
- 不新增任何广告执行能力。

允许修改：
- `scripts/smoke_sales_performance_real_probe.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `frontend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_sales_performance_real_probe.py
python scripts\smoke_real_e2e.py
```

完成依据：
- 新增 `scripts/smoke_sales_performance_real_probe.py`。
- 探针使用真实积加配置只读调用 `/operation/sts/salesAnalysis/page`。
- 请求参数为 `groupByType=seller_sku`、`pagesize=10`、`marketId=1`、周期 `2026-05-12` 至 `2026-06-10`。
- 探针调用前固定等待 `5.2` 秒，符合用户截图补充的“每 5 秒 1 次”限流规则。
- 真实接口返回成功：`row_count=10`、`total=209`、`page=1`。
- 真实样本包含 `asin`、`msku`、`sku`、`productName`、`marketId`、`marketName`。
- 真实样本字段覆盖销售、流量、广告辅助三类表现字段。
- `python scripts\smoke_sales_performance_real_probe.py` 通过。
- `python scripts\smoke_real_e2e.py` 通过。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、前端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-053：销售表现产品基础档案同步`。按 `market_id + msku` 最小 upsert 到 `products`，只写产品基础字段，销售表现指标先不混入产品表。

## TASK-053：销售表现产品基础档案同步

状态：已完成。

背景：
- TASK-052 已确认销售表现真实接口可返回 `seller_sku` 维度产品数据。
- 当前真实接口周期 `2026-05-12` 至 `2026-06-10` 返回 `total=209`。
- 产品中心需要真实产品基础档案，不能继续依赖广告活动 / 广告组名称反推产品。

目标：
- 新增最小同步脚本，从销售表现接口读取真实产品基础字段。
- 按 `market_id + msku` upsert 到 `products`。
- 只写产品基础字段：`asin`、`msku`、`sku`、`product_name`、`image_url`、`brand`、`category`、`market_id`。
- 不把销售额、订单、ACOS、Sessions 等表现指标写入 `products`。
- 保持接口限流：分页请求间隔不小于 `5.2` 秒。
- 不改前端、不新增广告执行能力。

允许修改：
- `scripts/sync_sales_performance_products.py`
- `scripts/smoke_sales_performance_product_sync.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`
- `data/app.db`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `frontend/`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_sales_performance_product_sync.py
python scripts\sync_sales_performance_products.py --max-pages 1
python scripts\smoke_product_settings.py
python scripts\smoke_real_e2e.py
```

完成依据：
- 新增 `scripts/smoke_sales_performance_product_sync.py`，用临时 SQLite 验证 `market_id + msku` upsert、字段映射和不写表现指标。
- 新增 `scripts/sync_sales_performance_products.py`，从销售表现接口同步产品基础档案。
- 同步脚本使用 `/operation/sts/salesAnalysis/page`、`groupByType=seller_sku`、`pagesize=10`。
- 同步脚本分页间隔 `PAGE_DELAY_SECONDS=5.2`，符合销售表现接口每 5 秒 1 次限流。
- 同步脚本只写 `asin`、`msku`、`sku`、`product_name`、`image_url`、`brand`、`category`、`market_id`。
- 真实完整同步已完成：`pages_synced=21`、`rows_seen=209`、`reported_total=209`。
- 真实完整同步结果：`created=199`、`updated=10`、`skipped=0`。
- 本地 API `/api/products?market_id=1` 当前返回 `count=218`，其中包含销售表现接口同步的真实产品。
- `python scripts\smoke_sales_performance_product_sync.py` 通过。
- `python scripts\sync_sales_performance_products.py --max-pages 1` 通过。
- `python scripts\sync_sales_performance_products.py` 完整同步通过。
- `python scripts\smoke_product_settings.py` 通过。
- `python scripts\smoke_real_e2e.py` 通过。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、前端。
- 未新增任何广告自动执行能力。
- 下一步建议：`TASK-055：产品中心真实身份字段前端收敛`。产品列表 API 已优先返回真实且可读的产品；下一步可在前端产品中心强化 ASIN、MSKU、产品名、类目、店铺来源的展示口径。

## TASK-034：搜索词归类聚合分析规划与目标设定

状态：已收敛为 `TASK-034：搜索词归类聚合组`。

目标：
- 先明确用户搜索词归类聚合分析的业务目标、归类层级、聚合口径和页面输出。
- 避免在产品归因未确认时直接输出误导性的产品维度结论。
- 保持只读分析和人工确认边界，不新增任何广告自动执行能力。

## TASK-034：搜索词归类聚合组

状态：已完成。

头脑风暴结论：
- 方案 A：在现有搜索词分析上补“归类聚合组”，把语义分类和表现分类组合成运营可处理分组。推荐，最小可用。
- 方案 B：等人工产品归因后做产品维度完整聚合。结论更准，但当前 `active_binding_count=0`，会卡住。
- 方案 C：引入 AI 聚类。长期更强，但第一版不可控，不符合 KISS。

目标：
- 在 `/api/search-terms/analysis` 返回 `group_summary`，按“语义分类 + 表现分类”聚合搜索词。
- 每个聚合组展示词数、点击、花费、订单、销售、ACOS、CVR、代表搜索词和人工判断提示。
- 修正表现分类：有订单的搜索词应优先按 ACOS 判断，不应因为点击 / 花费低直接归为“数据不足”。
- 前端搜索词分析页新增“归类聚合组”区块，让运营先看组，再看明细。
- 不写库，不自动生成广告动作，不自动新增 / 否定关键词，不自动调整竞价。

允许修改：
- `backend/app/services/search_term_analysis_service.py`
- `backend/app/api/routes/search_terms.py`
- `frontend/src/api.ts`
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_search_term_group_aggregation.py`
- `scripts/smoke_frontend_search_term_group_aggregation.py`
- `scripts/smoke_real_search_term_group_aggregation.py`
- `开发任务清单.md`
- `当前任务.md`
- `project_state.json`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/app/services/gerpgo_client.py`
- `backend/app/services/sync_service.py`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts/smoke_search_term_group_aggregation.py
python scripts/smoke_frontend_search_term_group_aggregation.py
python scripts/smoke_real_search_term_group_aggregation.py
python scripts/smoke_search_term_analysis.py
python scripts/smoke_frontend_search_terms.py
python scripts/smoke_real_e2e.py
npm run build
```

成功标准：
- `/api/search-terms/analysis` 返回 `group_summary`。
- `group_summary` 每组包含 `group_key`、`group_label`、`semantic_category`、`performance_status`、指标汇总、`representative_terms`、`manual_hint`。
- 有订单且 ACOS 不高于目标的低点击搜索词归为 `high_conversion`，不是 `data_insufficient`。
- 真实库能返回非空 `group_summary`，且不改变归因规则数量。
- 前端显示“归类聚合组”和代表搜索词。
- API 和前端源码不包含任何自动执行广告动作字段或入口。

完成依据：
- `backend/app/services/search_term_analysis_service.py` 新增 `group_summary` 聚合输出。
- `group_summary` 按 `semantic_category + performance_status` 聚合，包含指标汇总、代表搜索词和人工判断提示。
- 修正表现分类顺序：有订单搜索词优先按 ACOS 判断，不再因为点击 / 花费低直接归为数据不足。
- `frontend/src/api.ts` 新增 `SearchTermGroupSummary`。
- `frontend/src/App.tsx` 搜索词分析页新增“归类聚合组”，并在默认周期错位时优先使用最近成功同步周期。
- 浏览器确认真实页面显示 10 个归类聚合组和代表搜索词。
- 真实库 Top 聚合组：`age_spec:data_insufficient`，92 个词、花费 101.99、代表词包含 `baby sunglasses 0-6 months`。
- 真实库 `active_binding_count=0`，本任务未改变产品归因规则数量。
- 验收命令已通过：

```powershell
python scripts/smoke_search_term_group_aggregation.py
python scripts/smoke_frontend_search_term_group_aggregation.py
python scripts/smoke_real_search_term_group_aggregation.py
python scripts/smoke_search_term_analysis.py
python scripts/smoke_frontend_search_terms.py
python scripts/smoke_real_e2e.py
npm run build
```

- `npm run build` 仍有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、积加客户端、同步服务、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
