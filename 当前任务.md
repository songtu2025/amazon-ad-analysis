# 当前任务

## TASK-082：异常队列空态原因可解释化

状态：已完成。

背景：
- TASK-080 已确认真实目标规则生效，当前真实 ACOS 未高于目标 ACOS，因此不应生成异常。
- TASK-081 已在产品中心展示“目标已配置 / 当前未触发异常”。
- 运营切到“异常与 AI 建议队列”时，如果仍只看到“暂无异常”，还可能误解为无数据或系统无效。

目标：
- 异常队列在真实异常数为 0 且存在已配置未触发异常的广告调优对象时，显示可解释空态。
- 空态文案说明“当前已配置对象均未越线”，而不是只显示“暂无异常”。
- 如果只是筛选条件导致表格为空，不误显示全局健康结论。
- 不改后端、不写真实库、不新增任何广告自动执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_anomaly_empty_reason.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_anomaly_empty_reason.py
python scripts\smoke_frontend_configured_no_anomaly_state.py
python scripts\smoke_frontend_anomaly_status_scope.py
python scripts\smoke_real_goal_rule_queue_gate.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 前端存在异常队列空态解释函数。
- 当 `anomalies.length === 0` 且存在已配置未触发异常产品时，空态显示“当前已配置对象均未越线”。
- 当 `displayedAnomalies` 为空但 `anomalies.length > 0` 时，空态只表达筛选无结果，不误判为全局健康。
- 空态提供回到“产品中心”查看目标配置的入口。
- 源码和脚本不出现任何自动执行广告动作字段或按钮。

完成依据：
- 新增 [smoke_frontend_anomaly_empty_reason.py](D:/DataProject/coedx_project/Amazon广告分析/scripts/smoke_frontend_anomaly_empty_reason.py)，先红灯确认异常队列缺少空态原因解释，再实现转绿。
- [App.tsx](D:/DataProject/coedx_project/Amazon广告分析/frontend/src/App.tsx) 新增 `configuredNoAnomalyProducts` 和 `renderAnomalyQueueEmptyState`。
- 异常队列在 `anomalies.length === 0` 且存在已配置未触发异常产品时，显示“当前已配置对象均未越线”。
- 空态说明“不是系统无数据或异常生成失败”，并显示已配置且未触发异常对象数量。
- 当 `displayedAnomalies.length === 0` 但 `anomalies.length > 0` 时，只显示“当前筛选条件下暂无异常”，不误判为全局健康。
- 空态提供“查看产品中心目标”入口，切回产品中心广告调优对象视图。
- 当前真实门禁仍返回 `workflow_status=no_anomaly_under_current_rule`、`queue_ready=false`，说明空态解释对应真实规则判断。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_anomaly_empty_reason.py
python scripts\smoke_frontend_configured_no_anomaly_state.py
python scripts\smoke_frontend_anomaly_status_scope.py
python scripts\smoke_real_goal_rule_queue_gate.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告执行能力。
- 按用户要求：本小任务完成后已执行 Product Design 复核。
- Product Design 复核结论：异常队列空态现在把“无异常”翻译成运营可判断的业务状态，同时保留筛选空态，避免把筛选无结果误说成全局健康。
- Product Design 复核发现：下一步应继续做真实日常使用的主链路巡检，优先从驾驶舱进入异常队列、产品中心和复盘页的跨页状态一致性开始。
