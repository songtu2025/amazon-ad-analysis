# 当前任务

## TASK-084：产品中心真实商品与广告草稿对象口径拆分

状态：已完成。

背景：
- 用户在产品中心指出：`RBK004 kids sunglasses` 这一行的数据来源和口径明显不对。
- 只读核对真实库后确认：该对象 `category = SP广告来源草稿`，`asin = null`，没有销售表现快照，不是销售表现接口同步来的真实商品。
- 该对象的 SP 花费、订单、ACOS 来自人工归因后的广告组指标，不能被展示成“真实产品身份”。
- 当前产品中心主表虽然在目标设置抽屉里识别了“产品身份待核查”，但表格主行仍容易让运营误以为这是已确认真实商品。

目标：
- 产品中心必须清楚区分“真实商品档案”和“广告来源草稿”。
- `SP广告来源草稿` 不再在主表里被表达为真实商品身份。
- 广告草稿对象可以继续出现在广告调优对象视图，但必须明确显示“广告对象待关联真实商品”。
- 已保存的目标 / 规则保留，但页面提示这些规则当前挂在广告草稿对象上，需关联真实商品后再作为产品级规则使用。
- 不改后端、不写真实库、不新增任何广告自动执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_ad_draft_product_center_clarity.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_ad_draft_product_center_clarity.py
python scripts\smoke_frontend_product_center_view_scope.py
python scripts\smoke_real_goal_rule_queue_gate.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 前端源码存在广告草稿对象在产品中心主表的独立展示逻辑。
- 广告草稿对象可见文案包含“广告对象待关联真实商品”。
- 广告草稿对象不再只以“真实产品身份”语境展示。
- 广告草稿对象的目标 / 规则区域提示“当前规则挂在广告草稿对象上”。
- 销售表现商品仍按销售档案展示，不受本次改动影响。
- 源码和脚本不出现任何自动执行广告动作字段或按钮。

完成依据：
- 新增 `scripts/smoke_frontend_ad_draft_product_center_clarity.py`，先红灯确认产品中心仍存在“真实产品身份”误导性表头，再实现转绿。
- `frontend/src/App.tsx` 将产品中心表头改为“产品 / 广告对象身份”。
- 广告草稿对象主行新增“广告对象待关联真实商品”和“不是销售表现商品档案”提示。
- 目标匹配区域新增“当前规则挂在广告草稿对象上，关联真实商品后再作为产品级规则使用”。
- `frontend/src/styles.css` 新增草稿对象口径提示样式。
- 浏览器复核时当前 in-app browser 点击产品中心页签未触发切换，已如实记录；源码 smoke、真实 API smoke 和构建均已通过。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_ad_draft_product_center_clarity.py
python scripts\smoke_frontend_product_center_view_scope.py
python scripts\smoke_real_goal_rule_queue_gate.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
