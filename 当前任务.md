# 当前任务

## TASK-087：产品中心商品族归属提示与人工路径

状态：已完成。

背景：
- TASK-086 已只读诊断：`RBK004-kids sunglasses-广泛` 当前更像商品族广告对象，而不是单一 SKU。
- 当前产品中心已有 `关联真实商品` 入口，但该入口仍容易让运营理解成必须选择一个具体 MSKU。
- 对精品运营来说，广告对象可能对应商品族 / 款式组；先明确归属层级，比强行绑定单 SKU 更重要。

目标：
- 产品中心广告草稿对象在具备商品族证据时，展示 `疑似商品族广告对象`。
- 展示最小证据：同系列候选数量、Top 销售占比、搜索词是否命中明确 ASIN / MSKU / SKU。
- 人工路径文案改为：先选择 / 创建商品族；如能确认单品，再细分到具体 SKU。
- 保留现有 `关联真实商品` 人工入口，但不暗示系统会自动迁移目标 / 规则。
- 不改后端、不写真实库、不新增任何广告自动执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_product_family_ad_object_path.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_product_family_ad_object_path.py
python scripts\smoke_frontend_ad_draft_rebind_action.py
python scripts\smoke_frontend_ad_draft_product_center_clarity.py
python scripts\smoke_ad_object_attribution_granularity.py
python scripts\smoke_real_goal_rule_queue_gate.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 前端源码存在商品族归属层级提示函数或渲染逻辑。
- 产品中心广告草稿对象可见 `疑似商品族广告对象`。
- 页面文案包含 `先选择 / 创建商品族` 和 `再细分到具体 SKU`。
- 页面展示候选数量、Top 销售占比、搜索词未命中明确 ASIN / MSKU / SKU 这类证据。
- 保留现有 `关联真实商品` 人工入口和本地归因边界文案。
- 源码和脚本不出现任何自动执行广告动作字段或按钮。

完成依据：
- 新增 `scripts/smoke_frontend_product_family_ad_object_path.py`，先红灯确认缺少商品族提示函数、证据文案和人工路径文案，再实现转绿。
- `frontend/src/App.tsx` 新增 `findProductFamilyCandidates` 和 `getAdObjectGranularityHint`，基于当前已加载产品和活跃归因证据判断疑似商品族广告对象。
- 产品中心广告草稿对象新增 `疑似商品族广告对象` 提示。
- 商品族提示展示 `同系列候选`、`Top 销售占比`、`搜索词未命中明确 ASIN / MSKU / SKU`。
- 人工路径文案调整为：`先选择 / 创建商品族`，如能确认单品，`再细分到具体 SKU`。
- 保留 `关联真实商品` 入口和“只更新本地归因关系和证据快照，不会修改广告”的人工边界。
- 浏览器复核：页面可正常加载且控制台无错误，但当前 in-app browser 仍无法通过 Ant Tabs 切换到产品中心；未把浏览器产品中心画面作为通过依据。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_product_family_ad_object_path.py
python scripts\smoke_frontend_ad_draft_rebind_action.py
python scripts\smoke_frontend_ad_draft_product_center_clarity.py
python scripts\smoke_ad_object_attribution_granularity.py
python scripts\smoke_real_goal_rule_queue_gate.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
- Product Design 复核结论：商品族提示能阻止运营继续把商品族广告对象误压成单 SKU；下一步应补真正的商品族人工归属模型，而不是只停留在提示。
