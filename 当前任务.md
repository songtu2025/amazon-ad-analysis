# 当前任务

## TASK-085：广告草稿对象关联真实商品的人工修正动作

状态：已完成。

背景：
- 用户指出产品中心 `RBK004 kids sunglasses` 行的数据来源和口径明显不对。
- 只读核对真实库确认：该对象 `category = SP广告来源草稿`、`asin = null`、没有销售表现快照，不是销售表现接口同步来的真实商品。
- TASK-084 已把页面文案改成“广告对象待关联真实商品”，但仍缺少一个直接的人工修正动作。
- 当前已有广告归因证据弹窗和 `bindAdSourceToProduct` 保存流程，可复用，不需要新增后端接口。

目标：
- 在产品中心的广告草稿对象行提供明确入口：`关联真实商品`。
- 点击后进入现有广告归因证据流程，并尽量带入该草稿对象已绑定的广告来源。
- 弹窗必须提示：本操作只更新本地归因关系和证据快照，不会修改广告。
- 不自动迁移草稿对象上的产品目标 / 规则，避免把错误规则带到真实商品。
- 不改后端、不写真实库结构、不新增任何广告自动执行能力。

允许修改：
- `frontend/src/App.tsx`
- `frontend/src/styles.css`
- `scripts/smoke_frontend_ad_draft_rebind_action.py`
- `当前任务.md`
- `project_state.json`
- `开发任务清单.md`
- `项目进展.md`

禁止修改：
- `.env`
- `.env.example`
- `frontend/.env.local`
- `backend/`
- `data/app.db`
- 任何广告执行接口或自动投放能力

验收命令：

```powershell
python scripts\smoke_frontend_ad_draft_rebind_action.py
python scripts\smoke_frontend_ad_draft_product_center_clarity.py
python scripts\smoke_frontend_product_center_view_scope.py
python scripts\smoke_real_goal_rule_queue_gate.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

成功标准：
- 广告草稿对象在产品中心主行可见 `关联真实商品` 人工动作入口。
- 该入口复用现有广告归因证据 / 人工确认流程。
- 源码文案明确说明只保存本地归因关系和证据快照，不修改广告。
- 源码不出现自动执行广告动作字段或按钮。

完成依据：
- 新增 `scripts/smoke_frontend_ad_draft_rebind_action.py`，先红灯确认缺少草稿对象关联真实商品处理函数和边界文案，再实现转绿。
- `frontend/src/App.tsx` 新增 `openAdDraftRealProductRebind`，从草稿对象已确认归因规则中带出广告来源，复用现有归因证据弹窗。
- 产品中心广告草稿对象主行新增 `关联真实商品` 人工动作入口。
- 归因产品下拉候选已排除 `SP广告来源草稿`，避免再次选择草稿对象。
- 归因证据弹窗新增说明：只更新本地归因关系和证据快照，不会修改广告；草稿对象上的产品目标 / 规则不会自动迁移。
- 浏览器复核时当前 in-app browser 点击产品中心页签仍未触发切换，已如实记录；源码 smoke、真实 API smoke 和构建均已通过。
- 验收命令已通过：

```powershell
python scripts\smoke_frontend_ad_draft_rebind_action.py
python scripts\smoke_frontend_ad_draft_product_center_clarity.py
python scripts\smoke_frontend_product_center_view_scope.py
python scripts\smoke_real_goal_rule_queue_gate.py
python scripts\smoke_real_e2e.py
cd frontend
npm run build
```

- `npm run build` 仍有既有 Vite chunk size warning，但构建成功。
- 未修改 `.env`、`.env.example`、`frontend/.env.local`、后端、真实 `data/app.db`。
- 未新增任何广告自动执行能力。
